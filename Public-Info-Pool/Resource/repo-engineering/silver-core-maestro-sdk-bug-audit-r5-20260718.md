# Silver Core Maestro SDK bug 审计第五轮战报（T56）

- 日期：2026-07-18
- 范围：`projects/silver-core-maestro-sdk/`（0.73.0 基线 → 0.74.0 落位）
- 方法（第五轮三镜头合编,动态编排工作流 `maestro-audit-r5`,9 代理零失败）：
  ① 第四轮新并发码的逐行对抗审读（回填分支 / 互斥 / CAS 均为未磨洗新面）；
  ② 上层件并发交错（驱动器 / 调度器 / workflow / goal / 送达,第四轮只锤了台账层）；
  ③ 四店矩阵模型化性质扫荡（素店 / CAS 店 / 混沌店 / 骗子店 × 有无 CAS,共六配置
  1,200 序列,**零翻红**——核心不变量经受住机器暴力,该 harness 收编为常驻测试
  `tests/r5-property-stores.test.ts`）。查证纪律照旧：默认驳回、亲手复现才入账。
- 账目：**原始 6 → 确认 6 → 驳回 0**（3 P2 + 3 P3）+ 4 leads 采纳 2,
  全处置配 fail-on-old 锁（`tests/audit-r5.test.ts`,13 锁含歼灭轮 3 条）。
- 产量曲线：29 → 16 → 0 → 11 → **6**。三条新码发现全落在第四轮自己的回填分支——
  印证「新码即新面」假设;上层三条则是「先结算后落行」窗口的消费面代价。

## 确认缺陷清单

### 新码审读镜头（全部落在 r4 回填分支）

| ID | 级 | 摘要 | 处置 |
|----|----|------|------|
| R5-3 | P2 | CAS 店上,对手宿主在结算赢家的 put→append 空窗内**无围栏回填**,一 attempt 落两条分歧行,规范化行与 lastError 自相矛盾（r4 注释承诺「输家永不落行」落空） | 回填一致性门加**错误文本比对**（失败类回填须 error === session.lastError——真赢家重试同载荷可愈,分歧对手被拒）;listQueries 规范化改 **last-wins**（复现竞跑中后落行者即结算赢家） |
| R5-2 | P3 | **retrying 态**结算的 append 崩溃无修复路径——文档承诺的一致重试回填只盖终态,重试在 transition('retrying') 上引爆,下一 attempt 认领后账行永失 | 增 retrying 态回填分支（同一致性门） |
| R5-1 | P3 | **省略 attempt** 的一致重试不能回填（守卫要求显式号,与「省略 = 视作当前」文档矛盾） | 回填判定 `result.attempt ?? session.attempts` |

### 上层交错镜头

| ID | 级 | 摘要 | 处置 |
|----|----|------|------|
| A1 | P2 | 「先结算后落行」窗口上漏：同实例读者见 done + 零行,WorkflowRun 把 **null 依赖摘要持久化**进 join 载荷、GoalChaser 拿缺数据判 verdict | getSession / listQueries 进**单会话互斥**（同实例窗口关死;跨进程窗口文档化） |
| A2 | P2 | 共驻驱动器租约清扫在发送中途结算送达审计会话,deliver() 以 InvalidTransitionError **拒绝**——回执丢失、已送达消息被记失败 | 送达簿记改 best-effort:租约竞态类错误吸收进回执,真店故障照抛;claimSession CAS 输局回执化（lead 一并采） |
| A3 | P3 | 「搁浅会话请修复」信号对**健康自愈会话**误发,且携带陈旧 running 快照——宿主照文档去修会伤活会话 | 驱动器补一次店重读:仍 running 才携当前记录发信,否则不携会话;事件文档同步 |

### Leads 处置

- **采**：分数节拍 fireAt 后缀被恢复解析器拒收（重启即静默重锚）→ 解析放宽
  `\d+(\.\d+)?`。艾瑞卡首刀曾把 `every` 收紧为整数,被 r2/r4 分数浮点守卫锁族
  当场打回——正解是修解析而非砍功能（过度修正反例,诚实记档）。
- **采**：deliver claimSession CAS 输局抛错 → 回执化（并入 A2 修复）。
- **记档不动**：ok 类回填无可比对文本（残余信任,已注释）;running 态调和分支
  单实例不可达（跨宿主可达,保留）。

## 变异歼灭轮

channel.ts 新吸收逻辑首测 100→93.02（3 存活:instanceof 窄化 ×2 / record 本体
no-op）→ 三条击杀锁（generic 错误必须照抛 ×2 / 簿记落账实证）→ **100% 复位**（43/43）。

## 验证

- maestro 335 全绿（28 文件;audit-r5 13 锁 + 收编性质矩阵 5 测）
- testbed 30 / agent 3080+2skip（版本卫兵曾抓 CHANGELOG 漏同步,即补）/ pytest 2981+4skip
- typecheck / dep-direction 段 A–D 绿;delivery-channel 变异 100% 地板保持
- 家族锁步 0.74.0

## 累计账（T56）

29 + 16 + 0 + 11 + **6** + 前期审查 5 = **确认真缺陷 67 / 500 上限**。
产量曲线 29→16→0→11→6:第五轮产量再降但未枯竭,且三条新码发现证明
「修复本身会造新面」——审计与修复进入追尾段。
