# 银芯 BIAV-SC 重新定位与循环支撑接口面 · 需求说明书

- 编号:SCS-REQ-REPOS-01(如与仓库现行 SCS-REQ 编号序冲突,由 Code 侧按序重编)
- 日期:2026-07-17(UTC+8)
- 状态:守密人已裁定,待实现
- 优先级:定位改写与文档(§2、§6)与接口面(§3)同期进行;§3 内部按 R1→R5 排序

---

## 1. 定位声明(终态)

银芯 BIAV-SC 是黑池五层介质架构中的**引擎层**:最慢的介质、版本化、可替换的商品泵。它的职责由三条否定与一条肯定完整定义:

1. **不认识斜杠。** 引擎不拦截、不解析、不展开任何以 `/` 开头的文本约定。用户输入长什么样、如何触发能力,是客户端层的 UX 决策。
2. **不持有钟。** 引擎不含任何跨 query 的定时、轮询、编排逻辑。钟属于 runner 守护层(宿主进程)。
3. **不追宿主面。** 引擎的宿主侧 API(query 签名、options、编排语义)按黑池需求自主演化,不以官方 Claude Code / claude-agent-sdk 的宿主侧形状为义务。
4. **只做一件事:把一次结构化的 agent 调用执行到位,并把模型侧表面保持在模型训练分布附近。**

模型侧表面(工具名、参数 schema、系统提示词习语)持续贴近官方形状——受益者是模型本身的调用稳定性,而非替换退路。直连 Messages API 的轻快响应是核心价值:任何提案若代价是引入 CLI 式中间层开销,直接拒绝。

**扩展面总原则**:引擎对外的全部扩展面限定为三种缝——**钩子**(引擎定义的时机槽位)、**工具**(模型侧能力)、**结构化配置**(装配期声明)。任何新需求先归类为时机、能力或声明之一,不引入第四种缝。

**钩子契约总则**:引擎拥有"何时"与"契约"(回调在哪些生命周期节点被调用、可见什么、返回什么、返回值如何影响引擎行为);宿主拥有"什么"(回调内容,引擎不知情也不关心)。回调的生命周期不得超出它所嵌的 query——在钩子内挂墙钟或等待外部世界变化,视为将 runner 层的钟走私进引擎,定位违规。此总则写入钩子文档,对既有与新增钩子一体适用。

## 2. 兼容政策改写

1. drop-in 兼容承诺退役。兼容基线不再构成待办来源。
2. COMPAT 文档降级为**参照笔记**:记录官方做法作为设计输入,逐条移除"待补齐"性质的义务表述。
3. 对标 chase 从常态任务改为两个触发条件:新模型发布(模型侧分布可能移动);银芯自行设计新的模型侧表面时(参考官方作业)。
4. 主权退路的论证更新:保障来自引擎自持与测试资产(单测全绿 + 变异纪律),不来自"可换回官方"。

## 3. 需求范围:循环支撑接口面

背景:黑池将于 runner 守护层自建无人值守 loop(周期轮询监控/采集,通宵常驻级)。引擎的义务是让该 loop **可被外部零补丁地干净构建**,而非自己成为 loop。交付以下六项:

### R1 续轮注入 API
公开 API:向一个已存在的会话追加一条新的用户轮并继续执行,返回该轮完整结果。要求:
- 与现有 sessions/resume 机制统一,不另起会话抽象;
- 宿主可在注入前读取会话的上下文占用与累计成本;
- 每轮注入支持附带结构化前置内容(供 R4 ledger 注入)。

### R2 预算事件流
在既有 maxBudgetUsd 基础上补结构化事件面:
- `budget:threshold`(可配置比例,如 80%)与 `budget:exhausted` 两级事件;
- `budget:exhausted` 触发时,当前轮完成后停止,并产出一份**收尾报告**(累计成本、轮次数、最后状态摘要),以结构化对象交给宿主;
- 事件通过引擎既有 hook/事件机制暴露,宿主订阅即可,不需轮询。

### R3 压缩钩子(保留区)
自动压缩发生时,允许宿主声明**不可丢弃的结构化上下文区**:
- 宿主注册的保留区内容原样进入压缩后的上下文;
- 保留区有大小上限并在超限时向宿主报错,而非静默截断;
- 典型用途:R4 的已报异常清单必须活过压缩,去重语义不能托付给有损摘要。

### R4 ledger 原语
去重账本的纯逻辑,引擎级测试纪律罩护:
- 记录已报告事件(键 + 时间戳 + 摘要)、查询是否已报、按容量/时限淘汰;
- 可序列化/反序列化(宿主自选存储介质,引擎不做持久化);
- 提供与 R1"结构化前置内容"和 R3"保留区"的适配函数,一行代码接入。

### R5 LoopControl 工具定义
模型侧表面,引擎出品并测试:
- 工具形状(初稿,以 chase 结果修正):`LoopControl { action: "propose_stop", reason: string }`;
- 语义:模型只能**提议**终止,提议以结构化事件抛给宿主,循环是否继续由宿主决定;引擎不因提议而改变自身行为;
- 该工具作为可选导出,由宿主在装配 loop 会话时注册;
- chase 前置(唯一一项):对照官方当前版本的命令工具面 schema(工具名、参数、命令列表注入方式),使 R5 形状贴近训练分布。若语料暂不可得,按上述自研 schema 先行,并在 COMPAT 参照笔记中标注"待对齐"。

### R6 引擎版本声明面
运行时可查询的引擎表面版本声明(引擎版本、工具面清单及各自版本),供热更新能力层(技能 md、MCP 清单)做加载时兼容校验。配套:在技能编写标准中增补 frontmatter 引擎兼容声明字段的规范条目。

## 4. 斜杠退役(引擎行为变更,一刀切净)

本节在**同一个版本内一次完成**,不设弃用期、不设兼容垫片、不保留双路径。理由:文本路径与结构化路径并存的过渡态是混淆源——两个真相来源会诱发后续错误与重复工作,其成本高于一次性切换。

1. 引擎不再对 prompt 文本做任何斜杠命令识别与展开;query 收到的 prompt 一律原样进入装配。
2. 既有 /loop、/goal 文本解析器与控制器(parseLoopCommand、createPromptLoop、goal 文本拼写接线等)**整体删除**:代码、公开导出、及其行为测试在同一提交内移除,不降级保留。输入解析属于客户端层——BPT 客户端若需文本命令,在其 UX 所在处自行实现(解析一条命令字符串是平凡工作,重写成本远低于维护一份"看似功能实则未接线"的困惑源)。
3. /goal 裁定:**机制保留在引擎,文本拼写与机制在同一版本内完成切换**。
   - 保留:Stop 拦截点、裁判反馈注回、impossible 逃生口、maxTurns/预算上限帽——全部发生在单次 query 生命周期内,不含墙钟,符合定位;goal 只是 agent 环的一种更严格停机条件。
   - 结构化:goal 作为 query options 的结构化配置项——目标描述、**评估器(宿主注入回调)**、逃生策略。评估器可为纯函数(确定性裁判:跑测试/断言,优先)或宿主自行发起的裁判模型调用(全新上下文、只看目标与交付物;用哪档模型、给多少预算是宿主的成本策略,引擎不硬编码任何模型选择)。引擎义务仅两条:目标未达时将裁判理由干净注回环内重驱,保住逃生口防死循环。
   - 切换即终态:结构化配置是 goal 的唯一入口,`/goal ...` 文本自本版本起为普通 prompt,无任何识别残留。
4. 以回归测试锁定终态:纯文本 `/loop ...`、`/goal ...` 作为 prompt 的 query 行为 = 普通文本透传;引擎源码内不存在斜杠识别逻辑(以搜索断言或等价手段防止回潮)。

## 5. 非目标(本期明确不做)

- loop 编排本体(间隔调度、通宵生命周期、报告投递、提议确认流)——归黑池 runner 守护层;
- Sleep 工具;schedule 跨进程持久化与崩溃恢复(衔接 T13,银芯侧仅在 T13 启动时交付唤醒缝协议,本期不做);
- 追平官方宿主侧任何新表面;
- 斜杠命令解析作为引擎行为;
- 任何形式的 CLI 式中间层。

## 6. 文档与落簿

1. POSITIONING 按 §1 重写;COMPAT 按 §2 改写为参照笔记。
2. decisions.md 新增两条覆盖注:
   - 创始 drop-in 兼容承诺退役,代之以"模型面贴近、宿主面自主"(理由:接口已超集、替换退路无人行使、CLI 中间层开销与轻快价值冲突、无第二消费者);
   - v0.59"/loop 不进引擎内建"之后曾议"内建化",终裁为"机制归引擎、钟与装配归宿主",斜杠解析退出引擎行为(理由:五层介质架构,钟属 runner 层,斜杠属客户端层)。
3. 本文自身入库归档;todo.md 相应任务项按 §3 拆分挂账。

## 7. 验收标准

1. **装配验收(核心)**:仓库内提供一个示例装配测试(examples 或集成测试),仅用 R1–R5 公开接口拼出如下行为并通过断言:同会话续轮循环、跨轮去重(ledger 注入且活过一次强制压缩)、预算到帽停机并产出收尾报告、模型经 LoopControl 发出的提议以结构化事件到达宿主且循环不受影响、goal 以结构化配置装配(宿主裁判回调判"未达标"时引擎注回理由重驱,impossible 逃生生效)。该测试即"黑池可零补丁装配"的证明,不引入任何真实钟(fake timer)。
2. 全部新增面有单测覆盖,纳入既有变异测试纪律,全量测试保持全绿。
3. 斜杠退役为一次性切净:纯文本 `/loop ...`、`/goal ...` 透传为普通 prompt(回归测试锁定),引擎源码与公开导出中无任何斜杠识别残留,无弃用期双路径。
4. §6 文档与落簿全部完成;交付物中不含版本对比性注记。
