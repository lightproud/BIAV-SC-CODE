diff --git a/agent/agent_init.py b/agent/agent_init.py
index 407f9a6..f863598 100644
--- a/agent/agent_init.py
+++ b/agent/agent_init.py
@@ -2437,7 +2437,7 @@ def init_agent(
         raise ValueError(
             f"Model {agent.model} has a context window of {_ctx:,} tokens, "
             f"which is below the minimum {MINIMUM_CONTEXT_LENGTH:,} required "
-            f"by Hermes Agent.  Choose a model with at least "
+            f"by Silver Core.  Choose a model with at least "
             f"{MINIMUM_CONTEXT_LENGTH // 1000}K context.  If your server "
             f"reports a window smaller than the model's true window, set "
             f"model.context_length in config.yaml to the real value "
diff --git a/agent/anthropic_adapter.py b/agent/anthropic_adapter.py
index 0d59d94..0fc5393 100644
--- a/agent/anthropic_adapter.py
+++ b/agent/anthropic_adapter.py
@@ -1,4 +1,4 @@
-"""Anthropic Messages API adapter for Hermes Agent.
+"""Anthropic Messages API adapter for Silver Core.
 
 Translates between Hermes's internal OpenAI-style message format and
 Anthropic's Messages API. Follows the same pattern as the codex_responses
@@ -2773,7 +2773,7 @@ def build_anthropic_kwargs(
         for block in system:
             if isinstance(block, dict) and block.get("type") == "text":
                 text = block.get("text", "")
-                text = text.replace("Hermes Agent", "Claude Code")
+                text = text.replace("Silver Core", "Claude Code")
                 text = text.replace("Hermes agent", "Claude Code")
                 text = text.replace("hermes-agent", "claude-code")
                 text = text.replace("Nous Research", "Anthropic")
diff --git a/agent/auxiliary_client.py b/agent/auxiliary_client.py
index acd22d8..7f6421b 100644
--- a/agent/auxiliary_client.py
+++ b/agent/auxiliary_client.py
@@ -615,7 +615,7 @@ _PROVIDERS_WITHOUT_VISION: frozenset = frozenset({
 # reads; the previous `X-OpenRouter-Title` label was not recognized there.
 _OR_HEADERS_BASE = {
     "HTTP-Referer": "https://hermes-agent.nousresearch.com",
-    "X-Title": "Hermes Agent",
+    "X-Title": "Silver Core",
     "X-OpenRouter-Categories": "productivity,cli-agent",
 }
 
@@ -736,7 +736,7 @@ from hermes_cli import __version__ as _HERMES_VERSION
 
 _AI_GATEWAY_HEADERS = {
     "HTTP-Referer": "https://hermes-agent.nousresearch.com",
-    "X-Title": "Hermes Agent",
+    "X-Title": "Silver Core",
     "User-Agent": f"HermesAgent/{_HERMES_VERSION}",
 }
 
@@ -806,7 +806,7 @@ def _codex_cloudflare_headers(access_token: str) -> Dict[str, str]:
     crash at client construction.
     """
     headers = {
-        "User-Agent": "codex_cli_rs/0.0.0 (Hermes Agent)",
+        "User-Agent": "codex_cli_rs/0.0.0 (Silver Core)",
         "originator": "codex_cli_rs",
     }
     if not isinstance(access_token, str) or not access_token.strip():
diff --git a/agent/bedrock_adapter.py b/agent/bedrock_adapter.py
index c399081..b01404a 100644
--- a/agent/bedrock_adapter.py
+++ b/agent/bedrock_adapter.py
@@ -1,4 +1,4 @@
-"""AWS Bedrock Converse API adapter for Hermes Agent.
+"""AWS Bedrock Converse API adapter for Silver Core.
 
 Provides native integration with Amazon Bedrock using the Converse API,
 bypassing the OpenAI-compatible endpoint in favor of direct AWS SDK calls.
diff --git a/agent/copilot_acp_client.py b/agent/copilot_acp_client.py
index 9cbdcd3..24e9583 100644
--- a/agent/copilot_acp_client.py
+++ b/agent/copilot_acp_client.py
@@ -630,7 +630,7 @@ class CopilotACPClient:
                     },
                     "clientInfo": {
                         "name": "hermes-agent",
-                        "title": "Hermes Agent",
+                        "title": "Silver Core",
                         "version": "0.0.0",
                     },
                 },
diff --git a/agent/credential_pool.py b/agent/credential_pool.py
index 08b0c0e..975e425 100644
--- a/agent/credential_pool.py
+++ b/agent/credential_pool.py
@@ -1619,7 +1619,7 @@ class CredentialPool:
                 continue
             # For anthropic claude_code entries, sync from the credentials file
             # before any status/refresh checks. This picks up tokens refreshed
-            # by other processes (Claude Code CLI, other Hermes profiles).
+            # by other processes (Claude Code CLI, other Silver Core profiles).
             if (self.provider == "anthropic" and entry.source == "claude_code"
                     and entry.last_status in {STATUS_EXHAUSTED, STATUS_DEAD}):
                 synced = self._sync_anthropic_entry_from_credentials_file(entry)
diff --git a/agent/file_safety.py b/agent/file_safety.py
index 2e08db3..cdf8b49 100644
--- a/agent/file_safety.py
+++ b/agent/file_safety.py
@@ -495,7 +495,7 @@ def get_cross_profile_warning(path: str) -> Optional[str]:
         return None
     return (
         f"Cross-profile write blocked by soft guard: {info['target_path']} "
-        f"belongs to Hermes profile {info['target_profile']!r}, but the "
+        f"belongs to Silver Core profile {info['target_profile']!r}, but the "
         f"agent is running under profile {info['active_profile']!r}. "
         f"Editing another profile's {info['area']}/ will affect that "
         f"profile's future sessions, not the one you are currently in. "
@@ -522,7 +522,7 @@ def get_cross_profile_warning(path: str) -> Optional[str]:
 #
 # This guard is path-shape-only: it detects the
 # ``…/sandboxes/<backend>/<task>/home/.hermes/…`` segment and warns
-# regardless of which Hermes profile is active. It does NOT cover the
+# regardless of which Silver Core profile is active. It does NOT cover the
 # inner-container case where the bind mount strips the ``sandboxes/`` prefix
 # (the agent's view inside the container is plain ``/root/.hermes/...``);
 # that case needs a separate dispatch-layer or host-side ``profile_state``
diff --git a/agent/insights.py b/agent/insights.py
index 9d148a1..c8d370b 100644
--- a/agent/insights.py
+++ b/agent/insights.py
@@ -1,11 +1,11 @@
 """
-Session Insights Engine for Hermes Agent.
+Session Insights Engine for Silver Core.
 
 Analyzes historical session data from the SQLite state database to produce
 comprehensive usage insights — token consumption, cost estimates, tool usage
 patterns, activity trends, model/platform breakdowns, and session metrics.
 
-Inspired by Claude Code's /insights command, adapted for Hermes Agent's
+Inspired by Claude Code's /insights command, adapted for Silver Core's
 multi-platform architecture with additional cost estimation and platform
 breakdown capabilities.
 
diff --git a/agent/lsp/__init__.py b/agent/lsp/__init__.py
index 7819162..5e8ed46 100644
--- a/agent/lsp/__init__.py
+++ b/agent/lsp/__init__.py
@@ -1,4 +1,4 @@
-"""Language Server Protocol (LSP) integration for Hermes Agent.
+"""Language Server Protocol (LSP) integration for Silver Core.
 
 Hermes runs full language servers (pyright, gopls, rust-analyzer,
 typescript-language-server, etc.) as subprocesses and pipes their
diff --git a/agent/model_metadata.py b/agent/model_metadata.py
index cec891d..84ffbc0 100644
--- a/agent/model_metadata.py
+++ b/agent/model_metadata.py
@@ -273,7 +273,7 @@ CONTEXT_PROBE_TIERS = [
 # Default context length when no detection method succeeds.
 DEFAULT_FALLBACK_CONTEXT = CONTEXT_PROBE_TIERS[0]
 
-# Minimum context length required to run Hermes Agent.  Models with fewer
+# Minimum context length required to run Silver Core.  Models with fewer
 # tokens cannot maintain enough working memory for tool-calling workflows.
 # Sessions, model switches, and cron jobs should reject models below this.
 MINIMUM_CONTEXT_LENGTH = 64_000
diff --git a/agent/plugin_llm.py b/agent/plugin_llm.py
index 8fcd336..8f7eefd 100644
--- a/agent/plugin_llm.py
+++ b/agent/plugin_llm.py
@@ -2,7 +2,7 @@
 Plugin LLM facade — host-owned LLM access for trusted plugins.
 ==============================================================
 
-Plugins built on Hermes Agent often need to make their own LLM calls
+Plugins built on Silver Core often need to make their own LLM calls
 out-of-band — a hook that rewrites a tool error before the user sees
 it, a gateway adapter that translates inbound text, a slash command
 that summarises a paste, a scheduled job that scores yesterday's
diff --git a/agent/portal_tags.py b/agent/portal_tags.py
index ecfecdc..1b2f2a4 100644
--- a/agent/portal_tags.py
+++ b/agent/portal_tags.py
@@ -3,7 +3,7 @@
 Every Hermes request that hits the Nous Portal — main agent loop, auxiliary
 client (compression / titles / vision / web_extract / session_search / etc.),
 and any future code path — must carry the same product-attribution tags so
-Nous can attribute usage to Hermes Agent and bucket it by client release.
+Nous can attribute usage to Silver Core and bucket it by client release.
 
 Tag shape (sent in OpenAI-compatible ``extra_body['tags']``):
 
diff --git a/agent/prompt_builder.py b/agent/prompt_builder.py
index 6bd88fa..927b9c0 100644
--- a/agent/prompt_builder.py
+++ b/agent/prompt_builder.py
@@ -142,7 +142,7 @@ def _strip_yaml_frontmatter(content: str) -> str:
 # =========================================================================
 
 DEFAULT_AGENT_IDENTITY = (
-    "You are Hermes Agent, an intelligent AI assistant created by Nous Research. "
+    "You are Silver Core, an intelligent AI assistant. "
     "You are helpful, knowledgeable, and direct. You assist users with a wide "
     "range of tasks including answering questions, writing and editing code, "
     "analyzing information, creative work, and executing actions via your tools. "
@@ -152,7 +152,7 @@ DEFAULT_AGENT_IDENTITY = (
 )
 
 HERMES_AGENT_HELP_GUIDANCE = (
-    "You run on Hermes Agent (by Nous Research). When the user needs help with "
+    "You run on Silver Core (by Nous Research). When the user needs help with "
     "Hermes itself — configuring, setting up, using, extending, or troubleshooting "
     "it — or when you need to understand your own features, tools, or capabilities, "
     "the documentation at https://hermes-agent.nousresearch.com/docs is your "
@@ -1845,7 +1845,7 @@ def build_skills_system_prompt(
             "for tasks like code review, planning, and testing — load them even for tasks you "
             "already know how to do, because the skill defines how it should be done here.\n"
             "Whenever the user asks you to configure, set up, install, enable, disable, modify, "
-            "or troubleshoot Hermes Agent itself — its CLI, config, models, providers, tools, "
+            "or troubleshoot Silver Core itself — its CLI, config, models, providers, tools, "
             "skills, voice, gateway, plugins, or any feature — load the `hermes-agent` skill "
             "first. It has the actual commands (e.g. `hermes config set …`, `hermes tools`, "
             "`hermes setup`) so you don't have to guess or invent workarounds.\n"
diff --git a/agent/relay_runtime.py b/agent/relay_runtime.py
index 5336047..68ec1c8 100644
--- a/agent/relay_runtime.py
+++ b/agent/relay_runtime.py
@@ -405,7 +405,7 @@ RelayHost = RelayRuntime | NoopRelayRuntime
 
 
 class RelayHostRegistry:
-    """Own exactly one Relay host for each canonical Hermes profile."""
+    """Own exactly one Relay host for each canonical Silver Core profile."""
 
     def __init__(self) -> None:
         self._lock = threading.RLock()
@@ -959,7 +959,7 @@ def get_runtime(
     create: bool = True,
     profile_key: str | None = None,
 ) -> RelayRuntime | None:
-    """Return the Relay host for the active Hermes profile."""
+    """Return the Relay host for the active Silver Core profile."""
     host = HOST_REGISTRY.for_profile(profile_key, create=create)
     return host if isinstance(host, RelayRuntime) else None
 
diff --git a/agent/ssl_guard.py b/agent/ssl_guard.py
index ac1b784..64d0010 100644
--- a/agent/ssl_guard.py
+++ b/agent/ssl_guard.py
@@ -1,4 +1,4 @@
-"""Preventive SSL CA certificate checks for Hermes Agent.
+"""Preventive SSL CA certificate checks for Silver Core.
 
 This module catches broken CA bundle paths before OpenAI/httpx turns them into
 opaque ``FileNotFoundError: [Errno 2] No such file or directory`` failures.
diff --git a/agent/system_prompt.py b/agent/system_prompt.py
index 8b8832c..63225a2 100644
--- a/agent/system_prompt.py
+++ b/agent/system_prompt.py
@@ -396,7 +396,7 @@ def build_system_prompt_parts(agent: Any, system_message: Optional[str] = None)
             # Probe failure must never block prompt build.
             pass
 
-    # Active-profile hint — names the Hermes profile the agent is running
+    # Active-profile hint — names the Silver Core profile the agent is running
     # under so it doesn't conflate ~/.hermes/skills/ (default profile) with
     # ~/.hermes/profiles/<active>/skills/ (this profile's). Deterministic
     # for the lifetime of the agent — profile name doesn't change
@@ -410,7 +410,7 @@ def build_system_prompt_parts(agent: Any, system_message: Optional[str] = None)
         active_profile = "default"
     if active_profile == "default":
         post_workspace_parts.append(
-            "Active Hermes profile: default. Other profiles (if any) live "
+            "Active Silver Core profile: default. Other profiles (if any) live "
             "under " + str(get_hermes_home()) + "/profiles/<name>/. Each profile has its own "
             "skills/, plugins/, cron/, and memories/ that affect a different "
             "session than this one. Do not modify another profile's "
@@ -419,7 +419,7 @@ def build_system_prompt_parts(agent: Any, system_message: Optional[str] = None)
         )
     else:
         post_workspace_parts.append(
-            f"Active Hermes profile: {active_profile}. This session reads "
+            f"Active Silver Core profile: {active_profile}. This session reads "
             f"and writes {get_hermes_home()}/profiles/{active_profile}/. The default "
             f"profile's data lives at {get_hermes_home()}/skills/, {get_hermes_home()}/plugins/, "
             f"{get_hermes_home()}/cron/, {get_hermes_home()}/memories/ — those belong to a "
diff --git a/agent/transports/codex_app_server.py b/agent/transports/codex_app_server.py
index c23ff83..a70dc8c 100644
--- a/agent/transports/codex_app_server.py
+++ b/agent/transports/codex_app_server.py
@@ -160,7 +160,7 @@ class CodexAppServerClient:
     def initialize(
         self,
         client_name: str = "hermes",
-        client_title: str = "Hermes Agent",
+        client_title: str = "Silver Core",
         client_version: str = "0.1",
         capabilities: Optional[dict] = None,
         timeout: float = 10.0,
diff --git a/agent/transports/codex_app_server_session.py b/agent/transports/codex_app_server_session.py
index e2ace75..97b1df4 100644
--- a/agent/transports/codex_app_server_session.py
+++ b/agent/transports/codex_app_server_session.py
@@ -324,7 +324,7 @@ class CodexAppServerSession:
             )
         self._client.initialize(
             client_name="hermes",
-            client_title="Hermes Agent",
+            client_title="Silver Core",
             client_version=_get_hermes_version(),
         )
         # Permission selection is intentionally NOT sent on thread/start.
diff --git a/agent/transports/hermes_tools_mcp_server.py b/agent/transports/hermes_tools_mcp_server.py
index 7fc23cf..f4b9e38 100644
--- a/agent/transports/hermes_tools_mcp_server.py
+++ b/agent/transports/hermes_tools_mcp_server.py
@@ -169,7 +169,7 @@ def _build_server() -> Any:
     mcp = FastMCP(
         "hermes-tools",
         instructions=(
-            "Hermes Agent's tool surface, exposed for use inside a Codex "
+            "Silver Core's tool surface, exposed for use inside a Codex "
             "session. Use these for capabilities Codex's built-in toolset "
             "doesn't cover: web search/extract, browser automation, "
             "subagent delegation, vision, image generation, persistent "
diff --git a/agent/vertex_adapter.py b/agent/vertex_adapter.py
index 6e42575..f3e5b73 100644
--- a/agent/vertex_adapter.py
+++ b/agent/vertex_adapter.py
@@ -1,4 +1,4 @@
-"""Vertex AI (Google Cloud) adapter for Hermes Agent.
+"""Vertex AI (Google Cloud) adapter for Silver Core.
 
 Provides authentication and configuration for Vertex AI's OpenAI-compatible
 endpoint. This allows Hermes to use Gemini models via Google Cloud with
diff --git a/gateway/kanban_watchers.py b/gateway/kanban_watchers.py
index 2f5137a..23ee526 100644
--- a/gateway/kanban_watchers.py
+++ b/gateway/kanban_watchers.py
@@ -1282,7 +1282,7 @@ class GatewayKanbanWatchersMixin:
 
         def _ready_nonempty() -> bool:
             """Cheap probe: is there at least one ready+assigned+unclaimed
-            task on ANY board whose assignee maps to a real Hermes profile
+            task on ANY board whose assignee maps to a real Silver Core profile
             (i.e. one the dispatcher would actually spawn for)?
 
             Tasks assigned to control-plane lanes (e.g. ``orion-cc``,
@@ -1290,7 +1290,7 @@ class GatewayKanbanWatchersMixin:
             ``claim_task`` directly and never spawnable, so a queue full
             of those is "correctly idle", not "stuck". Filtering them out
             here keeps the stuck-warn fire only on real failures (broken
-            PATH, missing venv, credential loss for a real Hermes profile).
+            PATH, missing venv, credential loss for a real Silver Core profile).
             """
             try:
                 boards = _kb.list_boards(include_archived=False)
diff --git a/gateway/platforms/api_server.py b/gateway/platforms/api_server.py
index 62dff28..ccabcaf 100644
--- a/gateway/platforms/api_server.py
+++ b/gateway/platforms/api_server.py
@@ -96,7 +96,7 @@ logger = logging.getLogger(__name__)
 
 
 def _hermes_version() -> str:
-    """Return the canonical Hermes Agent version string.
+    """Return the canonical Silver Core version string.
 
     ``hermes_cli.__version__`` is the runtime source of truth used by the CLI,
     dashboard, portal tags, and release script. Prefer it over installed
diff --git a/gateway/platforms/base.py b/gateway/platforms/base.py
index d7654ff..bf607b0 100644
--- a/gateway/platforms/base.py
+++ b/gateway/platforms/base.py
@@ -1333,7 +1333,7 @@ def _media_delivery_denied_paths() -> List[Path]:
     home = Path(os.path.expanduser("~"))
     for sub in _MEDIA_DELIVERY_DENIED_HOME_SUBPATHS:
         denied.append(home / sub)
-    # The active Hermes profile and shared Hermes root both contain control
+    # The active Silver Core profile and shared Hermes root both contain control
     # files and credentials. Only cache subdirectories under them are
     # explicitly allowlisted above (matched BEFORE this denylist in
     # validate_media_delivery_path, so generated media still delivers).
diff --git a/gateway/platforms/weixin.py b/gateway/platforms/weixin.py
index b44ce1e..f7f5aed 100644
--- a/gateway/platforms/weixin.py
+++ b/gateway/platforms/weixin.py
@@ -1,7 +1,7 @@
 """
 Weixin platform adapter.
 
-Connects Hermes Agent to WeChat personal accounts via Tencent's iLink Bot API.
+Connects Silver Core to WeChat personal accounts via Tencent's iLink Bot API.
 
 Design notes:
 - Long-poll ``getupdates`` drives inbound delivery.
diff --git a/gateway/platforms/whatsapp_common.py b/gateway/platforms/whatsapp_common.py
index 70f77b2..ea9009d 100644
--- a/gateway/platforms/whatsapp_common.py
+++ b/gateway/platforms/whatsapp_common.py
@@ -54,7 +54,7 @@ class WhatsAppBehaviorMixin:
     MAX_MESSAGE_LENGTH: int = 4096
     supports_code_blocks = True  # WhatsApp renders fenced code blocks (monospace)
 
-    DEFAULT_REPLY_PREFIX: str = "⚕ *Hermes Agent*\n────────────\n"
+    DEFAULT_REPLY_PREFIX: str = "⚕ *Silver Core*\n────────────\n"
 
     _OUTBOUND_INVISIBLE_CHARS_RE = re.compile(r"[\u200b\u2060\u2063\ufeff]")
     _OUTBOUND_ODD_SPACE_RE = re.compile(r"[\u00a0\u1680\u180e\u2000-\u200a\u202f\u205f\u3000]")
diff --git a/gateway/relay/__init__.py b/gateway/relay/__init__.py
index 77481d8..18b480f 100644
--- a/gateway/relay/__init__.py
+++ b/gateway/relay/__init__.py
@@ -284,10 +284,10 @@ def relay_display_name() -> Optional[str]:
         except Exception:  # noqa: BLE001 - branding absence must never crash boot
             value = ""
         # The stock brand name is IDENTICAL on every default install, so in a
-        # multi-agent scope it would prefix every reply "**Hermes Agent:**" —
+        # multi-agent scope it would prefix every reply "**Silver Core:**" —
         # shadowing the connector's linked-owner fallback, which actually
         # disambiguates. Only a deliberately customized name is forwarded.
-        if value == "Hermes Agent":
+        if value == "Silver Core":
             value = ""
     # Mirror the connector's ingest sanitization (trim + 64-char cap) so what
     # we send is what gets stored.
diff --git a/gateway/relay/command_manifest.py b/gateway/relay/command_manifest.py
index 36d7a8f..c1db253 100644
--- a/gateway/relay/command_manifest.py
+++ b/gateway/relay/command_manifest.py
@@ -113,7 +113,7 @@ def build_relay_command_manifest() -> List[Dict[str, Any]]:
             "description": "Re-scan skills for new or removed entries",
         },
         {"name": "voice", "description": "Toggle voice reply mode"},
-        {"name": "update", "description": "Update Hermes Agent to the latest version"},
+        {"name": "update", "description": "Update Silver Core to the latest version"},
         {"name": "restart", "description": "Gracefully restart the Hermes gateway"},
         {
             "name": "approve",
diff --git a/gateway/slash_commands.py b/gateway/slash_commands.py
index 6cbd017..6f4de3d 100644
--- a/gateway/slash_commands.py
+++ b/gateway/slash_commands.py
@@ -1632,7 +1632,7 @@ class GatewaySlashCommandsMixin:
         return EphemeralReply(t("gateway.restart.restarting"))
 
     async def _handle_version_command(self, event: MessageEvent) -> str:
-        """Handle /version — show the running Hermes Agent version."""
+        """Handle /version — show the running Silver Core version."""
         from hermes_cli.slash_exec import CommandContext, execute_command
 
         return execute_command("version", CommandContext(surface="gateway")).text
@@ -5329,7 +5329,7 @@ class GatewaySlashCommandsMixin:
         return await loop.run_in_executor(None, _collect_and_upload)
 
     async def _handle_update_command(self, event: MessageEvent) -> str:
-        """Handle /update command — update Hermes Agent to the latest version.
+        """Handle /update command — update Silver Core to the latest version.
 
         Spawns ``hermes update`` in a detached session (via ``setsid``) so it
         survives the gateway restart that ``hermes update`` may trigger. Marker
@@ -5357,7 +5357,7 @@ class GatewaySlashCommandsMixin:
                 return t("gateway.update.platform_not_messaging")
 
         if is_managed():
-            return f"✗ {format_managed_message('update Hermes Agent')}"
+            return f"✗ {format_managed_message('update Silver Core')}"
 
         project_root = Path(__file__).parent.parent.resolve()
         git_dir = project_root / '.git'
diff --git a/hermes_cli/__init__.py b/hermes_cli/__init__.py
index c87e06c..b132a24 100644
--- a/hermes_cli/__init__.py
+++ b/hermes_cli/__init__.py
@@ -1,5 +1,5 @@
 """
-Hermes CLI - Unified command-line interface for Hermes Agent.
+Hermes CLI - Unified command-line interface for Silver Core.
 
 Provides subcommands for:
 - hermes chat          - Interactive chat (same as ./hermes)
diff --git a/hermes_cli/_parser.py b/hermes_cli/_parser.py
index cb5be6b..5f36411 100644
--- a/hermes_cli/_parser.py
+++ b/hermes_cli/_parser.py
@@ -91,7 +91,7 @@ def build_top_level_parser():
     """
     parser = argparse.ArgumentParser(
         prog="hermes",
-        description="Hermes Agent - AI assistant with tool-calling capabilities",
+        description="Silver Core - AI assistant with tool-calling capabilities",
         formatter_class=argparse.RawDescriptionHelpFormatter,
         epilog=_EPILOGUE,
     )
@@ -269,7 +269,7 @@ def build_top_level_parser():
     chat_parser = subparsers.add_parser(
         "chat",
         help="Interactive chat with the agent",
-        description="Start an interactive chat session with Hermes Agent",
+        description="Start an interactive chat session with Silver Core",
     )
     chat_parser.add_argument(
         "-q", "--query", help="Single query (non-interactive mode)"
diff --git a/hermes_cli/auth.py b/hermes_cli/auth.py
index 0d08007..4579db2 100644
--- a/hermes_cli/auth.py
+++ b/hermes_cli/auth.py
@@ -1,5 +1,5 @@
 """
-Multi-provider authentication system for Hermes Agent.
+Multi-provider authentication system for Silver Core.
 
 Supports OAuth device code flows (Nous Portal, future: OpenAI Codex) and
 traditional API key providers (OpenRouter, custom endpoints). Auth state
diff --git a/hermes_cli/banner.py b/hermes_cli/banner.py
index 7811899..a126f89 100644
--- a/hermes_cli/banner.py
+++ b/hermes_cli/banner.py
@@ -487,7 +487,7 @@ def get_latest_release_tag(repo_dir: Optional[Path] = None) -> Optional[tuple]:
 
 def format_banner_version_label() -> str:
     """Return the version label shown in the startup banner title."""
-    base = f"Hermes Agent v{VERSION} ({RELEASE_DATE})"
+    base = f"Silver Core v{VERSION} ({RELEASE_DATE})"
     state = get_git_banner_state()
     if not state:
         return base
diff --git a/hermes_cli/build_info.py b/hermes_cli/build_info.py
index e4cc6f0..2178363 100644
--- a/hermes_cli/build_info.py
+++ b/hermes_cli/build_info.py
@@ -1,5 +1,5 @@
 """
-Baked-in build metadata for Hermes Agent.
+Baked-in build metadata for Silver Core.
 
 Source installs report their git revision live via ``git rev-parse`` (see
 ``hermes_cli/dump.py`` and ``hermes_cli/banner.py``).  That doesn't work inside
diff --git a/hermes_cli/cli_billing_mixin.py b/hermes_cli/cli_billing_mixin.py
index b0ec388..562e09f 100644
--- a/hermes_cli/cli_billing_mixin.py
+++ b/hermes_cli/cli_billing_mixin.py
@@ -852,7 +852,7 @@ class CLIBillingMixin:
             _cprint(f"  {_d('Remote spending is off for this org.')}")
             self._billing_portal_hint(
                 state,
-                reason="A billing admin can turn it on from the portal's Hermes Agent page to add funds here.",
+                reason="A billing admin can turn it on from the portal's Silver Core page to add funds here.",
             )
             return
 
@@ -967,7 +967,7 @@ class CLIBillingMixin:
             _cprint(f"  💳 {_d('Remote spending is off for this org.')}")
             self._billing_portal_hint(
                 state,
-                reason="A billing admin can turn it on from the portal's Hermes Agent page before adding funds.",
+                reason="A billing admin can turn it on from the portal's Silver Core page before adding funds.",
             )
             return False
         return True
@@ -1240,7 +1240,7 @@ class CLIBillingMixin:
             print("  💳 No card on file — top up and manage billing on the portal.")
         elif code in ("cli_billing_disabled", "remote_spending_disabled") or \
                 getattr(exc, "code", None) == "remote_spending_disabled":
-            print("  Remote spending is off for this account — a billing admin can turn it on from the portal's Hermes Agent page.")
+            print("  Remote spending is off for this account — a billing admin can turn it on from the portal's Silver Core page.")
         elif code == "role_required":
             print("  Adding funds needs an org admin/owner. Ask an admin, or manage on the portal.")
         elif code == "idempotency_conflict":
@@ -1317,7 +1317,7 @@ class CLIBillingMixin:
 
         fresh = build_billing_state()
         if not (fresh.logged_in and fresh.cli_billing_enabled):
-            print("  Remote Spending is allowed for this terminal, but it's still off for this org. A billing admin can turn it on from the portal's Hermes Agent page, then run /topup again.")
+            print("  Remote Spending is allowed for this terminal, but it's still off for this org. A billing admin can turn it on from the portal's Silver Core page, then run /topup again.")
             self._billing_portal_hint(fresh)
             return
 
diff --git a/hermes_cli/cli_commands_mixin.py b/hermes_cli/cli_commands_mixin.py
index c9ae879..ea10d06 100644
--- a/hermes_cli/cli_commands_mixin.py
+++ b/hermes_cli/cli_commands_mixin.py
@@ -3138,7 +3138,7 @@ class CLICommandsMixin:
         run_debug_share(args)
 
     def _handle_update_command(self) -> bool:
-        """Handle /update — update Hermes Agent to the latest version.
+        """Handle /update — update Silver Core to the latest version.
 
         In the classic CLI this exits the session and relaunches as
         ``hermes update`` so the user sees update output directly and gets
@@ -3152,7 +3152,7 @@ class CLICommandsMixin:
         from hermes_cli.config import is_managed, format_managed_message
 
         if is_managed():
-            print(f"  ✗ {format_managed_message('update Hermes Agent')}")
+            print(f"  ✗ {format_managed_message('update Silver Core')}")
             return False
 
         # Use the prompt_toolkit-native modal so the confirmation panel
@@ -3160,11 +3160,11 @@ class CLICommandsMixin:
         # with the prompt_toolkit event loop (same pattern as
         # _confirm_destructive_slash).
         choices = [
-            ("once", "Update Now", "exit the current session and update Hermes Agent"),
+            ("once", "Update Now", "exit the current session and update Silver Core"),
             ("cancel", "Cancel", "keep the current session"),
         ]
         raw = self._prompt_text_input_modal(
-            title="⚕  Update Hermes Agent",
+            title="⚕  Update Silver Core",
             detail="This will exit the current session and run `hermes update`.",
             choices=choices,
         )
diff --git a/hermes_cli/commands.py b/hermes_cli/commands.py
index 52c7bc9..18d3777 100644
--- a/hermes_cli/commands.py
+++ b/hermes_cli/commands.py
@@ -318,9 +318,9 @@ COMMAND_REGISTRY: list[CommandDef] = [
                cli_only=True),
     CommandDef("image", "Attach a local image file for your next prompt", "Info",
                cli_only=True, args_hint="<path>"),
-    CommandDef("update", "Update Hermes Agent to the latest version", "Info",
+    CommandDef("update", "Update Silver Core to the latest version", "Info",
                busy_policy="dispatch"),
-    CommandDef("version", "Show Hermes Agent version", "Info", aliases=("v",),
+    CommandDef("version", "Show Silver Core version", "Info", aliases=("v",),
                busy_policy="dispatch", execute="version"),
     CommandDef("debug", "Upload debug report (system info + logs) and get shareable links", "Info",
                args_hint="[nous|local]"),
diff --git a/hermes_cli/completion.py b/hermes_cli/completion.py
index cd4815e..41dfa26 100644
--- a/hermes_cli/completion.py
+++ b/hermes_cli/completion.py
@@ -97,7 +97,7 @@ def generate_bash(parser: argparse.ArgumentParser) -> str:
 
     cases_str = "\n".join(cases)
 
-    return f"""# Hermes Agent bash completion
+    return f"""# Silver Core bash completion
 # Add to ~/.bashrc:
 #   eval "$(hermes completion bash)"
 
@@ -200,7 +200,7 @@ def generate_zsh(parser: argparse.ArgumentParser) -> str:
     sub_cases_str = "\n".join(sub_cases)
 
     return f"""#compdef hermes
-# Hermes Agent zsh completion
+# Silver Core zsh completion
 # Add to ~/.zshrc:
 #   eval "$(hermes completion zsh)"
 
@@ -254,7 +254,7 @@ def generate_fish(parser: argparse.ArgumentParser) -> str:
     top_cmds_str = " ".join(top_cmds)
 
     lines: list[str] = [
-        "# Hermes Agent fish completion",
+        "# Silver Core fish completion",
         "# Add to your config:",
         "#   hermes completion fish | source",
         "",
diff --git a/hermes_cli/config.py b/hermes_cli/config.py
index 4523f92..ea79417 100644
--- a/hermes_cli/config.py
+++ b/hermes_cli/config.py
@@ -1,5 +1,5 @@
 """
-Configuration management for Hermes Agent.
+Configuration management for Silver Core.
 
 Config files are stored in ~/.hermes/ for easy access:
 - ~/.hermes/config.yaml  - All settings (model, toolsets, terminal, etc.)
@@ -568,7 +568,7 @@ def recommended_update_command() -> str:
 _DOCKER_UPDATE_MESSAGE = """\
 ✗ ``hermes update`` doesn't apply inside the Docker container.
 
-Hermes Agent runs as a published image (nousresearch/hermes-agent), not a
+Silver Core runs as a published image (nousresearch/hermes-agent), not a
 git checkout — the container has no working tree to pull into.  Update by
 pulling a fresh image and restarting your container instead:
 
diff --git a/hermes_cli/config_defaults.py b/hermes_cli/config_defaults.py
index dd1cfe8..da82fab 100644
--- a/hermes_cli/config_defaults.py
+++ b/hermes_cli/config_defaults.py
@@ -1,4 +1,4 @@
-"""Default configuration data for Hermes Agent.
+"""Default configuration data for Silver Core.
 
 Pure-data leaf module: DEFAULT_CONFIG and OPTIONAL_ENV_VARS, extracted
 verbatim from hermes_cli/config.py. Must not import from hermes_cli.config.
@@ -1887,7 +1887,7 @@ DEFAULT_CONFIG = {
     # WhatsApp platform settings (gateway mode)
     "whatsapp": {
         # Reply prefix prepended to every outgoing WhatsApp message.
-        # Default (None) uses the built-in "⚕ *Hermes Agent*" header.
+        # Default (None) uses the built-in "⚕ *Silver Core*" header.
         # Set to "" (empty string) to disable the header entirely.
         # Supports \n for newlines, e.g. "🤖 *My Bot*\n──────\n"
     },
diff --git a/hermes_cli/dashboard_auth/login_page.py b/hermes_cli/dashboard_auth/login_page.py
index 1ccdfcb..921a209 100644
--- a/hermes_cli/dashboard_auth/login_page.py
+++ b/hermes_cli/dashboard_auth/login_page.py
@@ -38,7 +38,7 @@ _LOGIN_HTML_TEMPLATE = """\
 <head>
 <meta charset="utf-8">
 <meta name="viewport" content="width=device-width, initial-scale=1">
-<title>Sign in — Hermes Agent</title>
+<title>Sign in — Silver Core</title>
 <style>
   /* Brand fonts shipped by @nous-research/ui — same files the SPA loads. */
   @font-face {{
@@ -305,7 +305,7 @@ _LOGIN_HTML_TEMPLATE = """\
   <div class="brand">Nous<span class="dot"></span>Research</div>
   <div class="card">
     <h1>Sign in</h1>
-    <p class="subtitle">Choose a sign-in method to continue to the Hermes Agent dashboard.</p>
+    <p class="subtitle">Choose a sign-in method to continue to the Silver Core dashboard.</p>
     <div class="provider-list">
 {provider_buttons}
     </div>
@@ -325,7 +325,7 @@ _EMPTY_HTML = """\
 <head>
 <meta charset="utf-8">
 <meta name="viewport" content="width=device-width, initial-scale=1">
-<title>Sign-in unavailable — Hermes Agent</title>
+<title>Sign-in unavailable — Silver Core</title>
 <style>
   @font-face {
     font-family: 'Collapse';
diff --git a/hermes_cli/debug.py b/hermes_cli/debug.py
index b5a7852..59d8d92 100644
--- a/hermes_cli/debug.py
+++ b/hermes_cli/debug.py
@@ -1,4 +1,4 @@
-"""``hermes debug`` debug tools for Hermes Agent.
+"""``hermes debug`` debug tools for Silver Core.
 
 Currently supports:
     hermes debug share    Upload debug report (system info + logs) to a
diff --git a/hermes_cli/default_soul.py b/hermes_cli/default_soul.py
index f4a6281..b4c2376 100644
--- a/hermes_cli/default_soul.py
+++ b/hermes_cli/default_soul.py
@@ -1,7 +1,7 @@
 """Default SOUL.md template seeded into HERMES_HOME on first run."""
 
 DEFAULT_SOUL_MD = (
-    "You are Hermes Agent, an intelligent AI assistant created by Nous Research. "
+    "You are Silver Core, an intelligent AI assistant. "
     "You are helpful, knowledgeable, and direct. You assist users with a wide "
     "range of tasks including answering questions, writing and editing code, "
     "analyzing information, creative work, and executing actions via your tools. "
@@ -22,7 +22,7 @@ DEFAULT_SOUL_MD = (
 # safety guarantee is that these strings carry zero user intent.
 _LEGACY_TEMPLATE_SOULS = (
     (
-        "# Hermes Agent Persona\n"
+        "# Silver Core Persona\n"
         "\n"
         "<!--\n"
         "This file defines the agent's personality and tone.\n"
@@ -42,7 +42,7 @@ _LEGACY_TEMPLATE_SOULS = (
     # block / trailing newline in some historical revisions; the bare scaffold
     # (no Examples block) was also shipped briefly.
     (
-        "# Hermes Agent Persona\n"
+        "# Silver Core Persona\n"
         "\n"
         "<!--\n"
         "This file defines the agent's personality and tone.\n"
diff --git a/hermes_cli/doctor.py b/hermes_cli/doctor.py
index ca8b889..84c5d96 100644
--- a/hermes_cli/doctor.py
+++ b/hermes_cli/doctor.py
@@ -1,7 +1,7 @@
 """
 Doctor command for hermes CLI.
 
-Diagnoses issues with Hermes Agent setup.
+Diagnoses issues with Silver Core setup.
 """
 
 import os
@@ -1458,7 +1458,7 @@ def run_doctor(args):
         if should_fix:
             soul_path.parent.mkdir(parents=True, exist_ok=True)
             soul_path.write_text(
-                "# Hermes Agent Persona\n\n"
+                "# Silver Core Persona\n\n"
                 "<!-- Edit this file to customize how Hermes communicates. -->\n\n"
                 "You are Hermes, a helpful AI assistant.\n",
                 encoding="utf-8",
diff --git a/hermes_cli/gateway.py b/hermes_cli/gateway.py
index 55b8a19..6900558 100644
--- a/hermes_cli/gateway.py
+++ b/hermes_cli/gateway.py
@@ -599,7 +599,7 @@ def find_gateway_pids(
             profiles (the pre-7923 global behaviour).  ``hermes update``
             needs this because a code update affects every profile.
             When ``False`` (default), only PIDs belonging to the current
-            Hermes profile are returned.
+            Silver Core profile are returned.
     """
     _exclude = set(exclude_pids or set())
     pids: list[int] = []
@@ -628,7 +628,7 @@ def find_gateway_pids(
 def find_profile_gateway_processes(
     exclude_pids: set | None = None,
 ) -> list[ProfileGatewayProcess]:
-    """Return running gateway PIDs mapped to Hermes profiles via PID files."""
+    """Return running gateway PIDs mapped to Silver Core profiles via PID files."""
     _exclude = set(exclude_pids or set())
     processes: list[ProfileGatewayProcess] = []
     try:
@@ -1721,7 +1721,7 @@ def _windows_gateway_should_absorb_console_controls() -> bool:
 # =============================================================================
 
 _SERVICE_BASE = "hermes-gateway"
-SERVICE_DESCRIPTION = "Hermes Agent Gateway - Messaging Platform Integration"
+SERVICE_DESCRIPTION = "Silver Core Gateway - Messaging Platform Integration"
 
 
 def _profile_suffix() -> str:
diff --git a/hermes_cli/gateway_windows.py b/hermes_cli/gateway_windows.py
index 4d09ab3..8a3c1c1 100644
--- a/hermes_cli/gateway_windows.py
+++ b/hermes_cli/gateway_windows.py
@@ -56,7 +56,7 @@ _FALLBACK_PATTERNS = re.compile(
 _ACCESS_DENIED_PATTERN = re.compile(r"(access is denied|acceso denegado)", re.IGNORECASE)
 
 _TASK_NAME_DEFAULT = "Hermes_Gateway"
-_TASK_DESCRIPTION = "Hermes Agent Gateway - Messaging Platform Integration"
+_TASK_DESCRIPTION = "Silver Core Gateway - Messaging Platform Integration"
 _TASK_LOGON_DELAY = "PT30S"
 _TASK_RESTART_INTERVAL = "PT1M"
 _TASK_RESTART_COUNT = 999
@@ -198,7 +198,7 @@ def _is_running_as_admin() -> bool:
 
 
 def _current_profile_cli_args() -> list[str]:
-    """Return CLI args that preserve the current Hermes profile."""
+    """Return CLI args that preserve the current Silver Core profile."""
     from hermes_cli.gateway import _profile_arg
 
     profile_arg = _profile_arg()
diff --git a/hermes_cli/kanban.py b/hermes_cli/kanban.py
index a08cb8f..bade115 100644
--- a/hermes_cli/kanban.py
+++ b/hermes_cli/kanban.py
@@ -222,7 +222,7 @@ def build_parser(parent_subparsers: argparse._SubParsersAction) -> argparse.Argu
         "kanban",
         help="Multi-profile collaboration board (tasks, links, comments)",
         description=(
-            "Durable SQLite-backed task board shared across Hermes profiles. "
+            "Durable SQLite-backed task board shared across Silver Core profiles. "
             "Tasks are claimed atomically, can depend on other tasks, and "
             "are executed by a named profile in an isolated workspace. "
             "See https://hermes-agent.nousresearch.com/docs/user-guide/features/kanban "
@@ -2650,7 +2650,7 @@ def _cmd_daemon(args: argparse.Namespace) -> int:
 
     def _ready_queue_nonempty() -> bool:
         """Cheap probe — is there at least one ready+assigned+unclaimed
-        task whose assignee maps to a real Hermes profile (i.e. one the
+        task whose assignee maps to a real Silver Core profile (i.e. one the
         dispatcher would actually try to spawn for)?
 
         Filters out tasks assigned to control-plane lanes
diff --git a/hermes_cli/kanban_db.py b/hermes_cli/kanban_db.py
index f558130..9c47f64 100644
--- a/hermes_cli/kanban_db.py
+++ b/hermes_cli/kanban_db.py
@@ -7999,7 +7999,7 @@ def check_respawn_guard(conn: sqlite3.Connection, task_id: str) -> Optional[str]
 
 def has_spawnable_ready(conn: sqlite3.Connection) -> bool:
     """Return True iff there is at least one ready+assigned+unclaimed task
-    whose assignee maps to a real Hermes profile.
+    whose assignee maps to a real Silver Core profile.
 
     Used by the gateway- and CLI-embedded dispatchers' health telemetry to
     decide whether ``0 spawned`` is a "stuck" condition (real spawnable
@@ -8031,7 +8031,7 @@ def has_spawnable_ready(conn: sqlite3.Connection) -> bool:
 
 def has_spawnable_review(conn: sqlite3.Connection) -> bool:
     """Return True iff there is at least one review+assigned+unclaimed task
-    whose assignee maps to a real Hermes profile.
+    whose assignee maps to a real Silver Core profile.
 
     Mirror of :func:`has_spawnable_ready` for the review column —
     used by the health telemetry to decide whether the dispatcher
@@ -8313,7 +8313,7 @@ def _dispatch_once_locked(
             else:
                 result.skipped_unassigned.append(row["id"])
                 continue
-        # Skip ready tasks whose assignee is not a real Hermes profile.
+        # Skip ready tasks whose assignee is not a real Silver Core profile.
         # `_default_spawn` invokes ``hermes -p <assignee>`` which fails
         # with "Profile 'X' does not exist" when the assignee names a
         # control-plane lane (e.g. an interactive Claude Code terminal
@@ -8979,7 +8979,7 @@ def _default_spawn(
         log_f.close()
         raise RuntimeError(
             "`hermes` executable not found on PATH. "
-            "Install Hermes Agent or activate its venv before running the kanban dispatcher."
+            "Install Silver Core or activate its venv before running the kanban dispatcher."
         )
     # NOTE: we intentionally do NOT close log_f here — we want Popen's
     # child process to keep writing after this function returns.  The
diff --git a/hermes_cli/kanban_decompose.py b/hermes_cli/kanban_decompose.py
index d6c248c..7c2f0fe 100644
--- a/hermes_cli/kanban_decompose.py
+++ b/hermes_cli/kanban_decompose.py
@@ -49,7 +49,7 @@ from hermes_cli import profiles as profiles_mod
 logger = logging.getLogger(__name__)
 
 
-_SYSTEM_PROMPT = """You are the Kanban decomposer for the Hermes Agent board.
+_SYSTEM_PROMPT = """You are the Kanban decomposer for the Silver Core board.
 
 A user dropped a rough idea into the Triage column. Your job is to break it
 into a small graph of concrete child tasks and route each one to the best-
diff --git a/hermes_cli/kanban_diagnostics.py b/hermes_cli/kanban_diagnostics.py
index 8173f1e..3d724b2 100644
--- a/hermes_cli/kanban_diagnostics.py
+++ b/hermes_cli/kanban_diagnostics.py
@@ -901,7 +901,7 @@ def _rule_stranded_in_ready(task, events, runs, now, cfg) -> list[Diagnostic]:
     on the dispatcher and a different operator response).
 
     The signal is age-based on purpose: it's identity-agnostic, so it
-    works for Hermes profiles, registered lanes, external workers, and
+    works for Silver Core profiles, registered lanes, external workers, and
     typos uniformly. No registry to curate, no per-board allowlist.
     """
     threshold_seconds = float(
diff --git a/hermes_cli/kanban_specify.py b/hermes_cli/kanban_specify.py
index e7aba34..242b49d 100644
--- a/hermes_cli/kanban_specify.py
+++ b/hermes_cli/kanban_specify.py
@@ -50,7 +50,7 @@ HERMES_KANBAN_SPECIFY_MAX_TOKENS = max(
 logger = logging.getLogger(__name__)
 
 
-_SYSTEM_PROMPT = """You are the Kanban triage specifier for the Hermes Agent board.
+_SYSTEM_PROMPT = """You are the Kanban triage specifier for the Silver Core board.
 A user dropped a rough idea into the Triage column. Your job is to turn it
 into a concrete, actionable task spec that an autonomous worker can pick up
 and execute without further clarification.
diff --git a/hermes_cli/main.py b/hermes_cli/main.py
index 814fa7e..1ae78c8 100644
--- a/hermes_cli/main.py
+++ b/hermes_cli/main.py
@@ -36,7 +36,7 @@ Usage:
     hermes honcho migrate                  # Step-by-step migration guide: OpenClaw native → Hermes + Honcho
     hermes version             Show version
     hermes update              Update to latest version
-    hermes uninstall           Uninstall Hermes Agent
+    hermes uninstall           Uninstall Silver Core
     hermes acp                 Run as an ACP server for editor integration
     hermes sessions browse     Interactive session picker with search
 
@@ -391,7 +391,7 @@ def _read_openai_version_fast() -> str | None:
 def _print_fast_version_info() -> None:
     from hermes_cli import __release_date__, __version__
 
-    print(f"Hermes Agent v{__version__} ({__release_date__})")
+    print(f"Silver Core v{__version__} ({__release_date__})")
     print(f"Install directory: {PROJECT_ROOT}")
 
     print(f"Python: {sys.version.split()[0]}")
@@ -2274,7 +2274,7 @@ def _launch_tui(
     except Exception:
         logger.debug("Failed to apply terminal config bridge for TUI launch", exc_info=True)
     active_session_fd, active_session_file = tempfile.mkstemp(
-        prefix="hermes-tui-active-session-", suffix=".json"
+        prefix="silver-core-tui-active-session-", suffix=".json"
     )
     os.close(active_session_fd)
     env["HERMES_TUI_ACTIVE_SESSION_FILE"] = active_session_file
@@ -2942,14 +2942,14 @@ def cmd_whatsapp(args):
             print("    2. Send a message to the bot's WhatsApp number")
             print("    3. The agent will reply automatically")
             print()
-            print("  Tip: Agent responses are prefixed with '⚕ Hermes Agent'")
+            print("  Tip: Agent responses are prefixed with '⚕ Silver Core'")
         else:
             print("  Next steps:")
             print("    1. Start the gateway:  hermes gateway")
             print("    2. Open WhatsApp → Message Yourself")
             print("    3. Type a message — the agent will reply")
             print()
-            print("  Tip: Agent responses are prefixed with '⚕ Hermes Agent'")
+            print("  Tip: Agent responses are prefixed with '⚕ Silver Core'")
             print("  so you can tell them apart from your own messages.")
         print()
         print("  Or install as a service: hermes gateway install")
@@ -4927,7 +4927,7 @@ def cmd_version(args):
 
 
 def cmd_uninstall(args):
-    """Uninstall Hermes Agent (or just the Chat GUI with --gui)."""
+    """Uninstall Silver Core (or just the Chat GUI with --gui)."""
     # Machine-readable install snapshot for the desktop app's uninstall UI.
     # Must run before any TTY gate — it's called from a non-interactive child.
     if getattr(args, "gui_summary", False):
@@ -8955,7 +8955,7 @@ def _size_delta_label(saved_mb: float) -> str:
 
 
 def cmd_update(args):
-    """Update Hermes Agent to the latest version.
+    """Update Silver Core to the latest version.
 
     Thin wrapper around ``_cmd_update_impl``: installs hangup protection,
     runs the update, then restores stdio on the way out (even on
@@ -8970,7 +8970,7 @@ def cmd_update(args):
     )
 
     if is_managed():
-        managed_error("update Hermes Agent")
+        managed_error("update Silver Core")
         return
 
     # Docker users can't ``git pull`` — the image excludes ``.git`` from
@@ -10893,7 +10893,7 @@ def cmd_memory(args):
 
 
 def cmd_acp(args):
-    """Launch Hermes Agent as an ACP server."""
+    """Launch Silver Core as an ACP server."""
     try:
         from acp_adapter.entry import main as acp_main
 
diff --git a/hermes_cli/model_switch.py b/hermes_cli/model_switch.py
index 75a1e9d..7f7eaab 100644
--- a/hermes_cli/model_switch.py
+++ b/hermes_cli/model_switch.py
@@ -180,7 +180,7 @@ def _bare_custom_provider_def(current_base_url: str) -> Optional[ProviderDef]:
 
 _HERMES_MODEL_WARNING = (
     "Nous Research Hermes 3 & 4 models are NOT agentic and are not designed "
-    "for use with Hermes Agent. They lack the tool-calling capabilities "
+    "for use with Silver Core. They lack the tool-calling capabilities "
     "required for agent workflows. Consider using an agentic model instead "
     "(Claude, GPT, Gemini, DeepSeek, etc.)."
 )
diff --git a/hermes_cli/observability/relay_shared_metrics.py b/hermes_cli/observability/relay_shared_metrics.py
index a7af6b8..093322a 100644
--- a/hermes_cli/observability/relay_shared_metrics.py
+++ b/hermes_cli/observability/relay_shared_metrics.py
@@ -631,7 +631,7 @@ class _Runtime:
 
 
 def enabled() -> bool:
-    """Return the shared-metrics policy for the active Hermes profile."""
+    """Return the shared-metrics policy for the active Silver Core profile."""
     profile_key = relay_runtime.current_profile_key()
     try:
         from hermes_cli.config import read_raw_config_readonly
diff --git a/hermes_cli/platforms.py b/hermes_cli/platforms.py
index 730dbed..aa5ab3b 100644
--- a/hermes_cli/platforms.py
+++ b/hermes_cli/platforms.py
@@ -1,5 +1,5 @@
 """
-Shared platform registry for Hermes Agent.
+Shared platform registry for Silver Core.
 
 Single source of truth for platform metadata consumed by both
 skills_config (label display) and tools_config (default toolset
diff --git a/hermes_cli/plugins.py b/hermes_cli/plugins.py
index 8dd02a1..e6b4c96 100644
--- a/hermes_cli/plugins.py
+++ b/hermes_cli/plugins.py
@@ -387,7 +387,7 @@ class PluginContext:
 
     @property
     def profile_name(self) -> str:
-        """Return the active Hermes profile name (e.g. ``"default"``).
+        """Return the active Silver Core profile name (e.g. ``"default"``).
 
         Derived from ``HERMES_HOME`` via
         :func:`hermes_cli.profiles.get_active_profile_name`, so it works in
diff --git a/hermes_cli/profile_describer.py b/hermes_cli/profile_describer.py
index c6af27a..3d3ec61 100644
--- a/hermes_cli/profile_describer.py
+++ b/hermes_cli/profile_describer.py
@@ -44,7 +44,7 @@ logger = logging.getLogger(__name__)
 MAX_SKILLS_FOR_PROMPT = 60
 
 
-_SYSTEM_PROMPT = """You are a profile-describer for the Hermes Agent kanban board.
+_SYSTEM_PROMPT = """You are a profile-describer for the Silver Core kanban board.
 
 A user runs multiple "profiles" — distinct agent identities, each with their
 own skills, model, and configuration. The kanban board's orchestrator routes
@@ -70,7 +70,7 @@ Rules:
                          refactors functions, opens GitHub PRs."
   - 1-2 sentences, <= 280 characters total.
   - Never invent capabilities the skills don't suggest.
-  - Never write "Hermes Agent profile" or other meta-narration.
+  - Never write "Silver Core profile" or other meta-narration.
   - No code fences, no preamble, no closing remarks. Output only JSON.
 """
 
diff --git a/hermes_cli/profile_distribution.py b/hermes_cli/profile_distribution.py
index 55a6be1..353b5f3 100644
--- a/hermes_cli/profile_distribution.py
+++ b/hermes_cli/profile_distribution.py
@@ -1,6 +1,6 @@
-"""Profile distributions — shareable, packaged Hermes profiles via git.
+"""Profile distributions — shareable, packaged Silver Core profiles via git.
 
-A distribution is a Hermes profile published as a git repository (or
+A distribution is a Silver Core profile published as a git repository (or
 installed from a local directory for development). Install with one command
 from a git URL, update in place, and keep your local memories / sessions /
 credentials untouched.
@@ -415,7 +415,7 @@ def _stage_source(source: str, workdir: Path) -> Tuple[Path, str]:
         if not (cloned / MANIFEST_FILENAME).is_file():
             raise DistributionError(
                 f"No {MANIFEST_FILENAME} at the root of {src_str!r}. "
-                "This repository is not a Hermes profile distribution."
+                "This repository is not a Silver Core profile distribution."
             )
         return cloned, src_str
 
diff --git a/hermes_cli/profiles.py b/hermes_cli/profiles.py
index 4ed7176..a9defc0 100644
--- a/hermes_cli/profiles.py
+++ b/hermes_cli/profiles.py
@@ -227,7 +227,7 @@ _DEFAULT_EXPORT_EXCLUDE_ROOT = frozenset({
 # Allow-list for ``export_profile("default")``: when HERMES_HOME equals the
 # cwd (Docker/custom deployments), the default profile home is the working
 # directory and contains arbitrary user files that should NOT be bundled
-# into the export. The set below identifies the *known Hermes profile
+# into the export. The set below identifies the *known Silver Core profile
 # artifacts* at the root of HERMES_HOME; everything else is excluded.
 # Sensitive runtime infrastructure (``state.db``, ``logs/``, ``auth.*``,
 # other profiles) is intentionally *not* in this list so the export stays
@@ -1117,7 +1117,7 @@ def create_profile(
     if not env_path.exists():
         try:
             env_path.write_text(
-                "# Per-profile secrets for this Hermes profile.\n"
+                "# Per-profile secrets for this Silver Core profile.\n"
                 "# API keys and tokens set here override the shell environment.\n"
                 "# Behavioral settings belong in config.yaml, not here.\n",
                 encoding="utf-8",
@@ -1264,7 +1264,7 @@ def backfill_profile_envs(quiet: bool = False) -> List[str]:
                 shutil.copy2(default_env, env_path)
             else:
                 env_path.write_text(
-                    "# Per-profile secrets for this Hermes profile.\n"
+                    "# Per-profile secrets for this Silver Core profile.\n"
                     "# API keys and tokens set here override the shell environment.\n"
                     "# Behavioral settings belong in config.yaml, not here.\n",
                     encoding="utf-8",
@@ -1888,7 +1888,7 @@ def _default_export_ignore(root_dir: Path):
             elif entry in {"package.json", "package-lock.json"}:
                 ignored.add(entry)
         # Root-level allow-list: drop everything that isn't a known
-        # Hermes profile artifact.
+        # Silver Core profile artifact.
         if Path(directory) == root_dir:
             ignored.update(
                 entry for entry in contents if entry not in _DEFAULT_EXPORT_INCLUDE_ROOT
diff --git a/hermes_cli/projects_cmd.py b/hermes_cli/projects_cmd.py
index 4e2655b..6ee981b 100644
--- a/hermes_cli/projects_cmd.py
+++ b/hermes_cli/projects_cmd.py
@@ -36,7 +36,7 @@ def build_parser(
     sub = parser.add_subparsers(dest="project_action")
 
     p_create = sub.add_parser("create", help="Create a new project")
-    p_create.add_argument("name", help="Human name, e.g. 'Hermes Agent'")
+    p_create.add_argument("name", help="Human name, e.g. 'Silver Core'")
     p_create.add_argument(
         "folders", nargs="*", help="Folder paths to include (first = primary)"
     )
diff --git a/hermes_cli/providers.py b/hermes_cli/providers.py
index 17cae5d..132fe89 100644
--- a/hermes_cli/providers.py
+++ b/hermes_cli/providers.py
@@ -1,5 +1,5 @@
 """
-Single source of truth for provider identity in Hermes Agent.
+Single source of truth for provider identity in Silver Core.
 
 Two data sources, merged at runtime:
 
diff --git a/hermes_cli/pty_bridge.py b/hermes_cli/pty_bridge.py
index cf4a4e6..53528ce 100644
--- a/hermes_cli/pty_bridge.py
+++ b/hermes_cli/pty_bridge.py
@@ -129,7 +129,7 @@ class PtyBridge:
             if sys.platform.startswith("win"):
                 raise PtyUnavailableError(
                     "Pseudo-terminals are unavailable on this platform. "
-                    "Hermes Agent supports Windows only via WSL."
+                    "Silver Core supports Windows only via WSL."
                 )
             if ptyprocess is None:
                 raise PtyUnavailableError(
diff --git a/hermes_cli/security_advisories.py b/hermes_cli/security_advisories.py
index ef058a5..d7024b1 100644
--- a/hermes_cli/security_advisories.py
+++ b/hermes_cli/security_advisories.py
@@ -1,5 +1,5 @@
 """
-Security advisory checker for Hermes Agent.
+Security advisory checker for Silver Core.
 
 Detects known-compromised Python packages installed in the active venv
 (supply-chain attacks like the Mini Shai-Hulud worm of May 2026 that
diff --git a/hermes_cli/security_audit.py b/hermes_cli/security_audit.py
index f29c61f..a9e925e 100644
--- a/hermes_cli/security_audit.py
+++ b/hermes_cli/security_audit.py
@@ -1,4 +1,4 @@
-"""On-demand supply-chain audit for Hermes Agent installs.
+"""On-demand supply-chain audit for Silver Core installs.
 
 Scans three surfaces a Hermes user actually controls and we can map to
 upstream advisories without auth or extra binaries:
diff --git a/hermes_cli/session_export_html.py b/hermes_cli/session_export_html.py
index 6c91ed6..e8f2452 100644
--- a/hermes_cli/session_export_html.py
+++ b/hermes_cli/session_export_html.py
@@ -563,7 +563,7 @@ HTML_TEMPLATE = """<!DOCTYPE html>
             {sessions_html}
             
             <footer>
-                Built with ☤ Hermes Agent • Generated on {generated_at}
+                Built with ☤ Silver Core • Generated on {generated_at}
             </footer>
         </div>
     </div>
diff --git a/hermes_cli/setup.py b/hermes_cli/setup.py
index 9031524..d22bdbf 100644
--- a/hermes_cli/setup.py
+++ b/hermes_cli/setup.py
@@ -1,5 +1,5 @@
 """
-Interactive setup wizard for Hermes Agent.
+Interactive setup wizard for Silver Core.
 
 Modular wizard with independently-runnable sections:
   1. Model & Provider — choose your AI provider and model
@@ -2389,7 +2389,7 @@ def setup_telemetry(config: dict):
     """Configure the local, privacy-safe shared-metrics subscriber."""
     print_header("Shared Metrics")
     print_info("Shared metrics contain only bounded counters and histograms.")
-    print_info("Packages stay under this Hermes profile and are not uploaded.")
+    print_info("Packages stay under this Silver Core profile and are not uploaded.")
 
     telemetry = config.get("telemetry")
     if not isinstance(telemetry, dict):
@@ -3017,7 +3017,7 @@ def run_setup_wizard(args):
     )
     print(
         color(
-            "│             ⚕ Hermes Agent Setup Wizard                │", Colors.MAGENTA
+            "│             ⚕ Silver Core Setup Wizard                │", Colors.MAGENTA
         )
     )
     print(
@@ -3028,7 +3028,7 @@ def run_setup_wizard(args):
     )
     print(
         color(
-            "│  Let's configure your Hermes Agent installation.       │", Colors.MAGENTA
+            "│  Let's configure your Silver Core installation.       │", Colors.MAGENTA
         )
     )
     print(
diff --git a/hermes_cli/skills_config.py b/hermes_cli/skills_config.py
index 720b9eb..bece6ff 100644
--- a/hermes_cli/skills_config.py
+++ b/hermes_cli/skills_config.py
@@ -1,5 +1,5 @@
 """
-Skills configuration for Hermes Agent.
+Skills configuration for Silver Core.
 `hermes skills` enters this module.
 
 Toggle individual skills or categories on/off, globally or per-platform.
diff --git a/hermes_cli/skin_engine.py b/hermes_cli/skin_engine.py
index a2a093b..58e9a29 100644
--- a/hermes_cli/skin_engine.py
+++ b/hermes_cli/skin_engine.py
@@ -94,7 +94,7 @@ All fields are optional. Missing values inherit from the ``default`` skin.
 
     # Branding: text strings used throughout the CLI
     branding:
-      agent_name: "Hermes Agent"          # Banner title, status display
+      agent_name: "Silver Core"          # Banner title, status display
       welcome: "Welcome message"          # Shown at CLI startup
       goodbye: "Goodbye! ⚕"              # Shown on exit
       response_label: " ⚕ Hermes "       # Response box header label
@@ -119,7 +119,7 @@ USAGE
 
     skin = get_active_skin()
     print(skin.colors["banner_title"])    # "#FFD700"
-    print(skin.get_branding("agent_name"))  # "Hermes Agent"
+    print(skin.get_branding("agent_name"))  # "Silver Core"
 
     set_active_skin("ares")               # Switch to built-in ares skin
     set_active_skin("mytheme")            # Switch to user skin from ~/.hermes/skins/
@@ -274,8 +274,8 @@ _BUILTIN_SKINS: Dict[str, Dict[str, Any]] = {
             # Empty = use hardcoded defaults in display.py
         },
         "branding": {
-            "agent_name": "Hermes Agent",
-            "welcome": "Welcome to Hermes Agent! Type your message or /help for commands.",
+            "agent_name": "Silver Core",
+            "welcome": "Welcome to Silver Core! Type your message or /help for commands.",
             "goodbye": "Goodbye! ⚕",
             "response_label": " ⚕ Hermes ",
             "prompt_symbol": "❯",
@@ -395,8 +395,8 @@ _BUILTIN_SKINS: Dict[str, Dict[str, Any]] = {
         },
         "spinner": {},
         "branding": {
-            "agent_name": "Hermes Agent",
-            "welcome": "Welcome to Hermes Agent! Type your message or /help for commands.",
+            "agent_name": "Silver Core",
+            "welcome": "Welcome to Silver Core! Type your message or /help for commands.",
             "goodbye": "Goodbye! ⚕",
             "response_label": " ⚕ Hermes ",
             "prompt_symbol": "❯",
@@ -439,8 +439,8 @@ _BUILTIN_SKINS: Dict[str, Dict[str, Any]] = {
         },
         "spinner": {},
         "branding": {
-            "agent_name": "Hermes Agent",
-            "welcome": "Welcome to Hermes Agent! Type your message or /help for commands.",
+            "agent_name": "Silver Core",
+            "welcome": "Welcome to Silver Core! Type your message or /help for commands.",
             "goodbye": "Goodbye! ⚕",
             "response_label": " ⚕ Hermes ",
             "prompt_symbol": "❯",
@@ -485,8 +485,8 @@ _BUILTIN_SKINS: Dict[str, Dict[str, Any]] = {
         },
         "spinner": {},
         "branding": {
-            "agent_name": "Hermes Agent",
-            "welcome": "Welcome to Hermes Agent! Type your message or /help for commands.",
+            "agent_name": "Silver Core",
+            "welcome": "Welcome to Silver Core! Type your message or /help for commands.",
             "goodbye": "Goodbye! ⚕",
             "response_label": " ⚕ Hermes ",
             "prompt_symbol": "❯",
@@ -531,8 +531,8 @@ _BUILTIN_SKINS: Dict[str, Dict[str, Any]] = {
         },
         "spinner": {},
         "branding": {
-            "agent_name": "Hermes Agent",
-            "welcome": "Welcome to Hermes Agent! Type your message or /help for commands.",
+            "agent_name": "Silver Core",
+            "welcome": "Welcome to Silver Core! Type your message or /help for commands.",
             "goodbye": "Goodbye! \u2695",
             "response_label": " \u2695 Hermes ",
             "prompt_symbol": "\u276f",
diff --git a/hermes_cli/status.py b/hermes_cli/status.py
index 2fed6b4..5a95f4d 100644
--- a/hermes_cli/status.py
+++ b/hermes_cli/status.py
@@ -1,7 +1,7 @@
 """
 Status command for hermes CLI.
 
-Shows the status of all Hermes Agent components.
+Shows the status of all Silver Core components.
 """
 
 import os
@@ -106,12 +106,12 @@ from hermes_constants import is_termux as _is_termux
 
 
 def show_status(args):
-    """Show status of all Hermes Agent components."""
+    """Show status of all Silver Core components."""
     deep = getattr(args, 'deep', False)
 
     print()
     print(color("┌─────────────────────────────────────────────────────────┐", Colors.CYAN))
-    print(color("│                 ⚕ Hermes Agent Status                  │", Colors.CYAN))
+    print(color("│                 ⚕ Silver Core Status                  │", Colors.CYAN))
     print(color("└─────────────────────────────────────────────────────────┘", Colors.CYAN))
 
     # =========================================================================
diff --git a/hermes_cli/subcommands/acp.py b/hermes_cli/subcommands/acp.py
index 5282996..922efd9 100644
--- a/hermes_cli/subcommands/acp.py
+++ b/hermes_cli/subcommands/acp.py
@@ -15,8 +15,8 @@ def build_acp_parser(subparsers, *, cmd_acp: Callable) -> None:
     """Attach the ``acp`` subcommand to ``subparsers``."""
     acp_parser = subparsers.add_parser(
         "acp",
-        help="Run Hermes Agent as an ACP (Agent Client Protocol) server",
-        description="Start Hermes Agent in ACP mode for editor integration (VS Code, Zed, JetBrains)",
+        help="Run Silver Core as an ACP (Agent Client Protocol) server",
+        description="Start Silver Core in ACP mode for editor integration (VS Code, Zed, JetBrains)",
     )
     add_accept_hooks_flag(acp_parser)
     acp_parser.add_argument(
diff --git a/hermes_cli/subcommands/config.py b/hermes_cli/subcommands/config.py
index 8fe1297..22f8ab3 100644
--- a/hermes_cli/subcommands/config.py
+++ b/hermes_cli/subcommands/config.py
@@ -17,7 +17,7 @@ def build_config_parser(subparsers, *, cmd_config: Callable) -> None:
     config_parser = subparsers.add_parser(
         "config",
         help="View and edit configuration",
-        description="Manage Hermes Agent configuration",
+        description="Manage Silver Core configuration",
     )
     config_subparsers = config_parser.add_subparsers(dest="config_command")
 
diff --git a/hermes_cli/subcommands/dashboard.py b/hermes_cli/subcommands/dashboard.py
index 0b695e0..2f2a032 100644
--- a/hermes_cli/subcommands/dashboard.py
+++ b/hermes_cli/subcommands/dashboard.py
@@ -101,7 +101,7 @@ def build_dashboard_parser(
     dashboard_parser = subparsers.add_parser(
         "dashboard",
         help="Start the web UI dashboard",
-        description="Launch the Hermes Agent web dashboard for managing config, API keys, and sessions",
+        description="Launch the Silver Core web dashboard for managing config, API keys, and sessions",
     )
     _add_server_runtime_args(dashboard_parser)
     dashboard_parser.add_argument(
diff --git a/hermes_cli/subcommands/debug.py b/hermes_cli/subcommands/debug.py
index 65eb842..c826cab 100644
--- a/hermes_cli/subcommands/debug.py
+++ b/hermes_cli/subcommands/debug.py
@@ -18,7 +18,7 @@ def build_debug_parser(subparsers, *, cmd_debug: Callable) -> None:
     debug_parser = subparsers.add_parser(
         "debug",
         help="Debug tools — upload logs and system info for support",
-        description="Debug utilities for Hermes Agent. Use 'hermes debug share' to "
+        description="Debug utilities for Silver Core. Use 'hermes debug share' to "
         "upload a debug report (system info + recent logs) to a paste "
         "service and get a shareable URL.",
         formatter_class=argparse.RawDescriptionHelpFormatter,
diff --git a/hermes_cli/subcommands/doctor.py b/hermes_cli/subcommands/doctor.py
index 5be37c6..565b2c5 100644
--- a/hermes_cli/subcommands/doctor.py
+++ b/hermes_cli/subcommands/doctor.py
@@ -17,7 +17,7 @@ def build_doctor_parser(subparsers, *, cmd_doctor: Callable) -> None:
     doctor_parser = subparsers.add_parser(
         "doctor",
         help="Check configuration and dependencies",
-        description="Diagnose issues with Hermes Agent setup",
+        description="Diagnose issues with Silver Core setup",
     )
     doctor_parser.add_argument(
         "--fix", action="store_true", help="Attempt to fix issues automatically"
diff --git a/hermes_cli/subcommands/profile.py b/hermes_cli/subcommands/profile.py
index d812fad..9101461 100644
--- a/hermes_cli/subcommands/profile.py
+++ b/hermes_cli/subcommands/profile.py
@@ -148,7 +148,7 @@ def build_profile_parser(subparsers, *, cmd_profile: Callable) -> None:
         "install",
         help="Install a profile distribution from a git URL or local directory",
         description=(
-            "Install a Hermes profile distribution. SOURCE can be a git URL "
+            "Install a Silver Core profile distribution. SOURCE can be a git URL "
             "(github.com/user/repo, https://..., git@...) or a local "
             "directory containing distribution.yaml at its root."
         ),
diff --git a/hermes_cli/subcommands/setup.py b/hermes_cli/subcommands/setup.py
index fa0c0dc..42a1b9b 100644
--- a/hermes_cli/subcommands/setup.py
+++ b/hermes_cli/subcommands/setup.py
@@ -17,7 +17,7 @@ def build_setup_parser(subparsers, *, cmd_setup: Callable) -> None:
     setup_parser = subparsers.add_parser(
         "setup",
         help="Interactive setup wizard",
-        description="Configure Hermes Agent with an interactive wizard. "
+        description="Configure Silver Core with an interactive wizard. "
         "Run a specific section: "
         "hermes setup model|tts|terminal|gateway|tools|telemetry|agent",
     )
diff --git a/hermes_cli/subcommands/status.py b/hermes_cli/subcommands/status.py
index ad107a3..c55663a 100644
--- a/hermes_cli/subcommands/status.py
+++ b/hermes_cli/subcommands/status.py
@@ -17,7 +17,7 @@ def build_status_parser(subparsers, *, cmd_status: Callable) -> None:
     status_parser = subparsers.add_parser(
         "status",
         help="Show status of all components",
-        description="Display status of Hermes Agent components",
+        description="Display status of Silver Core components",
     )
     status_parser.add_argument(
         "--all", action="store_true", help="Show all details (redacted for sharing)"
diff --git a/hermes_cli/subcommands/uninstall.py b/hermes_cli/subcommands/uninstall.py
index f746a89..d804a5a 100644
--- a/hermes_cli/subcommands/uninstall.py
+++ b/hermes_cli/subcommands/uninstall.py
@@ -16,8 +16,8 @@ def build_uninstall_parser(subparsers, *, cmd_uninstall: Callable) -> None:
     # =========================================================================
     uninstall_parser = subparsers.add_parser(
         "uninstall",
-        help="Uninstall Hermes Agent",
-        description="Remove Hermes Agent from your system. Can keep configs/data for reinstall.",
+        help="Uninstall Silver Core",
+        description="Remove Silver Core from your system. Can keep configs/data for reinstall.",
     )
     uninstall_parser.add_argument(
         "--full",
diff --git a/hermes_cli/subcommands/update.py b/hermes_cli/subcommands/update.py
index f612001..eed155a 100644
--- a/hermes_cli/subcommands/update.py
+++ b/hermes_cli/subcommands/update.py
@@ -16,7 +16,7 @@ def build_update_parser(subparsers, *, cmd_update: Callable) -> None:
     # =========================================================================
     update_parser = subparsers.add_parser(
         "update",
-        help="Update Hermes Agent to the latest version",
+        help="Update Silver Core to the latest version",
         description="Pull the latest changes from git and reinstall dependencies",
     )
     update_parser.add_argument(
diff --git a/hermes_cli/telegram_managed_bot.py b/hermes_cli/telegram_managed_bot.py
index 89395d5..1ee4d4f 100644
--- a/hermes_cli/telegram_managed_bot.py
+++ b/hermes_cli/telegram_managed_bot.py
@@ -27,7 +27,7 @@ TELEGRAM_ONBOARDING_URL_ENV = "TELEGRAM_ONBOARDING_URL"
 # actual deep link, so this is only used by local helpers/tests.
 DEFAULT_MANAGER_BOT = "HermesSetupBot"
 
-DEFAULT_BOT_NAME = "Hermes Agent"
+DEFAULT_BOT_NAME = "Silver Core"
 DEFAULT_POLL_TIMEOUT = 180
 POLL_INTERVAL = 2
 
diff --git a/hermes_cli/tools_config.py b/hermes_cli/tools_config.py
index 162a642..0672667 100644
--- a/hermes_cli/tools_config.py
+++ b/hermes_cli/tools_config.py
@@ -1,5 +1,5 @@
 """
-Unified tool configuration for Hermes Agent.
+Unified tool configuration for Silver Core.
 
 `hermes tools` and `hermes setup tools` both enter this module.
 Select a platform → toggle toolsets on/off → for newly enabled tools
diff --git a/hermes_cli/uninstall.py b/hermes_cli/uninstall.py
index fb7548b..3aed312 100644
--- a/hermes_cli/uninstall.py
+++ b/hermes_cli/uninstall.py
@@ -1,5 +1,5 @@
 """
-Hermes Agent Uninstaller.
+Silver Core Uninstaller.
 
 Provides options for:
 - Full uninstall: Remove everything including configs and data
@@ -65,8 +65,8 @@ def remove_path_from_shell_configs():
             skip_next = False
             
             for line in content.split('\n'):
-                # Skip the "# Hermes Agent" comment and following line
-                if '# Hermes Agent' in line or '# hermes-agent' in line:
+                # Skip the "# Silver Core" comment and following line
+                if '# Silver Core' in line or '# hermes-agent' in line:
                     skip_next = True
                     continue
                 if skip_next and ('hermes' in line.lower() and 'PATH' in line):
@@ -611,7 +611,7 @@ def run_uninstall(args):
 
     print()
     print(color("┌─────────────────────────────────────────────────────────┐", Colors.MAGENTA, Colors.BOLD))
-    print(color("│            ⚕ Hermes Agent Uninstaller                  │", Colors.MAGENTA, Colors.BOLD))
+    print(color("│            ⚕ Silver Core Uninstaller                  │", Colors.MAGENTA, Colors.BOLD))
     print(color("└─────────────────────────────────────────────────────────┘", Colors.MAGENTA, Colors.BOLD))
     print()
     
@@ -913,7 +913,7 @@ def _perform_uninstall(
         print(color("Reload your shell to complete the process:", Colors.YELLOW))
         print("  source ~/.bashrc  # or ~/.zshrc")
     print()
-    print("Thank you for using Hermes Agent! ⚕")
+    print("Thank you for using Silver Core! ⚕")
     print()
 
 
diff --git a/hermes_cli/update_cmd.py b/hermes_cli/update_cmd.py
index 6b777ff..74df255 100644
--- a/hermes_cli/update_cmd.py
+++ b/hermes_cli/update_cmd.py
@@ -532,7 +532,7 @@ def _atomic_replace_dir(src: str, dst: str) -> None:
         shutil.rmtree(backup, ignore_errors=True)
 
 def _update_via_zip(args):
-    """Update Hermes Agent by downloading a ZIP archive.
+    """Update Silver Core by downloading a ZIP archive.
 
     Used on Windows when git file I/O is broken (antivirus, NTFS filter
     drivers causing 'Invalid argument' errors on file creation).
@@ -1804,7 +1804,7 @@ def _update_node_dependencies() -> list[str]:
     from hermes_constants import get_default_hermes_root
 
     # This cache describes PROJECT_ROOT/node_modules, which is shared by every
-    # Hermes profile using this checkout. Keep one per-checkout cache under the
+    # Silver Core profile using this checkout. Keep one per-checkout cache under the
     # shared Hermes root rather than rerunning npm once per named profile.
     shared_hermes_root = get_default_hermes_root()
     if not _m()._npm_lockfile_changed(shared_hermes_root):
@@ -2143,7 +2143,7 @@ def _ensure_fhs_path_guard() -> None:
 
     path_line = 'export PATH="/usr/local/bin:$PATH"'
     path_comment = (
-        "# Hermes Agent — ensure /usr/local/bin is on PATH " "(RHEL non-login shells)"
+        "# Silver Core — ensure /usr/local/bin is on PATH " "(RHEL non-login shells)"
     )
     wrote_any = False
     for candidate in (".bashrc", ".bash_profile"):
@@ -2211,7 +2211,7 @@ def _ensure_acp_launcher() -> None:
                 continue
             shim = (
                 "#!/usr/bin/env bash\n"
-                "# Hermes Agent — ACP launcher (written by `hermes update`).\n"
+                "# Silver Core — ACP launcher (written by `hermes update`).\n"
                 "# ACP hosts (Zed, JetBrains, Buzz) resolve the agent by this\n"
                 "# command name on the login-shell PATH.\n"
                 f'exec "{hermes_cmd}" acp "$@"\n'
@@ -3167,7 +3167,7 @@ def _cmd_update_impl(args, gateway_mode: bool):
             logger.debug("Could not read updates.non_interactive_local_changes: %s", exc)
             discard_local_changes = False
 
-    print("⚕ Updating Hermes Agent...")
+    print("⚕ Updating Silver Core...")
     print()
 
     # On Windows, abort early if another hermes.exe is holding the venv shim
diff --git a/hermes_cli/web_server.py b/hermes_cli/web_server.py
index 767882b..52343fa 100644
--- a/hermes_cli/web_server.py
+++ b/hermes_cli/web_server.py
@@ -1,5 +1,5 @@
 """
-Hermes Agent — Web UI server.
+Silver Core — Web UI server.
 
 Provides a FastAPI backend serving the Vite/React frontend and REST API
 endpoints for managing configuration, environment variables, and sessions.
@@ -280,7 +280,7 @@ def _get_pty_active_session_files(app: "FastAPI") -> dict[str, Path]:
         return app.state.pty_active_session_files
 
 
-app = FastAPI(title="Hermes Agent", version=__version__, lifespan=_lifespan)
+app = FastAPI(title="Silver Core", version=__version__, lifespan=_lifespan)
 
 # Memory-provider OAuth connect routes live in the memory layer, not here.
 from hermes_cli.memory_oauth import router as _memory_oauth_router  # noqa: E402
@@ -8909,7 +8909,7 @@ async def _telegram_onboarding_request(
 
 @app.post("/api/messaging/telegram/onboarding/start")
 async def start_telegram_onboarding(body: TelegramOnboardingStart):
-    bot_name = (body.bot_name or "Hermes Agent").strip() or "Hermes Agent"
+    bot_name = (body.bot_name or "Silver Core").strip() or "Silver Core"
     payload = await _telegram_onboarding_request(
         "POST",
         "/v1/telegram/pairings",
@@ -17084,7 +17084,7 @@ def start_server(
         if not list_providers():
             # Surface the *specific* reason any bundled provider declined
             # to register (e.g. missing HERMES_DASHBOARD_OAUTH_CLIENT_ID).
-            # Each provider plugin that ships with Hermes Agent exposes a
+            # Each provider plugin that ships with Silver Core exposes a
             # module-level ``LAST_SKIP_REASON`` string for this purpose;
             # without it the operator would only see "no providers" which
             # is misleading when the provider IS installed but unconfigured.
diff --git a/plugins/dashboard_auth/nous/__init__.py b/plugins/dashboard_auth/nous/__init__.py
index 4806179..e0a0d92 100644
--- a/plugins/dashboard_auth/nous/__init__.py
+++ b/plugins/dashboard_auth/nous/__init__.py
@@ -631,7 +631,7 @@ def register(ctx) -> None:
             "HERMES_DASHBOARD_OAUTH_CLIENT_ID is not set (and "
             "dashboard.oauth.client_id in config.yaml is empty). The "
             "Nous Portal provisions this env var (shape "
-            "'agent:{instance_id}') when it deploys a Hermes Agent "
+            "'agent:{instance_id}') when it deploys a Silver Core "
             "instance — set it to your provisioned client id (either "
             "as an env var or under dashboard.oauth.client_id in "
             "config.yaml), or pass --insecure to skip the OAuth gate "
diff --git a/plugins/disk-cleanup/disk_cleanup.py b/plugins/disk-cleanup/disk_cleanup.py
index 31f5779..e6c8944 100755
--- a/plugins/disk-cleanup/disk_cleanup.py
+++ b/plugins/disk-cleanup/disk_cleanup.py
@@ -1,4 +1,4 @@
-"""disk_cleanup — ephemeral file cleanup for Hermes Agent.
+"""disk_cleanup — ephemeral file cleanup for Silver Core.
 
 Library module wrapping the deterministic cleanup rules written by
 @LVT382009 in PR #12212. The plugin ``__init__.py`` wires these
diff --git a/plugins/google_meet/cli.py b/plugins/google_meet/cli.py
index 45fede0..b2e8036 100644
--- a/plugins/google_meet/cli.py
+++ b/plugins/google_meet/cli.py
@@ -57,7 +57,7 @@ def register_cli(subparser: argparse.ArgumentParser) -> None:
 
     join_p = subs.add_parser("join", help="Join a Meet URL")
     join_p.add_argument("url", help="https://meet.google.com/...")
-    join_p.add_argument("--guest-name", default="Hermes Agent")
+    join_p.add_argument("--guest-name", default="Silver Core")
     join_p.add_argument("--duration", default=None, help="e.g. 30m, 2h, 90s")
     join_p.add_argument("--headed", action="store_true", help="show browser")
     join_p.add_argument(
diff --git a/plugins/google_meet/node/client.py b/plugins/google_meet/node/client.py
index 1965333..e1c63d3 100644
--- a/plugins/google_meet/node/client.py
+++ b/plugins/google_meet/node/client.py
@@ -73,7 +73,7 @@ class NodeClient:
     def start_bot(
         self,
         url: str,
-        guest_name: str = "Hermes Agent",
+        guest_name: str = "Silver Core",
         duration: Optional[str] = None,
         headed: bool = False,
         mode: str = "transcribe",
diff --git a/plugins/google_meet/process_manager.py b/plugins/google_meet/process_manager.py
index 0709c6a..060be22 100644
--- a/plugins/google_meet/process_manager.py
+++ b/plugins/google_meet/process_manager.py
@@ -87,7 +87,7 @@ def start(
     out_dir: Optional[Path] = None,
     headed: bool = False,
     auth_state: Optional[str] = None,
-    guest_name: str = "Hermes Agent",
+    guest_name: str = "Silver Core",
     duration: Optional[str] = None,
     session_id: Optional[str] = None,
     mode: str = "transcribe",
diff --git a/plugins/google_meet/tools.py b/plugins/google_meet/tools.py
index 034116b..35e9fa3 100644
--- a/plugins/google_meet/tools.py
+++ b/plugins/google_meet/tools.py
@@ -108,7 +108,7 @@ MEET_JOIN_SCHEMA: Dict[str, Any] = {
                 "type": "string",
                 "description": (
                     "Display name to use when joining as guest. Defaults to "
-                    "'Hermes Agent'."
+                    "'Silver Core'."
                 ),
             },
             "duration": {
@@ -252,7 +252,7 @@ def handle_meet_join(args: Dict[str, Any], **_kw) -> str:
         try:
             res = client.start_bot(
                 url=url,
-                guest_name=str(args.get("guest_name") or "Hermes Agent"),
+                guest_name=str(args.get("guest_name") or "Silver Core"),
                 duration=str(args.get("duration")) if args.get("duration") else None,
                 headed=bool(args.get("headed", False)),
                 mode=mode,
@@ -271,7 +271,7 @@ def handle_meet_join(args: Dict[str, Any], **_kw) -> str:
     res = pm.start(
         url=url,
         headed=bool(args.get("headed", False)),
-        guest_name=str(args.get("guest_name") or "Hermes Agent"),
+        guest_name=str(args.get("guest_name") or "Silver Core"),
         duration=str(args.get("duration")) if args.get("duration") else None,
         mode=mode,
     )
diff --git a/plugins/hermes-achievements/dashboard/dist/index.js b/plugins/hermes-achievements/dashboard/dist/index.js
index 5be8a3f..aeffe72 100644
--- a/plugins/hermes-achievements/dashboard/dist/index.js
+++ b/plugins/hermes-achievements/dashboard/dist/index.js
@@ -335,7 +335,7 @@
     // paste in the same flow.
     function tweetText() {
       const tierPart = achievement.tier ? (achievement.tier + " tier ") : "";
-      const tmpl = tx(t, "share.tweet_text", "Just unlocked {tier_part}\"{name}\" in Hermes Agent ☤", {
+      const tmpl = tx(t, "share.tweet_text", "Just unlocked {tier_part}\"{name}\" in Silver Core ☤", {
         tier_part: tierPart,
         name: achievement.name,
       });
diff --git a/plugins/image_gen/openrouter/__init__.py b/plugins/image_gen/openrouter/__init__.py
index a5c6c16..28d64d7 100644
--- a/plugins/image_gen/openrouter/__init__.py
+++ b/plugins/image_gen/openrouter/__init__.py
@@ -338,7 +338,7 @@ class OpenRouterCompatImageProvider(ImageGenProvider):
             "Content-Type": "application/json",
             # OpenRouter attribution headers (harmless against Nous Portal).
             "HTTP-Referer": "https://github.com/NousResearch/hermes-agent",
-            "X-Title": "Hermes Agent",
+            "X-Title": "Silver Core",
         }
         last_error: Optional[Dict[str, Any]] = None
         for i, model_id in enumerate(model_chain):
diff --git a/plugins/image_gen/openrouter/plugin.yaml b/plugins/image_gen/openrouter/plugin.yaml
index 3e5c3ec..8c41442 100644
--- a/plugins/image_gen/openrouter/plugin.yaml
+++ b/plugins/image_gen/openrouter/plugin.yaml
@@ -1,7 +1,7 @@
 name: openrouter
 version: 1.0.0
 description: "OpenRouter + Nous Portal image generation (chat-completions image output; reference-grounded). Text-to-image and image-to-image."
-author: Hermes Agent
+author: Silver Core
 kind: backend
 requires_env:
   - OPENROUTER_API_KEY
diff --git a/plugins/kanban/dashboard/dist/index.js b/plugins/kanban/dashboard/dist/index.js
index 959e4e6..ca85fc6 100644
--- a/plugins/kanban/dashboard/dist/index.js
+++ b/plugins/kanban/dashboard/dist/index.js
@@ -2195,7 +2195,7 @@
         ),
       ),
       h("div", { className: "flex flex-col gap-1",
-                 title: "Filter by assigned Hermes profile. Profiles are the named agent identities that claim and work on tasks." },
+                 title: "Filter by assigned Silver Core profile. Profiles are the named agent identities that claim and work on tasks." },
         h(Label, { className: "text-xs text-muted-foreground" }, tx(t, "assignee", "Assignee")),
         h(Select, Object.assign({
           value: props.assigneeFilter,
@@ -2325,7 +2325,7 @@
         }, tx(t, "setPriority", "Set priority")),
       ),
       h("div", { className: "hermes-kanban-bulk-reassign",
-                 title: "Reassign selected tasks to a different Hermes profile. Pick a profile (or unassign) and click Apply." },
+                 title: "Reassign selected tasks to a different Silver Core profile. Pick a profile (or unassign) and click Apply." },
         h(Select, Object.assign({
           value: assignee,
           className: "h-7 text-xs",
@@ -2872,7 +2872,7 @@
           h("div", { className: "hermes-kanban-card-row hermes-kanban-card-meta" },
             t.assignee
               ? h("span", { className: "hermes-kanban-assignee",
-                            title: `Assigned to Hermes profile @${t.assignee}` }, "@", t.assignee)
+                            title: `Assigned to Silver Core profile @${t.assignee}` }, "@", t.assignee)
               : h("span", { className: "hermes-kanban-unassigned",
                             title: needsAssignee
                               ? tx(i18n, "needsAssigneeHint", "Dependencies are satisfied, but the dispatcher skips this task until you assign a profile.")
@@ -3019,8 +3019,8 @@
                   : tx(t, "assigneePlaceholder", "assignee"),
                 className: "h-8 text-sm",
                 title: props.columnName === "triage"
-                  ? "Hermes profile that will spec this task (default: the dispatcher's configured specifier). Leave blank to let the dispatcher pick."
-                  : "Hermes profile to assign. Leave blank and the dispatcher will pick from available profiles when the task is Ready.",
+                  ? "Silver Core profile that will spec this task (default: the dispatcher's configured specifier). Leave blank to let the dispatcher pick."
+                  : "Silver Core profile to assign. Leave blank and the dispatcher will pick from available profiles when the task is Ready.",
                 style: { textTransform: "none" },
                 autoCapitalize: "none",
                 autoCorrect: "off",
diff --git a/plugins/kanban/dashboard/plugin_api.py b/plugins/kanban/dashboard/plugin_api.py
index 51feb4f..be6e095 100644
--- a/plugins/kanban/dashboard/plugin_api.py
+++ b/plugins/kanban/dashboard/plugin_api.py
@@ -1811,7 +1811,7 @@ def _configured_home_channels() -> list[dict]:
 
 
 def _active_profile_name() -> str:
-    """Return the current Hermes profile name for notify-sub ownership."""
+    """Return the current Silver Core profile name for notify-sub ownership."""
     try:
         from hermes_cli.profiles import get_active_profile_name
         return get_active_profile_name() or "default"
diff --git a/plugins/memory/hindsight/__init__.py b/plugins/memory/hindsight/__init__.py
index 736b255..49b846d 100644
--- a/plugins/memory/hindsight/__init__.py
+++ b/plugins/memory/hindsight/__init__.py
@@ -596,7 +596,7 @@ def _resolve_bank_id_template(template: str, fallback: str, **placeholders: str)
     """Resolve a bank_id template string with the given placeholders.
 
     Supported placeholders (each is sanitized before substitution):
-      {profile}   — active Hermes profile name (from agent_identity)
+      {profile}   — active Silver Core profile name (from agent_identity)
       {workspace} — Hermes workspace name (from agent_workspace)
       {platform}  — "cli", "telegram", "discord", etc.
       {user}      — platform user id (gateway sessions)
@@ -696,7 +696,7 @@ class HindsightMemoryProvider(MemoryProvider):
         self._auto_retain = True
         self._retain_every_n_turns = 1
         self._retain_async = True
-        self._retain_context = "conversation between Hermes Agent and the User"
+        self._retain_context = "conversation between Silver Core and the User"
         self._turn_counter = 0
         self._session_turns: list[str] = []  # accumulates ALL turns for the session
         # How many turns the last append-mode retain already shipped. Used to
@@ -1012,7 +1012,7 @@ class HindsightMemoryProvider(MemoryProvider):
             {"key": "auto_retain", "description": "Automatically retain conversation turns", "default": True},
             {"key": "retain_every_n_turns", "description": "Retain every N turns (1 = every turn)", "default": 1},
             {"key": "retain_async","description": "Process retain asynchronously on the Hindsight server", "default": True},
-            {"key": "retain_context", "description": "Context label for retained memories", "default": "conversation between Hermes Agent and the User"},
+            {"key": "retain_context", "description": "Context label for retained memories", "default": "conversation between Silver Core and the User"},
             {"key": "recall_max_tokens", "description": "Maximum tokens for recall results", "default": 4096},
             {"key": "recall_max_input_chars", "description": "Maximum input query length for auto-recall", "default": 800},
             {"key": "recall_prompt_preamble", "description": "Custom preamble for recalled memories in context"},
@@ -1339,7 +1339,7 @@ class HindsightMemoryProvider(MemoryProvider):
         # Retain controls
         self._auto_retain = self._config.get("auto_retain", True)
         self._retain_every_n_turns = max(1, int(self._config.get("retain_every_n_turns", 1)))
-        self._retain_context = self._config.get("retain_context", "conversation between Hermes Agent and the User")
+        self._retain_context = self._config.get("retain_context", "conversation between Silver Core and the User")
 
         # Recall controls
         self._auto_recall = self._config.get("auto_recall", True)
diff --git a/plugins/memory/honcho/cli.py b/plugins/memory/honcho/cli.py
index caec2f9..df130e3 100644
--- a/plugins/memory/honcho/cli.py
+++ b/plugins/memory/honcho/cli.py
@@ -161,7 +161,7 @@ def cmd_disable(args) -> None:
 def cmd_sync(args) -> None:
     """Sync Honcho config to all existing profiles.
 
-    Scans all Hermes profiles and creates host blocks for any that don't
+    Scans all Silver Core profiles and creates host blocks for any that don't
     have one yet. Inherits settings from the default host block.
     """
     try:
@@ -237,7 +237,7 @@ _profile_override: str | None = None
 
 
 def _host_key() -> str:
-    """Return the active Honcho host key, derived from the current Hermes profile."""
+    """Return the active Honcho host key, derived from the current Silver Core profile."""
     if _profile_override:
         if _profile_override in {"default", "custom"}:
             return HOST
@@ -1078,7 +1078,7 @@ def _apply_grant_to_host(hermes_host: dict, cred) -> None:
 
 
 def _active_profile_name() -> str:
-    """Return the active Hermes profile name (respects --target-profile override)."""
+    """Return the active Silver Core profile name (respects --target-profile override)."""
     if _profile_override:
         return _profile_override
     try:
diff --git a/plugins/memory/honcho/client.py b/plugins/memory/honcho/client.py
index cc1da55..ca107a1 100644
--- a/plugins/memory/honcho/client.py
+++ b/plugins/memory/honcho/client.py
@@ -36,7 +36,7 @@ HOST = "hermes"
 
 
 def profile_host_key(profile: str | None) -> str:
-    """Return the safe Honcho host key for a Hermes profile."""
+    """Return the safe Honcho host key for a Silver Core profile."""
     if not profile or profile in {"default", "custom"}:
         return HOST
     sanitized = "".join(c if c.isalnum() or c in "_-" else "_" for c in profile).strip("_")
@@ -54,7 +54,7 @@ def _host_block(raw: dict, host: str) -> dict:
 
 
 def resolve_active_host() -> str:
-    """Derive the Honcho host key from the active Hermes profile.
+    """Derive the Honcho host key from the active Silver Core profile.
 
     Resolution order:
       1. HERMES_HONCHO_HOST env var (explicit override)
@@ -76,7 +76,7 @@ def resolve_active_host() -> str:
     # Honcho's generic config can carry a defaultHost (for example "local"),
     # but applying it before profile resolution makes every named Hermes
     # profile share that same host.  Keep named profiles isolated; only the
-    # default Hermes profile may opt into the config's default host.
+    # default Silver Core profile may opt into the config's default host.
     if profile_host == HOST:
         try:
             path = resolve_config_path()
@@ -492,7 +492,7 @@ class HonchoClientConfig:
         """Create config from the resolved Honcho config path.
 
         Resolution: $HERMES_HOME/honcho.json -> ~/.honcho/config.json -> env vars.
-        When host is None, derives it from the active Hermes profile.
+        When host is None, derives it from the active Silver Core profile.
         """
         resolved_host = host or resolve_active_host()
         path = config_path or resolve_config_path()
diff --git a/plugins/memory/honcho/oauth.py b/plugins/memory/honcho/oauth.py
index 3d7cbbd..92731ed 100644
--- a/plugins/memory/honcho/oauth.py
+++ b/plugins/memory/honcho/oauth.py
@@ -46,7 +46,7 @@ def _config_refresh_lock(path: Path):
     """Machine-wide advisory lock around read-refresh-persist.
 
     The in-process ``_refresh_lock`` can't stop a second process (a sibling
-    Hermes profile or the desktop app sharing this honcho.json) from replaying
+    Silver Core profile or the desktop app sharing this honcho.json) from replaying
     the single-use refresh token and tripping reuse-detection — which revokes
     the whole grant. An OS file lock on ``<config>.lock`` serializes rotation
     across processes; best-effort, so a platform without flock degrades to
diff --git a/plugins/model-providers/ai-gateway/__init__.py b/plugins/model-providers/ai-gateway/__init__.py
index 9d01ab9..0f55897 100644
--- a/plugins/model-providers/ai-gateway/__init__.py
+++ b/plugins/model-providers/ai-gateway/__init__.py
@@ -35,7 +35,7 @@ vercel = VercelAIGatewayProfile(
     base_url="https://ai-gateway.vercel.sh/v1",
     default_headers={
         "HTTP-Referer": "https://hermes-agent.nousresearch.com",
-        "X-Title": "Hermes Agent",
+        "X-Title": "Silver Core",
     },
     default_aux_model="google/gemini-3-flash",
 )
diff --git a/plugins/model-providers/fireworks/plugin.yaml b/plugins/model-providers/fireworks/plugin.yaml
index dc9b256..7d94a69 100644
--- a/plugins/model-providers/fireworks/plugin.yaml
+++ b/plugins/model-providers/fireworks/plugin.yaml
@@ -2,4 +2,4 @@ name: fireworks-provider
 kind: model-provider
 version: 1.0.0
 description: Fireworks AI — fast inference for open and proprietary models
-author: Alex Jestin Taylor (@alex-fireworks) + Hermes Agent
+author: Alex Jestin Taylor (@alex-fireworks) + Silver Core
diff --git a/plugins/model-providers/gmi/__init__.py b/plugins/model-providers/gmi/__init__.py
index bf7dfb9..ad62514 100644
--- a/plugins/model-providers/gmi/__init__.py
+++ b/plugins/model-providers/gmi/__init__.py
@@ -13,7 +13,7 @@ gmi = ProviderProfile(
     env_vars=("GMI_API_KEY", "GMI_BASE_URL"),
     base_url="https://api.gmi-serving.com/v1",
     auth_type="api_key",
-    # Attribution so GMI can identify traffic from Hermes Agent.
+    # Attribution so GMI can identify traffic from Silver Core.
     # The generic profile.default_headers fallback in run_agent.py and
     # agent/auxiliary_client.py picks this up at client construction time.
     default_headers={"User-Agent": f"HermesAgent/{_HERMES_VERSION}"},
diff --git a/plugins/model-providers/vertex/plugin.yaml b/plugins/model-providers/vertex/plugin.yaml
index eb7479a..8753ed9 100644
--- a/plugins/model-providers/vertex/plugin.yaml
+++ b/plugins/model-providers/vertex/plugin.yaml
@@ -2,4 +2,4 @@ name: vertex-provider
 kind: model-provider
 version: 1.0.0
 description: Google Vertex AI (Gemini via OpenAI-compatible endpoint, OAuth2)
-author: Steve Lawton (@slawt), Hermes Agent
+author: Steve Lawton (@slawt), Silver Core
diff --git a/plugins/observability/nemo_relay/__init__.py b/plugins/observability/nemo_relay/__init__.py
index 13218ed..d96f482 100644
--- a/plugins/observability/nemo_relay/__init__.py
+++ b/plugins/observability/nemo_relay/__init__.py
@@ -55,7 +55,7 @@ class _Settings:
     atif_output_directory: str = ""
     atif_filename_template: str = "hermes-atif-{session_id}.json"
     atif_subagent_export_mode: str = "embedded"
-    atif_agent_name: str = "Hermes Agent"
+    atif_agent_name: str = "Silver Core"
     atif_agent_version: str = "unknown"
     atif_model_name: str = "unknown"
 
@@ -88,7 +88,7 @@ class _ProcessPluginConfiguration:
                     return True, self._activation
                 logger.warning(
                     "NeMo Relay plugin configuration is already active for another "
-                    "Hermes profile; keeping the existing process-global configuration "
+                    "Silver Core profile; keeping the existing process-global configuration "
                     "and using direct observability for this profile."
                 )
                 return False, None
diff --git a/plugins/platforms/buzz/adapter.py b/plugins/platforms/buzz/adapter.py
index b07830a..61b3dc5 100644
--- a/plugins/platforms/buzz/adapter.py
+++ b/plugins/platforms/buzz/adapter.py
@@ -1,5 +1,5 @@
 """
-Buzz Platform Adapter for Hermes Agent.
+Buzz Platform Adapter for Silver Core.
 
 A plugin-based gateway adapter that connects to a Buzz community relay
 (Block's open-source human+agent collaboration platform, built on the
diff --git a/plugins/platforms/buzz/plugin.yaml b/plugins/platforms/buzz/plugin.yaml
index 5bc8bed..cee3d76 100644
--- a/plugins/platforms/buzz/plugin.yaml
+++ b/plugins/platforms/buzz/plugin.yaml
@@ -3,7 +3,7 @@ label: Buzz
 kind: platform
 version: 1.0.0
 description: >
-  Buzz gateway adapter for Hermes Agent.
+  Buzz gateway adapter for Silver Core.
   Connects to a Buzz community relay (Block's open-source human+agent
   collaboration platform built on Nostr) and relays messages between
   channels/DMs and the Hermes agent.  Pure stdlib — shells out to the
diff --git a/plugins/platforms/dingtalk/plugin.yaml b/plugins/platforms/dingtalk/plugin.yaml
index ab22803..ebbb2e4 100644
--- a/plugins/platforms/dingtalk/plugin.yaml
+++ b/plugins/platforms/dingtalk/plugin.yaml
@@ -3,7 +3,7 @@ label: DingTalk
 kind: platform
 version: 1.0.0
 description: >
-  DingTalk gateway adapter for Hermes Agent.
+  DingTalk gateway adapter for Silver Core.
   Connects to DingTalk via the dingtalk-stream SDK (Stream Mode) and relays
   messages between DingTalk chats and the Hermes agent. Supports text, images,
   audio, video, rich text, files, group @mention gating, free-response chats,
diff --git a/plugins/platforms/discord/adapter.py b/plugins/platforms/discord/adapter.py
index 5fab230..60bcfeb 100644
--- a/plugins/platforms/discord/adapter.py
+++ b/plugins/platforms/discord/adapter.py
@@ -5391,7 +5391,7 @@ class DiscordAdapter(BasePlatformAdapter):
         async def slash_voice(interaction: discord.Interaction, mode: str = ""):
             await self._run_simple_slash(interaction, f"/voice {mode}".strip())
 
-        @tree.command(name="update", description="Update Hermes Agent to the latest version")
+        @tree.command(name="update", description="Update Silver Core to the latest version")
         async def slash_update(interaction: discord.Interaction):
             await self._run_simple_slash(interaction, "/update", "Update initiated~")
 
diff --git a/plugins/platforms/discord/plugin.yaml b/plugins/platforms/discord/plugin.yaml
index 3e09fc9..700ac42 100644
--- a/plugins/platforms/discord/plugin.yaml
+++ b/plugins/platforms/discord/plugin.yaml
@@ -3,7 +3,7 @@ label: Discord
 kind: platform
 version: 1.0.0
 description: >
-  Discord gateway adapter for Hermes Agent.
+  Discord gateway adapter for Silver Core.
   Connects to Discord via the discord.py library and relays messages
   between Discord guilds/DMs and the Hermes agent. Supports voice mode,
   slash commands, free-response channels, role-based DM auth, threads,
diff --git a/plugins/platforms/email/adapter.py b/plugins/platforms/email/adapter.py
index 572b5c1..be9d181 100644
--- a/plugins/platforms/email/adapter.py
+++ b/plugins/platforms/email/adapter.py
@@ -931,7 +931,7 @@ class EmailAdapter(BasePlatformAdapter):
 
         # Thread context for reply
         ctx = self._thread_context.get(to_addr, {})
-        subject = ctx.get("subject", "Hermes Agent")
+        subject = ctx.get("subject", "Silver Core")
         if not subject.startswith("Re:"):
             subject = f"Re: {subject}"
         msg["Subject"] = subject
@@ -1045,7 +1045,7 @@ class EmailAdapter(BasePlatformAdapter):
         msg["To"] = to_addr
 
         ctx = self._thread_context.get(to_addr, {})
-        subject = ctx.get("subject", "Hermes Agent")
+        subject = ctx.get("subject", "Silver Core")
         if not subject.startswith("Re:"):
             subject = f"Re: {subject}"
         msg["Subject"] = subject
@@ -1125,7 +1125,7 @@ class EmailAdapter(BasePlatformAdapter):
         msg["To"] = to_addr
 
         ctx = self._thread_context.get(to_addr, {})
-        subject = ctx.get("subject", "Hermes Agent")
+        subject = ctx.get("subject", "Silver Core")
         if not subject.startswith("Re:"):
             subject = f"Re: {subject}"
         msg["Subject"] = subject
@@ -1219,7 +1219,7 @@ async def _standalone_send(
         msg = MIMEText(message, "plain", "utf-8")
         msg["From"] = address
         msg["To"] = chat_id
-        msg["Subject"] = "Hermes Agent"
+        msg["Subject"] = "Silver Core"
         msg["Date"] = formatdate(localtime=True)
 
         server = smtplib.SMTP(smtp_host, smtp_port)
diff --git a/plugins/platforms/email/plugin.yaml b/plugins/platforms/email/plugin.yaml
index 8e9ca3d..07db748 100644
--- a/plugins/platforms/email/plugin.yaml
+++ b/plugins/platforms/email/plugin.yaml
@@ -3,7 +3,7 @@ label: Email
 kind: platform
 version: 1.0.0
 description: >
-  Email gateway adapter for Hermes Agent. Polls an IMAP mailbox for inbound
+  Email gateway adapter for Silver Core. Polls an IMAP mailbox for inbound
   messages and replies over SMTP, relaying email threads to and from the
   Hermes agent.
 author: NousResearch
diff --git a/plugins/platforms/feishu/plugin.yaml b/plugins/platforms/feishu/plugin.yaml
index 0eabd94..673e0c8 100644
--- a/plugins/platforms/feishu/plugin.yaml
+++ b/plugins/platforms/feishu/plugin.yaml
@@ -3,7 +3,7 @@ label: Feishu / Lark
 kind: platform
 version: 1.0.0
 description: >
-  Feishu / Lark gateway adapter for Hermes Agent.
+  Feishu / Lark gateway adapter for Silver Core.
   Connects to Feishu (China) or Lark (International) via the official
   lark-oapi SDK over WebSocket or webhook and relays messages between
   Feishu/Lark chats and the Hermes agent. Supports text, images, video,
diff --git a/plugins/platforms/google_chat/plugin.yaml b/plugins/platforms/google_chat/plugin.yaml
index d178a53..d702df4 100644
--- a/plugins/platforms/google_chat/plugin.yaml
+++ b/plugins/platforms/google_chat/plugin.yaml
@@ -3,7 +3,7 @@ label: Google Chat
 kind: platform
 version: 1.0.0
 description: >
-  Google Chat gateway adapter for Hermes Agent.
+  Google Chat gateway adapter for Silver Core.
   Connects through authenticated HTTP callbacks or an optional Cloud Pub/Sub
   pull subscription for inbound events, and uses the Google Chat REST API for
   outbound messages. Native file attachments are delivered via per-user OAuth
diff --git a/plugins/platforms/homeassistant/adapter.py b/plugins/platforms/homeassistant/adapter.py
index 8e04213..2d5fee4 100644
--- a/plugins/platforms/homeassistant/adapter.py
+++ b/plugins/platforms/homeassistant/adapter.py
@@ -403,7 +403,7 @@ class HomeAssistantAdapter(BasePlatformAdapter):
             "Content-Type": "application/json",
         }
         payload = {
-            "title": "Hermes Agent",
+            "title": "Silver Core",
             "message": content[:self.MAX_MESSAGE_LENGTH],
         }
 
diff --git a/plugins/platforms/homeassistant/plugin.yaml b/plugins/platforms/homeassistant/plugin.yaml
index b772d86..4d41302 100644
--- a/plugins/platforms/homeassistant/plugin.yaml
+++ b/plugins/platforms/homeassistant/plugin.yaml
@@ -3,7 +3,7 @@ label: Home Assistant
 kind: platform
 version: 1.0.0
 description: >
-  Home Assistant gateway adapter for Hermes Agent.
+  Home Assistant gateway adapter for Silver Core.
   Subscribes to HA's WebSocket event bus and forwards state-change events
   (with per-entity cooldowns and domain/entity filtering) to the agent.
   Outbound messages are delivered as HA persistent notifications via the
diff --git a/plugins/platforms/irc/adapter.py b/plugins/platforms/irc/adapter.py
index e78798a..1b17fa6 100644
--- a/plugins/platforms/irc/adapter.py
+++ b/plugins/platforms/irc/adapter.py
@@ -1,5 +1,5 @@
 """
-IRC Platform Adapter for Hermes Agent.
+IRC Platform Adapter for Silver Core.
 
 A plugin-based gateway adapter that connects to an IRC server and relays
 messages to/from the Hermes agent.  Zero external dependencies — uses
@@ -193,7 +193,7 @@ class IRCAdapter(BasePlatformAdapter):
         if self.server_password:
             await self._send_raw(f"PASS {self.server_password}")
         await self._send_raw(f"NICK {self.nickname}")
-        await self._send_raw(f"USER {self.nickname} 0 * :Hermes Agent")
+        await self._send_raw(f"USER {self.nickname} 0 * :Silver Core")
 
         # Start receive loop
         self._recv_task = asyncio.create_task(self._receive_loop())
@@ -231,7 +231,7 @@ class IRCAdapter(BasePlatformAdapter):
         self._mark_disconnected()
         if self._writer and not self._writer.is_closing():
             try:
-                await self._send_raw("QUIT :Hermes Agent shutting down")
+                await self._send_raw("QUIT :Silver Core shutting down")
                 await asyncio.sleep(0.5)
             except Exception:
                 pass
@@ -800,7 +800,7 @@ async def _standalone_send(
         if server_password:
             await _raw(f"PASS {_strip_irc_control_chars(server_password)}")
         await _raw(f"NICK {standalone_nick}")
-        await _raw(f"USER {standalone_nick} 0 * :Hermes Agent (cron)")
+        await _raw(f"USER {standalone_nick} 0 * :Silver Core (cron)")
 
         loop = asyncio.get_running_loop()
         deadline = loop.time() + 15.0
diff --git a/plugins/platforms/irc/plugin.yaml b/plugins/platforms/irc/plugin.yaml
index ccf83c4..945e9e2 100644
--- a/plugins/platforms/irc/plugin.yaml
+++ b/plugins/platforms/irc/plugin.yaml
@@ -3,7 +3,7 @@ label: IRC
 kind: platform
 version: 1.0.0
 description: >
-  IRC gateway adapter for Hermes Agent.
+  IRC gateway adapter for Silver Core.
   Connects to an IRC server and relays messages between an IRC channel
   (or DMs) and the Hermes agent.  No external dependencies — uses
   Python's stdlib asyncio for the IRC protocol.
diff --git a/plugins/platforms/line/adapter.py b/plugins/platforms/line/adapter.py
index cf14782..0472f4f 100644
--- a/plugins/platforms/line/adapter.py
+++ b/plugins/platforms/line/adapter.py
@@ -1,5 +1,5 @@
 """
-LINE Messaging API platform adapter for Hermes Agent.
+LINE Messaging API platform adapter for Silver Core.
 
 A bundled platform plugin that runs an aiohttp webhook server, accepts LINE
 webhook events (signature-verified), and relays messages to/from the agent
@@ -41,7 +41,7 @@ Synthesis credits
 -----------------
 
 This file is a synthesis of seven open community PRs adding LINE support
-to Hermes Agent. It deliberately ports the *strongest* idea from each into
+to Silver Core. It deliberately ports the *strongest* idea from each into
 a single plugin-form module that requires zero core edits:
 
 * PR #18153 (leepoweii)   — Template Buttons postback cache state machine,
diff --git a/plugins/platforms/line/plugin.yaml b/plugins/platforms/line/plugin.yaml
index 2f06b3e..79e1d34 100644
--- a/plugins/platforms/line/plugin.yaml
+++ b/plugins/platforms/line/plugin.yaml
@@ -3,7 +3,7 @@ label: LINE
 kind: platform
 version: 1.0.0
 description: >
-  LINE Messaging API gateway adapter for Hermes Agent.
+  LINE Messaging API gateway adapter for Silver Core.
   Runs an aiohttp webhook server that receives LINE webhook events
   (with HMAC-SHA256 signature verification) and relays messages between
   LINE chats (1:1, groups, rooms) and the Hermes agent. Outbound replies
@@ -11,7 +11,7 @@ description: >
   when the token has expired or is absent. Slow LLM responses surface a
   Template Buttons postback bubble so the user can fetch the answer with
   a fresh reply token (free) once it's ready.
-author: Hermes Agent contributors
+author: Silver Core contributors
 # ``requires_env`` and ``optional_env`` entries are surfaced in the
 # ``hermes config`` UI via the platform-plugin env var injector in
 # ``hermes_cli/config.py``.
diff --git a/plugins/platforms/matrix/adapter.py b/plugins/platforms/matrix/adapter.py
index 8ed4789..69fe9ca 100644
--- a/plugins/platforms/matrix/adapter.py
+++ b/plugins/platforms/matrix/adapter.py
@@ -1458,7 +1458,7 @@ class MatrixAdapter(BasePlatformAdapter):
                 resp = await client.login(
                     identifier=self._user_id,
                     password=self._password,
-                    device_name="Hermes Agent",
+                    device_name="Silver Core",
                     device_id=self._device_id or None,
                 )
                 if resp and hasattr(resp, "device_id"):
@@ -4500,7 +4500,7 @@ class MatrixAdapter(BasePlatformAdapter):
 
         Important: only strip explicit mention tokens (``@user:server`` or
         ``@localpart``). Do NOT strip bare words matching the bot localpart,
-        otherwise normal phrases like "Hermes Agent" become "Agent".
+        otherwise normal phrases like "Silver Core" become "Agent".
         """
         if not body:
             return ""
diff --git a/plugins/platforms/matrix/plugin.yaml b/plugins/platforms/matrix/plugin.yaml
index 77d65d9..d47b871 100644
--- a/plugins/platforms/matrix/plugin.yaml
+++ b/plugins/platforms/matrix/plugin.yaml
@@ -3,7 +3,7 @@ label: Matrix
 kind: platform
 version: 1.0.0
 description: >
-  Matrix gateway adapter for Hermes Agent.
+  Matrix gateway adapter for Silver Core.
   Connects to a Matrix homeserver via mautrix (with optional E2EE) and relays
   messages between Matrix rooms/DMs and the Hermes agent. Supports threads,
   HTML/markdown rendering, native media uploads, mention gating, free-response
diff --git a/plugins/platforms/mattermost/plugin.yaml b/plugins/platforms/mattermost/plugin.yaml
index 3ee5814..847e188 100644
--- a/plugins/platforms/mattermost/plugin.yaml
+++ b/plugins/platforms/mattermost/plugin.yaml
@@ -3,7 +3,7 @@ label: Mattermost
 kind: platform
 version: 1.0.0
 description: >
-  Mattermost gateway adapter for Hermes Agent.
+  Mattermost gateway adapter for Silver Core.
   Connects to a self-hosted or cloud Mattermost instance via the v4 REST
   API + WebSocket event stream and relays messages between Mattermost
   channels/DMs and the Hermes agent. Supports thread-mode replies, native
diff --git a/plugins/platforms/ntfy/plugin.yaml b/plugins/platforms/ntfy/plugin.yaml
index e476a36..315fde4 100644
--- a/plugins/platforms/ntfy/plugin.yaml
+++ b/plugins/platforms/ntfy/plugin.yaml
@@ -3,7 +3,7 @@ label: ntfy
 kind: platform
 version: 1.0.0
 description: >
-  ntfy push-notification gateway adapter for Hermes Agent.
+  ntfy push-notification gateway adapter for Silver Core.
   Subscribes to a topic on ntfy.sh or any self-hosted ntfy server via
   HTTP streaming, and publishes replies via HTTP POST. Lightweight —
   no external SDK, only httpx (already a Hermes dependency).
diff --git a/plugins/platforms/photon/adapter.py b/plugins/platforms/photon/adapter.py
index abebe36..10a30aa 100644
--- a/plugins/platforms/photon/adapter.py
+++ b/plugins/platforms/photon/adapter.py
@@ -1,5 +1,5 @@
 """
-Photon Spectrum (iMessage) platform adapter for Hermes Agent.
+Photon Spectrum (iMessage) platform adapter for Silver Core.
 
 Both directions of traffic flow through a small supervised Node sidecar
 (see ``sidecar/index.mjs``) that runs the ``spectrum-ts`` SDK — the SDK is
diff --git a/plugins/platforms/photon/auth.py b/plugins/platforms/photon/auth.py
index b8b356a..2c5e76b 100644
--- a/plugins/platforms/photon/auth.py
+++ b/plugins/platforms/photon/auth.py
@@ -74,7 +74,7 @@ DEFAULT_DASHBOARD_HOST = "https://app.photon.codes"
 DEFAULT_SPECTRUM_HOST = "https://spectrum.photon.codes"
 
 # Default name of the project Hermes provisions for the operator.
-DEFAULT_PROJECT_NAME = "Hermes Agent"
+DEFAULT_PROJECT_NAME = "Silver Core"
 
 # Polling defaults per RFC 8628.  Photon overrides via `interval` /
 # `expires_in` in the device-code response — those win.
@@ -88,7 +88,7 @@ E164_RE = re.compile(r"^\+[1-9]\d{6,14}$")
 # auth.json helpers — share the file with the rest of hermes-agent.
 
 def _auth_json_path() -> Path:
-    """Resolve ``~/.hermes/auth.json`` honouring the active Hermes profile."""
+    """Resolve ``~/.hermes/auth.json`` honouring the active Silver Core profile."""
     try:
         from hermes_constants import get_hermes_home
         return Path(get_hermes_home()) / "auth.json"
diff --git a/plugins/platforms/photon/cli.py b/plugins/platforms/photon/cli.py
index 1cc19f6..b47ecd8 100644
--- a/plugins/platforms/photon/cli.py
+++ b/plugins/platforms/photon/cli.py
@@ -72,7 +72,7 @@ def register_cli(parser: argparse.ArgumentParser) -> None:
         help="First-time setup (device login + project + user + sidecar)",
     )
     p_setup.add_argument("--project-name", default=None,
-                         help="Project name (default: 'Hermes Agent')")
+                         help="Project name (default: 'Silver Core')")
     p_setup.add_argument("--phone", default=None,
                          help="Your E.164 phone number (e.g. +15551234567)")
     p_setup.add_argument("--first-name", default=None)
@@ -181,7 +181,7 @@ def _cmd_setup(args: argparse.Namespace) -> int:
     else:
         print("[1/5] Reusing existing Photon token")
 
-    # 2. Find or create the "Hermes Agent" project.
+    # 2. Find or create the "Silver Core" project.
     name = args.project_name or photon_auth.DEFAULT_PROJECT_NAME
     dashboard_id = photon_auth.load_dashboard_project_id()
     try:
diff --git a/plugins/platforms/photon/plugin.yaml b/plugins/platforms/photon/plugin.yaml
index a39193a..445c9d4 100644
--- a/plugins/platforms/photon/plugin.yaml
+++ b/plugins/platforms/photon/plugin.yaml
@@ -3,7 +3,7 @@ label: iMessage via Photon
 kind: platform
 version: 0.3.0
 description: >
-  Photon Spectrum gateway adapter for Hermes Agent.
+  Photon Spectrum gateway adapter for Silver Core.
   Connects to iMessage (and other Spectrum interfaces) through Photon's
   managed Spectrum platform. Both directions run over the `spectrum-ts`
   SDK's long-lived gRPC stream via a small supervised Node sidecar —
diff --git a/plugins/platforms/photon/sidecar/index.mjs b/plugins/platforms/photon/sidecar/index.mjs
index 5c8611f..dfcad87 100644
--- a/plugins/platforms/photon/sidecar/index.mjs
+++ b/plugins/platforms/photon/sidecar/index.mjs
@@ -1,4 +1,4 @@
-// Hermes Agent — Photon Spectrum sidecar
+// Silver Core — Photon Spectrum sidecar
 //
 // Spawned by `plugins/platforms/photon/adapter.py` to bridge BOTH directions
 // of messaging to Photon's Spectrum platform via the `spectrum-ts` SDK (the
diff --git a/plugins/platforms/photon/sidecar/package.json b/plugins/platforms/photon/sidecar/package.json
index 0d0cff3..8290e73 100644
--- a/plugins/platforms/photon/sidecar/package.json
+++ b/plugins/platforms/photon/sidecar/package.json
@@ -2,7 +2,7 @@
   "name": "@hermes-agent/photon-sidecar",
   "private": true,
   "version": "0.4.0",
-  "description": "Spectrum-ts bridge for the Hermes Agent Photon platform plugin.",
+  "description": "Spectrum-ts bridge for the Silver Core Photon platform plugin.",
   "type": "module",
   "main": "index.mjs",
   "scripts": {
diff --git a/plugins/platforms/raft/plugin.yaml b/plugins/platforms/raft/plugin.yaml
index 81b772e..0ccf8ca 100644
--- a/plugins/platforms/raft/plugin.yaml
+++ b/plugins/platforms/raft/plugin.yaml
@@ -3,7 +3,7 @@ label: Raft
 kind: platform
 version: 1.0.0
 description: >
-  Raft gateway adapter for Hermes Agent.
+  Raft gateway adapter for Silver Core.
   Connects to a Raft workspace as an external agent via a local
   wake-channel bridge. The adapter starts a loopback HTTP endpoint
   that receives content-free wake hints from the bridge, then
diff --git a/plugins/platforms/simplex/plugin.yaml b/plugins/platforms/simplex/plugin.yaml
index 84ef401..73391b6 100644
--- a/plugins/platforms/simplex/plugin.yaml
+++ b/plugins/platforms/simplex/plugin.yaml
@@ -3,7 +3,7 @@ label: SimpleX Chat
 kind: platform
 version: 1.1.0
 description: >
-  SimpleX Chat gateway adapter for Hermes Agent.
+  SimpleX Chat gateway adapter for Silver Core.
   Connects to a local simplex-chat daemon via WebSocket and relays
   messages between SimpleX contacts/groups and the Hermes agent.
   SimpleX is decentralised and assigns no persistent user IDs —
diff --git a/plugins/platforms/slack/plugin.yaml b/plugins/platforms/slack/plugin.yaml
index b04ac8b..b4a2a01 100644
--- a/plugins/platforms/slack/plugin.yaml
+++ b/plugins/platforms/slack/plugin.yaml
@@ -3,7 +3,7 @@ label: Slack
 kind: platform
 version: 1.0.0
 description: >
-  Slack gateway adapter for Hermes Agent.
+  Slack gateway adapter for Silver Core.
   Connects to Slack via slack-bolt in Socket Mode and relays messages
   between Slack channels/DMs and the Hermes agent. Supports slash
   commands, threads, mrkdwn rendering, approval blocks, free-response
diff --git a/plugins/platforms/sms/plugin.yaml b/plugins/platforms/sms/plugin.yaml
index 222106b..1800c78 100644
--- a/plugins/platforms/sms/plugin.yaml
+++ b/plugins/platforms/sms/plugin.yaml
@@ -3,7 +3,7 @@ label: SMS (Twilio)
 kind: platform
 version: 1.0.0
 description: >
-  SMS gateway adapter for Hermes Agent via Twilio. Sends and receives SMS
+  SMS gateway adapter for Silver Core via Twilio. Sends and receives SMS
   through the Twilio REST API + inbound webhook, relaying texts between phone
   numbers and the Hermes agent. Markdown is stripped to plain text.
 author: NousResearch
diff --git a/plugins/platforms/teams/adapter.py b/plugins/platforms/teams/adapter.py
index db3ae77..6df46e0 100644
--- a/plugins/platforms/teams/adapter.py
+++ b/plugins/platforms/teams/adapter.py
@@ -1,5 +1,5 @@
 """
-Microsoft Teams platform adapter for Hermes Agent.
+Microsoft Teams platform adapter for Silver Core.
 
 Uses the microsoft-teams-apps SDK for authentication and activity processing.
 Runs an aiohttp webhook server to receive messages from Teams.
diff --git a/plugins/platforms/teams/plugin.yaml b/plugins/platforms/teams/plugin.yaml
index 6d6e301..fd8372f 100644
--- a/plugins/platforms/teams/plugin.yaml
+++ b/plugins/platforms/teams/plugin.yaml
@@ -3,7 +3,7 @@ label: Microsoft Teams
 kind: platform
 version: 1.0.0
 description: >
-  Microsoft Teams gateway adapter for Hermes Agent.
+  Microsoft Teams gateway adapter for Silver Core.
   Connects to Microsoft Teams via the Bot Framework and relays messages
   between Teams chats (personal DMs, group chats, channel posts) and
   the Hermes agent. Supports Adaptive Card approval prompts.
diff --git a/plugins/platforms/telegram/plugin.yaml b/plugins/platforms/telegram/plugin.yaml
index 468081d..8f0d661 100644
--- a/plugins/platforms/telegram/plugin.yaml
+++ b/plugins/platforms/telegram/plugin.yaml
@@ -3,7 +3,7 @@ label: Telegram
 kind: platform
 version: 1.0.0
 description: >
-  Telegram gateway adapter for Hermes Agent.
+  Telegram gateway adapter for Silver Core.
   Connects to Telegram via python-telegram-bot and relays messages between
   Telegram chats/groups/topics and the Hermes agent. Supports threads/topics,
   streaming edits, native media, inline keyboards, slash commands, fallback
diff --git a/plugins/platforms/wecom/plugin.yaml b/plugins/platforms/wecom/plugin.yaml
index ea213be..1dba2b5 100644
--- a/plugins/platforms/wecom/plugin.yaml
+++ b/plugins/platforms/wecom/plugin.yaml
@@ -3,7 +3,7 @@ label: WeCom (Enterprise WeChat)
 kind: platform
 version: 1.0.0
 description: >
-  WeCom / Enterprise WeChat gateway adapter for Hermes Agent. Registers two
+  WeCom / Enterprise WeChat gateway adapter for Silver Core. Registers two
   platforms: ``wecom`` (Smart Robot over WebSocket) and ``wecom_callback``
   (self-built apps over an HTTP callback endpoint with AES message crypto).
   Relays messages between WeCom chats and the Hermes agent.
diff --git a/plugins/platforms/whatsapp/plugin.yaml b/plugins/platforms/whatsapp/plugin.yaml
index 7446f52..dfb5e89 100644
--- a/plugins/platforms/whatsapp/plugin.yaml
+++ b/plugins/platforms/whatsapp/plugin.yaml
@@ -3,7 +3,7 @@ label: WhatsApp
 kind: platform
 version: 1.0.0
 description: >
-  WhatsApp gateway adapter for Hermes Agent.
+  WhatsApp gateway adapter for Silver Core.
   Connects to WhatsApp via a local Node.js bridge (WhatsApp Web client) over
   an HTTP API and relays messages between WhatsApp chats and the Hermes agent.
   Supports DM/group policies, mention gating, free-response chats, and
diff --git a/plugins/security-guidance/patterns.py b/plugins/security-guidance/patterns.py
index 6980888..cb3c20d 100644
--- a/plugins/security-guidance/patterns.py
+++ b/plugins/security-guidance/patterns.py
@@ -22,7 +22,7 @@ Forked verbatim from Anthropic's claude-plugins-official repository
   See the License for the specific language governing permissions and
   limitations under the License.
 
-Modifications by NousResearch for the Hermes Agent plugin port:
+Modifications by NousResearch for the Silver Core plugin port:
   - none to the pattern data itself; this file is byte-for-byte the upstream
     patterns.py at commit 0bde168 (2026-05-26). Hermes-side wiring lives in
     __init__.py.
diff --git a/tools/browser_camofox.py b/tools/browser_camofox.py
index 4151d3c..9d62a93 100644
--- a/tools/browser_camofox.py
+++ b/tools/browser_camofox.py
@@ -356,7 +356,7 @@ def _get_session(task_id: Optional[str]) -> Dict[str, Any]:
     """Get or create a camofox session for the given task.
 
     When managed persistence is enabled, uses a deterministic userId
-    derived from the Hermes profile so the Camofox server can map it
+    derived from the Silver Core profile so the Camofox server can map it
     to the same persistent browser profile across restarts.
     """
     task_id = task_id or "default"
diff --git a/tools/browser_camofox_state.py b/tools/browser_camofox_state.py
index 3a2bde0..d090baa 100644
--- a/tools/browser_camofox_state.py
+++ b/tools/browser_camofox_state.py
@@ -27,7 +27,7 @@ def get_camofox_state_dir() -> Path:
 def get_camofox_identity(task_id: Optional[str] = None) -> Dict[str, str]:
     """Return the stable Hermes-managed Camofox identity for this profile.
 
-    The user identity is profile-scoped (same Hermes profile = same userId).
+    The user identity is profile-scoped (same Silver Core profile = same userId).
     The session key is scoped to the logical browser task so newly created
     tabs within the same profile reuse the same identity contract.
     """
diff --git a/tools/cronjob_tools.py b/tools/cronjob_tools.py
index c0637de..2dd078e 100644
--- a/tools/cronjob_tools.py
+++ b/tools/cronjob_tools.py
@@ -1,5 +1,5 @@
 """
-Cron job management tools for Hermes Agent.
+Cron job management tools for Silver Core.
 
 Expose a single compressed action-oriented tool to avoid schema/context bloat.
 Compatibility wrappers remain for direct Python callers and legacy tests.
diff --git a/tools/environments/docker.py b/tools/environments/docker.py
index def1ac1..f2981d4 100644
--- a/tools/environments/docker.py
+++ b/tools/environments/docker.py
@@ -126,7 +126,7 @@ def _sanitize_label_value(value: str) -> str:
 
 
 def _get_active_profile_name() -> str:
-    """Return the active Hermes profile name, or ``"default"`` on any error.
+    """Return the active Silver Core profile name, or ``"default"`` on any error.
 
     Resolved at container-create time so a single container is permanently
     tagged with the profile that created it. Profile switches inside the
@@ -1307,7 +1307,7 @@ class DockerEnvironment(BaseEnvironment):
         #   * future cross-process reuse (`hermes-task-id`, `hermes-profile`)
         #   * operators running `docker ps --filter label=hermes-agent=1`
         # Values are limited to the safe character set defined by
-        # _sanitize_label_value(); the active Hermes profile is captured at
+        # _sanitize_label_value(); the active Silver Core profile is captured at
         # container-start time and never changes for the container's lifetime.
         profile_name = _sanitize_label_value(_get_active_profile_name())
         task_label = _sanitize_label_value(task_id)
diff --git a/tools/environments/local.py b/tools/environments/local.py
index f3ad9d5..6fcb988 100644
--- a/tools/environments/local.py
+++ b/tools/environments/local.py
@@ -788,7 +788,7 @@ def _find_bash() -> str:
         return candidates[0]
 
     raise RuntimeError(
-        "Git Bash not found. Hermes Agent requires Git for Windows on Windows.\n"
+        "Git Bash not found. Silver Core requires Git for Windows on Windows.\n"
         "Install it from: https://git-scm.com/download/win\n"
         "Or set HERMES_GIT_BASH_PATH to your bash.exe location."
     )
diff --git a/tools/file_tools.py b/tools/file_tools.py
index 6a100c0..2c56735 100644
--- a/tools/file_tools.py
+++ b/tools/file_tools.py
@@ -1959,7 +1959,7 @@ WRITE_FILE_SCHEMA = {
             "content": {"type": "string", "description": "Complete content to write to the file"},
             "cross_profile": {
                 "type": "boolean",
-                "description": "Opt out of the cross-profile soft guard. Defaults to false. Set true ONLY after explicit user direction to edit another Hermes profile's skills/plugins/cron/memories — by default these writes are blocked with a warning because they affect a different profile than the one this session is running under.",
+                "description": "Opt out of the cross-profile soft guard. Defaults to false. Set true ONLY after explicit user direction to edit another Silver Core profile's skills/plugins/cron/memories — by default these writes are blocked with a warning because they affect a different profile than the one this session is running under.",
                 "default": False,
             },
         },
@@ -2010,7 +2010,7 @@ PATCH_SCHEMA = {
             },
             "cross_profile": {
                 "type": "boolean",
-                "description": "Opt out of the cross-profile soft guard. Defaults to false. Set true ONLY after explicit user direction to edit another Hermes profile's skills/plugins/cron/memories.",
+                "description": "Opt out of the cross-profile soft guard. Defaults to false. Set true ONLY after explicit user direction to edit another Silver Core profile's skills/plugins/cron/memories.",
                 "default": False,
             },
         },
diff --git a/tools/lazy_deps.py b/tools/lazy_deps.py
index f0f4567..5ebb011 100644
--- a/tools/lazy_deps.py
+++ b/tools/lazy_deps.py
@@ -1,5 +1,5 @@
 """
-Lazy dependency installer for opt-in Hermes Agent backends.
+Lazy dependency installer for opt-in Silver Core backends.
 
 Many Hermes features (Mistral TTS, ElevenLabs TTS, Honcho memory, Bedrock,
 Slack, Matrix, etc.) require Python packages that not every user needs. The
diff --git a/tools/mcp_oauth.py b/tools/mcp_oauth.py
index ea19338..c89e0aa 100644
--- a/tools/mcp_oauth.py
+++ b/tools/mcp_oauth.py
@@ -31,7 +31,7 @@ Configuration in config.yaml::
           redirect_port: 0                      # 0 = auto-pick free port
           redirect_uri: "https://proxy/callback"  # default: loopback callback
           redirect_host: "localhost"            # loopback hostname (WAF-safe)
-          client_name: "My Custom Client"       # default: "Hermes Agent"
+          client_name: "My Custom Client"       # default: "Silver Core"
 """
 
 import asyncio
@@ -1128,7 +1128,7 @@ def _resolve_redirect_uri(cfg: dict, port: int) -> str:
 # of 2026-07, verified by live call against api.figma.com):
 #   "Claude Code" → 200
 #   "Codex"       → 200
-#   "Hermes Agent" / "Hermes" / "Cursor" / "VS Code" / … → 403
+#   "Silver Core" / "Hermes" / "Cursor" / "VS Code" / … → 403
 # pi-figma-remote-auth and similar tools work around this the same way — register
 # under an allowlisted name so the browser flow can start. User can still pin a
 # different name via oauth.client_name if Figma ever admits one.
@@ -1197,7 +1197,7 @@ def _build_client_metadata(cfg: dict) -> "OAuthClientMetadata":
         )
     if OAuthClientMetadata is None:
         _ensure_sdk_loaded()
-    client_name = cfg.get("client_name", "Hermes Agent")
+    client_name = cfg.get("client_name", "Silver Core")
     scope = cfg.get("scope")
     redirect_uri = _resolve_redirect_uri(cfg, port)
 
diff --git a/tools/session_search_tool.py b/tools/session_search_tool.py
index ed9fd49..18ca0d7 100644
--- a/tools/session_search_tool.py
+++ b/tools/session_search_tool.py
@@ -1106,7 +1106,7 @@ SESSION_SEARCH_SCHEMA = {
             "profile": {
                 "type": "string",
                 "description": (
-                    "Optional. Read sessions from another Hermes profile's database "
+                    "Optional. Read sessions from another Silver Core profile's database "
                     "(read-only). Use when resolving an `@session:<profile>/<id>` link: "
                     "pass the profile segment here with session_id as the id segment. "
                     "Omit to use the current profile."
diff --git a/tools/skill_manager_tool.py b/tools/skill_manager_tool.py
index 9c89e6b..e09ec7d 100644
--- a/tools/skill_manager_tool.py
+++ b/tools/skill_manager_tool.py
@@ -738,7 +738,7 @@ def _org_mirror_write_guard(name: str, skill_path: Path, action: str) -> Optiona
 
 
 def _find_skill_in_other_profiles(name: str) -> List[Tuple[str, Path]]:
-    """Look for ``name`` under SKILL.md across OTHER Hermes profiles.
+    """Look for ``name`` under SKILL.md across OTHER Silver Core profiles.
 
     Returns a list of ``(profile_name, skill_dir)`` pairs. Used to make
     the "Skill X not found" error explain when the user is editing the
diff --git a/tools/tool_search.py b/tools/tool_search.py
index e286fa0..376dbd2 100644
--- a/tools/tool_search.py
+++ b/tools/tool_search.py
@@ -1,4 +1,4 @@
-"""Progressive tool disclosure ("tool search") for Hermes Agent.
+"""Progressive tool disclosure ("tool search") for Silver Core.
 
 When enabled, MCP and non-core plugin tools are replaced in the model-visible
 tools array by three bridge tools — ``tool_search``, ``tool_describe``,
diff --git a/tools/xai_http.py b/tools/xai_http.py
index 8ef0b85..c79b0f6 100644
--- a/tools/xai_http.py
+++ b/tools/xai_http.py
@@ -101,7 +101,7 @@ def hermes_xai_default_headers() -> Dict[str, str]:
     """Default headers for OpenAI-SDK and raw HTTP clients talking to xAI.
 
     Replaces the OpenAI Python SDK's identifying ``User-Agent: OpenAI/Python …``
-    so chat/completions and Responses traffic is attributed as Hermes Agent,
+    so chat/completions and Responses traffic is attributed as Silver Core,
     matching the direct HTTP integrations (search, TTS, STT, image, video).
     """
     return {"User-Agent": hermes_xai_user_agent()}
diff --git a/ui-tui/src/__tests__/theme.test.ts b/ui-tui/src/__tests__/theme.test.ts
index 5496ae6..e8efc2e 100644
--- a/ui-tui/src/__tests__/theme.test.ts
+++ b/ui-tui/src/__tests__/theme.test.ts
@@ -43,7 +43,7 @@ describe('DEFAULT_THEME', () => {
   it('has brand defaults', async () => {
     const { DEFAULT_THEME } = await importThemeWithCleanEnv()
 
-    expect(DEFAULT_THEME.brand.name).toBe('Hermes Agent')
+    expect(DEFAULT_THEME.brand.name).toBe('Silver Core')
     expect(DEFAULT_THEME.brand.prompt).toBe('❯')
     expect(DEFAULT_THEME.brand.tool).toBe('┊')
   })
diff --git a/ui-tui/src/__tests__/useSessionLifecycle.test.ts b/ui-tui/src/__tests__/useSessionLifecycle.test.ts
index f214aa1..043939c 100644
--- a/ui-tui/src/__tests__/useSessionLifecycle.test.ts
+++ b/ui-tui/src/__tests__/useSessionLifecycle.test.ts
@@ -40,7 +40,7 @@ describe('writeActiveSessionFile', () => {
   })
 
   it('writes the actual resumed session id for the shell exit summary', () => {
-    dir = mkdtempSync(join(tmpdir(), 'hermes-tui-active-'))
+    dir = mkdtempSync(join(tmpdir(), 'silver-core-tui-active-'))
     const path = join(dir, 'active.json')
 
     writeActiveSessionFile('actual_session', path)
diff --git a/ui-tui/src/app/slash/commands/core.ts b/ui-tui/src/app/slash/commands/core.ts
index 00321ec..efa0224 100644
--- a/ui-tui/src/app/slash/commands/core.ts
+++ b/ui-tui/src/app/slash/commands/core.ts
@@ -147,7 +147,7 @@ export const coreCommands: SlashCommand[] = [
   },
 
   {
-    help: 'update Hermes Agent to the latest version (exits TUI)',
+    help: 'update Silver Core to the latest version (exits TUI)',
     name: 'update',
     run: (_arg, ctx) => {
       if (DASHBOARD_TUI_MODE) {
diff --git a/ui-tui/src/app/slash/commands/topup.ts b/ui-tui/src/app/slash/commands/topup.ts
index b3ab23c..3533fd4 100644
--- a/ui-tui/src/app/slash/commands/topup.ts
+++ b/ui-tui/src/app/slash/commands/topup.ts
@@ -68,9 +68,9 @@ const renderBillingError = (
 
     case 'remote_spending_disabled':
       // Account-wide switch is OFF (dual-emitted error/code). A billing admin can
-      // turn it on from the portal's Hermes Agent page; this is NOT a per-terminal stop.
+      // turn it on from the portal's Silver Core page; this is NOT a per-terminal stop.
       sys(
-        "Remote spending is off for this account — a billing admin can turn it on from the portal's Hermes Agent page."
+        "Remote spending is off for this account — a billing admin can turn it on from the portal's Silver Core page."
       )
 
       break
diff --git a/ui-tui/src/components/billingOverlay.tsx b/ui-tui/src/components/billingOverlay.tsx
index 269c8e0..4f6ba13 100644
--- a/ui-tui/src/components/billingOverlay.tsx
+++ b/ui-tui/src/components/billingOverlay.tsx
@@ -92,7 +92,7 @@ function OverviewScreen({ ctx, onClose, onPatch, s, t }: ScreenProps) {
   const note = !s.is_admin
     ? 'Billing actions need someone with billing permissions (owner, admin, or finance admin).'
     : !s.cli_billing_enabled
-      ? "Remote spending is off for this org — a billing admin can turn it on from the portal's Hermes Agent page."
+      ? "Remote spending is off for this org — a billing admin can turn it on from the portal's Silver Core page."
       : null
 
   // Always show the full billing menu for an admin/billing-on org — a missing
diff --git a/ui-tui/src/entry.tsx b/ui-tui/src/entry.tsx
index 6f856cf..d70e681 100644
--- a/ui-tui/src/entry.tsx
+++ b/ui-tui/src/entry.tsx
@@ -15,7 +15,7 @@ import { recordParentLifecycle } from './lib/parentLog.js'
 import { resetTerminalModes } from './lib/terminalModes.js'
 
 if (!process.stdin.isTTY) {
-  console.log('hermes-tui: no TTY')
+  console.log('silver-core-tui: no TTY')
   process.exit(0)
 }
 
@@ -53,7 +53,7 @@ const gw = new GatewayClient()
 gw.start()
 
 const dumpNotice = (snap: MemorySnapshot, dump: HeapDumpResult | null) =>
-  `hermes-tui: ${snap.level} memory (${formatBytes(snap.heapUsed)}) — auto heap dump → ${dump?.heapPath ?? dump?.diagPath ?? '(failed)'}\n`
+  `silver-core-tui: ${snap.level} memory (${formatBytes(snap.heapUsed)}) — auto heap dump → ${dump?.heapPath ?? dump?.diagPath ?? '(failed)'}\n`
 
 let consecutiveDeadStreamErrors = 0
 
@@ -90,7 +90,7 @@ setupGracefulExit({
     consecutiveDeadStreamErrors = 0
 
     try {
-      process.stderr.write(`hermes-tui lifecycle ${scope}: ${message.slice(0, 2000)}\n`)
+      process.stderr.write(`silver-core-tui lifecycle ${scope}: ${message.slice(0, 2000)}\n`)
     } catch {
       // stderr may be the dead stream itself.
     }
@@ -101,7 +101,7 @@ setupGracefulExit({
     // what tells SIGHUP (terminal/SSH dropped) apart from a real SIGTERM.
     recordParentLifecycle(`graceful-exit received signal=${signal} → killing gateway`)
     resetTerminalModes()
-    process.stderr.write(`hermes-tui lifecycle: received ${signal}\n`)
+    process.stderr.write(`silver-core-tui lifecycle: received ${signal}\n`)
   },
   // The dashboard chat tab has no in-page restart path after the PTY child
   // exits. Ignore SIGINT there so Ctrl+C cannot kill the embedded TUI if raw
@@ -120,10 +120,10 @@ const stopMemoryMonitor = startMemoryMonitor({
     )
     resetTerminalModes()
     process.stderr.write(
-      `hermes-tui lifecycle: memory critical exit heap=${formatBytes(snap.heapUsed)} rss=${formatBytes(snap.rss)}\n`
+      `silver-core-tui lifecycle: memory critical exit heap=${formatBytes(snap.heapUsed)} rss=${formatBytes(snap.rss)}\n`
     )
     process.stderr.write(dumpNotice(snap, dump))
-    process.stderr.write('hermes-tui: exiting to avoid OOM; restart to recover\n')
+    process.stderr.write('silver-core-tui: exiting to avoid OOM; restart to recover\n')
     process.exit(137)
   },
   onHigh: (snap, dump) => process.stderr.write(dumpNotice(snap, dump)),
@@ -136,7 +136,7 @@ const stopMemoryMonitor = startMemoryMonitor({
       `memory-warning fast heap growth heap=${formatBytes(snap.heapUsed)} rss=${formatBytes(snap.rss)}`
     )
     process.stderr.write(
-      `hermes-tui: heap climbing fast (${formatBytes(snap.heapUsed)}) — a large tool output or long session may be straining memory\n`
+      `silver-core-tui: heap climbing fast (${formatBytes(snap.heapUsed)}) — a large tool output or long session may be straining memory\n`
     )
   }
 })
diff --git a/ui-tui/src/lib/parentLog.ts b/ui-tui/src/lib/parentLog.ts
index 9af4030..7dd8e74 100644
--- a/ui-tui/src/lib/parentLog.ts
+++ b/ui-tui/src/lib/parentLog.ts
@@ -53,7 +53,7 @@ export function recordParentLifecycle(line: string): void {
   } catch {
     if (!warned) {
       warned = true
-      process.stderr.write('hermes-tui: parent lifecycle log unavailable\n')
+      process.stderr.write('silver-core-tui: parent lifecycle log unavailable\n')
     }
   }
 }
diff --git a/ui-tui/src/sdk/apps/weather.tsx b/ui-tui/src/sdk/apps/weather.tsx
index 676a067..544d217 100644
--- a/ui-tui/src/sdk/apps/weather.tsx
+++ b/ui-tui/src/sdk/apps/weather.tsx
@@ -79,7 +79,7 @@ const artColor = (art: Art, t: Theme): string =>
 
 async function fetchReport(location: string): Promise<Report> {
   const res = await fetch(`https://wttr.in/${encodeURIComponent(location)}?format=j1`, {
-    headers: { 'User-Agent': 'hermes-tui-weather' },
+    headers: { 'User-Agent': 'silver-core-tui-weather' },
     signal: AbortSignal.timeout(10_000)
   })
 
diff --git a/ui-tui/src/theme.ts b/ui-tui/src/theme.ts
index 11ef941..a30712e 100644
--- a/ui-tui/src/theme.ts
+++ b/ui-tui/src/theme.ts
@@ -250,7 +250,7 @@ export function themeToneHex(tone: string): string {
 // ── Defaults ─────────────────────────────────────────────────────────
 
 const BRAND: ThemeBrand = {
-  name: 'Hermes Agent',
+  name: 'Silver Core',
   icon: '⚕',
   prompt: '❯',
   welcome: 'Type your message or /help for commands.',
