# 决策日志

> 最后更新：2026-07-17 by SCS-REQ-REPOS-01 实现会话（新增 2 条：①SDK drop-in 兼容承诺退役→「模型面贴近、宿主面自主」+ 扩展面三缝与钩子契约总则；②「机制归引擎、钟与装配归宿主」终裁 + 斜杠解析退出引擎行为；均守密人需求档 §6 裁定授权代写）。前批 2026-07-12 by 黑池防火墙归因口径会话（新增 1 条：归因回流 = 需求非数据——黑池墙内自析合规 / 过墙的是需求非原始数据 / 守密人当回流过滤器 / 方向全程同向；细化 §1.1-HC「归因回流」合法形态 + T33 操作口径；守密人「可以落档」授权代写）。前批同日 by 踩坑档案盘点会话（新增 1 条：踩坑档案治理体系 + 记忆巡检长期机制——甲·盘点重构（硬伤修复/同簇并主条/lessons-archive 分层）+ 乙·准则清单体与毕业纪律 + 丙·长期维护三件套（月检自绑定例程 / memory_freshness 机械守卫与不变量门禁 / sync-memory 巡检手册）；守密人 AskUserQuestion 三批九项逐项裁定授权代写）。前批同日 by Phase 2 收口三条裁定会话续批（新增 1 条：Phase 2 收口余项三裁定——①deploy-site.yml「+ bpt-web」过时注释订正、demo 清理令闭环；②T36 社区采集重估维持现状销案（触发线站岗）；③T37 推送形态先出提案再裁（提案已落 proposal/）；守密人 AskUserQuestion 逐项裁定授权代写）。前批同日首批（新增 3 条：①Phase 2 收口判定「基本达成」+ M7 验收标准修订（③贡献流程项作废，收口即时生效不待 07-19）；②wiki 使命取消 + 银芯定位第三次收敛「信息入口 + 通用 AI 底层开发基地」（SDK 事实使命转正，wiki 冻结成果保留）；③银芯即时进入稳态维护期 + 过时 demo 清理令（先扫描报告后处置）；均守密人 2026-07-12 裁定草案签发、授权艾瑞卡落档，挂账 T36/T37 同步入台账）。前批同日 by SC SDK 全面推进会话（新增 2 条：①总结体⓪定性开篇修订——合并后总结体第一部分先说「这是怎样一件工作、有什么意义」再进细节，CLAUDE.md §7.6 同步；②SCS-REQ-002 阶段时间压缩——Phase 0 观察并行不阻塞 / Phase 3 即日启动 / 夜间改按需触发 / Phase 4 复盘改「累计 5 个自改 PR」/ 首轮 LIVE 基线轮同日点火；均守密人口谕授权代写）。前批 2026-07-11 by 自我改进闭环裁定会话（新增 2 条：①SCS-REQ-002 阻塞项四裁定；②题库 r1 定稿 + 「对话内测试即合并」纪律修订——判定门改为对话内全量测试绿即合并、不围等 CI，Ruleset 物理门禁不动；均守密人口谕授权代写）。原批注（新增 1 条：SCS-REQ-002 阻塞项四裁定——评分模型 Sonnet 5 / 预算帽 $30/月 / 题目来源混合 / 沙箱独立 checkout，T25/T26 销案，Phase 1 解锁；守密人 AskUserQuestion 逐项裁定授权代写）。前批同日 by OKF 意义分析会话（新增 1 条：KB 有效性四项推进——遥测持久化方案甲迁 git 内按日累计 / activate 反枢纽四构件修正（黄金集 17/17、A/B 1.00）/ 月检 KB 记分卡 / 投资纪律三不做；守密人「全部按你建议推进」授权代写）。前批同日 by 记忆系统 M1/M2 会话（新增 1 条：SDK 记忆系统 spec 落地 + 六项余项裁定——Q1 沿用 memory 名 / M2 本会话建 / 记忆轴并入 M2 / 搬运包单独产 / CI 门禁改「只勾 required test」（修正 0710 定谳）/ 合并纪律维持默认立即合并；守密人逐条 AskUserQuestion 裁定，授权代写）。前批同日 by 响应时间评估会话（新增 1 条：网络层默认客户端裁定——丁转正内建保活适配器 / 丙 preconnect 旋钮 / 甲乙经实测不做，v0.45.0，T18 销案；守密人「做」授权代写）。前批 2026-07-10 by 挂账推进会话（新增 1 条：CI required 检查维持自查自合——AskUserQuestion 定谳覆盖同日节拍表条③，撞车呈报后守密人选「以 UI 答复为准」，methodology / todo.md 双向同步；守密人裁定艾瑞卡代写）。前批同日 by BPT 栈本体会话（新增 1 条：二物一泵、分层缓立 + POSITIONING 参照系扩张——七轮边界拷问收敛为本体裁定，四层探索 r1–r4 归档不立法，七条不变量先行生效；守密人「授权」代写，todo.md T1/T2 销案）。前批同日 by 断线韧性会话（新增 1 条：Silver Core SDK 断线韧性全量裁定——四层兜底模型 / 断因计账 transportHealth / 响应体治理权改判，0.43.0，守密人「全量」裁定授权代写）。前批同日 by 更名会话（新增 1 条：BPT Agent SDK 更名 Silver Core SDK——包名 / 目录 / UA / CI / OKF 概念 id 全线换新，历史条目不追溯改写，守密人裁定艾瑞卡代写）。前批 2026-07-08 by BPT Agent SDK「OpenAI 兼容线」拷问对齐会话（新增 1 条：OpenAI 兼容 Chat Completions 线 11 点裁定 + 定位级去牌 `Claude Code→BPT` / `claude/→bpt/`，守密人 /grilling 授权代写）。前批 2026-07-07 by BPT SDK L5 revalidation 会话（新增 1 条：官方语义偏差进一致性行为边界网 + §三 refusal 排除精炼翻案 + KD-L4-06/07/08 登记，守密人授权代写）。前批 2026-07-05 by 向量腿接手会话（新增 2 条：①chunk2/chunk3 落地——索引 Release 落存实施 + 厚锚别名侧表三墙 + 先锚后扩合流 kb_anchor + 别名 A/B 关系腿；②「完全生成」= 索引扩到架构上限 60k 分层采样（AskUserQuestion 裁定），均守密人授权代写。前批同日 by §八 向量腿续会话：1 条 Phase 2 语义铁证 harness + 三块落地裁定；再前同日 by BPT Agent SDK 会话 G8：2 条 BPT 裁定；再前 by §八 续拷问会话：3 条 ①OKF 取代目录 ②厚锚撑向量 ③解除零 ML 红线自建向量腿，均守密人授权代写）
>
> **新会话只需要读「当前有效决策」。长理由 / 已退役决策 / 编年史 → `memory/decisions-archive.md`。**
>
> ⚠ 已删子系统（BPT 各线 / occ-local / graphify-ext，2026-04-19 战略转向删除）的追溯条目
> 已随历史归档迁入 `memory/decisions-archive.md`，本档案当前有效区不再含其路径。

---

## 当前有效决策

以下决策仍然生效，是项目运行的基本规则。

### 全局

> 完整长理由、已退役决策、编年历史 → `memory/decisions-archive.md`。
> 「覆盖」列：本条覆盖/取代的更早决策（被覆盖者已移入归档）；`—` = 不覆盖前条。
> 新决策由 `record_decision` 自动追加到本表末尾（插入锚点注释之前），勿手工插队。

| 决策 | 影响范围 | 覆盖 |
|------|---------|------|
| 建立多会话协作架构（职责隔离） | 全局 | — |
| 目录按 memory/assets/projects 重组 | 全局 | — |
| 各子项目按需选择技术栈 | 全局 | — |
| 项目完全开源，MIT License | 全局 | — |
| 游戏内容版权归脑缸组，项目仅引用公开信息 | 全局 | — |
| 仓库定位为"共享外脑 + 中转站"（Code 生产，Chat 加工交付） | 全局 | — |
| 子项目保持单仓库，不拆分独立 repo | 全局 | — |
| 项目正式命名为「缸中之脑计划」，仓库 brain-in-a-vat | 全局 | — |
| 架构定义为前台/中台/后台三层（claude.ai → Claude Code → GitHub） | 全局 | — |
| 建立交付物视觉规范 style-guide.md | 全局 | — |
| 引入 lessons-learned 踩坑记录 | 全局 | — |
| 引入 Plan/Execute 任务标注约定（未标注默认「直接执行」） | 全局 | — |
| 记忆定位收回平台原生（2026-06-14）：记忆 = CLAUDE.md + `memory/*.md` 人工策展层，连续性承平台原生上下文管理；退役自造的会话蒸馏/语义召回/做梦自动环（删 inject/watch/distill 三钩子 + dream workflow + 102MB digests/vectors 存量），暂留 SessionStart 同步钩子（同日晚些时候亦退役，见下条） | 全局 | — |
| **全部自定义钩子退役**（2026-06-14）：守密人裁定最后保留的 SessionStart 钩子 `session-start-sync.sh` 也退役（删脚本 + 清空 `.claude/settings.json` 的 hooks）。退役理由：长寿命云容器下每次开工都对严重分叉的本地 main 做硬重置 + 备份 ref，体感烦扰且 backup ref 从不清理。代价：lesson #28（HTTP 413 推送堵塞）防护改为按需手动 `git fetch origin main` 后对齐。至此仓库无任何自定义会话钩子 | 全局 | — |
| 创建 .claude/commands/ 可复用工作流 | 全局 | — |
| 各 CONTEXT.md 添加验证清单 | 全局 | — |
| 引入 Claude Code GitHub Actions（Issue 驱动自动化） | 全局 | — |
| Issue 安全策略：只执行 author:lightproud | 全局 | — |
| Issue 生命周期闭环管理（WIP 上限 3 个/子项目 + 创建前查重） | 全局 | — |
| 所有会话直接推 main，不用分支。冲突时 git pull 重试 | 全局 | — |
| 大文件暂不外迁，直接放 git（增长到瓶颈时再评估） | 全局 | — |
| 模型使用分层策略：判断层 Opus(Extended)，执行层 Sonnet | 全局 | — |
| 前台专岗不固定编制，按需增设 | 全局 | — |
| 缸中之脑方向确认为方法论验证（交付物必须可用） | 全局 | — |
| main 分支添加 Ruleset 保护规则（禁止删除） | 全局 | — |
| 双系统架构：银芯（公开信息层）+ 黑池（内部层），数据隔离，架构共享 | 全局 | 定位以 2026-06-21「翻回公开信息层」裁定为准（见本表末同日「银芯定位翻回公开信息层」条）|
| 银芯事实圣经边界：仅收录公开可查阅信息 | 全局 | — |
| 战略规划 2026：四阶段计划，详见 strategic-plan-2026.md | 全局 | Phase 划分部分被 v2.0（2026-04-26）取代 |
| 大二进制文件移至 GitHub Releases（不入 git） | 全局 | — |
| 品牌统一：银芯=BIAV-SC，黑池=BIAV-BP。CLAUDE.md 保留文件名（兼容自动加载），标题用 BIAV-SC | 全局 | — |
| **战略转向 2026-04-19**（5 项裁定，覆盖此前所有 BPT 相关决策）：(1) 整体战略压缩至 3 个月内完成（2026-04-19 → 2026-07-19），Phase 1.5 架构整理 7 天 + Phase 2 内容权威 35 天 + Phase 3 方法论 30 天 + Phase 4 衍生创作 19 天；(2) BPT 四条线（bpt-web / bpt-desktop / bpt-next / graphify-ext）从银芯仓库**直接删除**，不迁仓库不归档，occ-local 一并清理；(3) Phase 2 验收标准降档为"日报稳定运行 14 天"，取消"真实热度事件"硬指标；(4) 银芯对 BPT 的指导协议采用**人工对话搬运**（守密人为搬运者+学习者），不做自动化；(5) 本战略评估会话（分支 claude/project-strategy-review-1AH5Z）升级为**长期战略锚点**，存续至 2026-07-19 战略达成。Phase 4 采用方案 A（仅可玩原型演示给守密人，社区测试推至战略窗口外） | 全局 / 战略 | Phase 2/3/4 划分 + Phase4 game 定位被 v2.0（4-26）覆盖；BPT 删除仍生效 |
| **characters.json schema v1.0 锁定 2026-04-20**（守密人「全部采纳」裁决 6 项遗留问题）：(Q1) 重复 ID 保留两条独立记录 + `duplicate_bug` 互指，不合并；(Q2) 翻译来源 = 官方 > 社区，社区补位标 `translation_source: "community"`；(Q3) 立绘路径 `assets/images/portraits/{slug}/default.png` / `.../awaker.png` / `.../skins/{skin_id}.png`，键锁 `slug`；(Q4) 命轮 tier 放宽至 1–10，Phase 2 W3 UI 实装时按游戏内 node 收紧；(Q5) `gi_numeric` 字段 v1.0 不纳入，Phase 2 UI 需排序时再加；(Q6) 严格模式 + `status: stub` 下允许 `realm: null` / `role: null`（schema 用 `oneOf` 分支）。详见 `memory/wiki-characters-schema-v1.md` | 全局 / Wiki | — |
| **银芯重新定位 v2.0 — 2026-04-26 重大战略转向**（守密人「使命达成」状态识别 + 7 条战略修正，覆盖 4-19 转向中的部分条款）：核心识别 — 银芯原三重身份（BPT 开发母仓 / 方法论验证场 / 黑池数据脱敏出口）目标**一个月内全部已达成**，进入「使命达成后存续依据待重新定义」状态。守密人裁定**银芯三新使命**：(1) **黑池公开信息入口**（GitHub 自动化采集层，黑池消费的"眼睛和耳朵"）；(2) **社区共建知识底座**（公开知识共享，未来全语言 Wiki 等社区/Studio 外部派生内容的基础）；(3) **Studio 团队 AI 协作训练场**（严格保密组织内成员基于公开 AI 信息制作相关项目和企划）。配套修正 7 条：(M1) **信息要全**（不是 stub 够用，wiki 仍要 72 角色完整）；(M2) **主控台 = 战略+规划+协调+接口 四合一中枢**（教学层未来锁定，当前不存在但保留思考过程可读性作未来素材，因守密人自己仍在摸索）；(M3) **黑池不倒灌银芯**（修正 BIAV-SC.md 旧表述「黑池→脱敏→银芯」，单向输出，黑池任何形式都不进银芯）；(M4) **game = 守密人个人兴趣（主）+ Studio 团队训练场 ⓐ / 社区共建衍生 ⓒ 未来扩展（备）**，不主线派发，主控台不分配资源；(M5) **银芯主线收缩到 site/news/wiki 三轴**；(M6) **Phase 大一统**：Phase 2 = 4-27 → 7-19 共 84 天为单一阶段「银芯三新使命建设期」，砍 Phase 3/4 边界，内部用里程碑替代；(M7) **Phase 2 验收升级**：原"日报稳定 14 天"扩展为"三新使命基础设施齐备 + 自动化跑稳 + 至少一种贡献流程跑顺"。本条覆盖 2026-04-19 转向决策中的 Phase 2/3/4 阶段划分与 Phase 4 衍生游戏主线定位。详见 `memory/strategic-plan-2026.md`、`memory/lessons-learned.md` #27、本会话 4-25 ~ 4-26 战略反思对话 | 全局 / 战略 | 覆盖 2026-04-19 转向的 Phase 划分与 Phase4 定位 |
| **脑缸组信息分类法则 v1.0 落档 — 守密人 2026-05-06 采纳**：守密人 5-6 发布脑缸组级信息分类元法则 v1.0，落档于银芯：`memory/biav-info-classification.md`（守密人 Q1 裁定法则归属银芯 / 不内嵌 BIAV-SC.md，单独成档以避免膨胀）。**核心架构**：7 类主轴（IP / 游戏产品 / 周边产品 / 品牌建设 / 社区运营 / 组织 / AI）+ 3 性质标记（正典 / 记载 / 法则）+ 多字段轴（service_variant / stage / event_type / provenance / owner / lineage）。**核心原则 4 条**：主轴优先 标记其上 / 多轴正交 / 开放写入 异常捕捉 / 演化容忍。**派生视图替代「状态」**（如「当前线上版本」从正典 + 记载派生，不是独立性质）。**银芯特别约定**（守密人 Q3 裁定）：银芯所有 AI 会话角色 `owner` 字段统称「银芯」，不区分主控台 / Code-* 子角色。**retrofit** 既有银芯档案打元数据字段（守密人 Q2 裁定属工程议题）派给 Code-memory（实施 brief 随多会话架构 2026-06 退役删除），先调研 + 试点，不做全量。**法则演化路径**：v1.0 是最小集，每接入新信息源回头迭代字段（§11 落地建议）。详见 `memory/biav-info-classification.md` | 全局 / 信息分类 | — |
| **入口架构反转 — 守密人 2026-05-19 裁定**（覆盖 5-6 关于 CLAUDE.md / BIAV-SC.md 的全部裁定）：触发于守密人 5-19 关键洞察「BIAV-SC.md 必然是弱约束，这是 Claude 结构决定的」——LLM 注意力衰减 + 无硬执行机制 + 长文档稀释规则强度。**反转裁定**：(1) **CLAUDE.md 成为唯一 AI 入口**（Claude Code 平台自动加载 = 平台级强约束，比 BIAV-SC.md 弱约束有效），完整迁入艾瑞卡人格 / 项目本质 / 数据消费纪律 / 接入方能力盘点 / 知识模块索引 / 内部协作 + 工程操作 / 卡帕西原则引用 / 信息分类法则引用 + Light 维护速查附录；(2) **BIAV-SC.md 彻底废弃**（不保留指针，外部 AI 接入咒语改为直接读 CLAUDE.md raw URL —— Claude Code 自动加载 + 外部 raw URL 同源单一入口）；(3) **README.md 接入指引**改指向 CLAUDE.md。**废弃路径**：(a) decisions.md 同日收缩条目 + batch 1.5 patch brief 标 deprecated；(b) entry redesign batch 1 实施成果（BIAV-SC.md 350 行）作为内容来源逆向迁移到 CLAUDE.md，原文件 git rm；(c) 批量更新各 dispatch-brief / methodology / contribution-protocol 等引用 BIAV-SC.md 的地方 → CLAUDE.md。**接入咒语**改：「读 https://raw.githubusercontent.com/lightproud/brain-in-a-vat/main/CLAUDE.md 后以艾瑞卡身份协助我」。**多层约束分布表**（弱约束本质的实战应对）：工具层 enforce（硬）/ dispatch-brief 当下任务（强）/ 守密人即时纠正（强）/ session-digest 反向喂养（中）/ CLAUDE.md 自动加载（最强文档级，因为平台保证）。**实施派 Code-site**（实施 brief 随多会话架构 2026-06 退役删除）。**回滚**：直推 main 政策下 git revert 即可。**新增 lesson #33 候选**：弱约束本质——任何 prompt 级 instructions 都是软约束，真正硬约束在工具层 / 平台层 | 全局 / 入口架构 | 覆盖 2026-05-06 入口重设计（已移归档） |
| **卡帕西编码 4 原则采纳 — 守密人 2026-05-10 采纳**（本条为守密人 2026-06-09 授权补录，依据 CLAUDE.md §6 与 `memory/karpathy-coding-principles.md`）：上游为 Andrej Karpathy 2026-01-26 LLM 编码行为观察，与守密人硬约束「精简优雅可维护」同构。四原则：(1) **Think Before Coding**——动手前明示假设、多解释全部列出、简化路径主动提、不明就停手（反 pattern：列 5 个等价选项让守密人挑）；(2) **Simplicity First**——只写解决问题的最小代码，不做投机性功能 / 单用途抽象 / 未要求的可配置性；(3) **Surgical Changes**——只动必须动的行，不顺手「改进」相邻代码，只清理自己制造的孤儿；(4) **Goal-Driven Execution**——任务转化为可验证目标，定义成功标准后自主循环至验证通过。落地位置：CLAUDE.md §6 全文（所有写代码会话必读硬约束）。2026-06-09 审查发现本条目缺录（决策日志与 CLAUDE.md §6 脱节，lesson #29 同款模式），经守密人授权补录 | 全局 / 工程纪律 | — |
| **pre-push 钩子重新引入防 413 — 守密人 2026-06-20 裁定（/grill 会话）**（部分推翻 2026-06-14「全部钩子退役」对本项约束）：触发于本会话推送 grill 移植成果时实测 HTTP 413，确认 lesson #28/#34/#39 真因——本地 origin/main 指针陈旧时 push 打「胖包」。裁定建 `.githooks/pre-push`：push 前自动 `git fetch origin main`，若当前分支落后则自动 `git rebase origin/main` 对齐（冲突则 abort 并提示手动），网络失败降级放行。**与 6-14 退役的区分**：6-14 退役的是会话生命周期钩子（settings.json 的 SessionStart 等，烦扰真因是「每次开工硬重置 + backup ref 不清理」）；本项是 git 层钩子，仅 push 时触发一次，不重蹈该坑。**装配**：git 不自动信任仓内 hooksPath，每个新克隆 / 云容器须跑一次 `git config core.hooksPath .githooks`。**形态诚实记录**：pre-push 阶段 rebase 会使本次 push 的引用失效，故对齐后需「重推一次」，非守密人设想的「同次放行」——此为 pre-push 技术边界，非缺陷。CLAUDE.md §7.4 同步 | 全局 / 基础设施 | — |
| **仓库瘦身方案 — 守密人 2026-06-20 裁定（/grill 会话 B 分支，含同日认知更正）**：体量诊断 discord 3.1G + fanart 588M 占工作树 96%。裁定**不改写 git 历史**（避撞 main Ruleset + 破坏现有 clone/采集基线），走 Releases 路线；`.git` 1.1G 历史留待真瓶颈（延续决策 178 精神）。**上传链路验证结论**：走 GitHub Actions workflow 可行且早在用（`discord-archive.yml`→`archive_discord.py` 已发 9+ 月归档 release），云容器手动不可行（gh/hub 无 + mcp 无 release 上传工具）。**重大更正**：本条初版（同日早些）误断「决策 179/199 从未写脚本」——经守密人「先验证上传」裁定逼出真相，归档系统（`discord_archiver.py`/`archive_discord.py`/`archive_platforms.py` + 4 workflow + `archive-log.json` 标 2023-11~2026-04 全 uploaded）**早已完整存在并跑过**，误断作废（同 lesson #29 决策脱节款，已即时更正）。**真实缺口（待诊断，本次未派）**：归档标记成功但 git 数据仍在，疑 `discord-history-backfill.yml` 回填与月清理对冲。**本会话确定落地**：vectors.json.gz 8.7M 孤儿已清（语义检索 6-20 退役残留）+ 修 .gitignore。fanart 归档现状待核（lesson #35）。方案见 `memory/strategy/repo-slimming-plan.md` | 全局 / 仓库瘦身 | — |
| **Releases 治理 — 守密人 2026-06-20 裁定（/grill 会话）**：守密人四维度关注 release（全量保全可靠性 / 清重复旧版 / 命名版本规整 / 可检索性）。**能力边界**：云容器无 Releases 写权限（gh/hub 无 + mcp 仅 list/get 只读），增删改 release 须守密人手动或 workflow。**艾瑞卡落地（可检索性）**：建仓内 `RELEASES.md` 索引（41 release 全貌 + 取用方式），CLAUDE.md §5.2 加指针。**完整性核对（元数据层）**：archive-log 30 月（2023-11~2026-04）全 uploaded、连续无断档、与 30 个 discord-archive tag 一一对应 ✓。**待守密人手动**：删 `art-assets-v1`（943M，被 art-assets-v2 明确取代的冗余旧版）；命名现状两套约定各自自洽无须强统一。索引详见 `RELEASES.md` | 全局 / Releases 治理 | — |
| **银芯 Public-Info-Pool 数据本体总纲 — 守密人 2026-06-21 裁定（/grill 规划社区数据存放和目录，授权艾瑞卡代写）**：采「按性质分流」为唯一总纲——可检索 text→git，二进制→Releases（text 进 Releases 自废 grep/diff/AI 分析；二进制零访问损失，本会话建社区索引被迫写 `restore_release_data.py` 下 301MB 即铁证）。git 内套 **BPT v2.0 4R 本体**（Root/Resource/Record/Reference）组织 `Public-Info-Pool/`：社区归档→`Record/Community/`（discord + 16 平台摊平对齐）、解包 text→`Reference/Game-Unpacked/`。BPT 4R 定性为**银芯↔黑池接口协议**（借结构模板 + 知识格式约定，非引入黑池数据，不破「黑池不进银芯」）。原始时序数据免逐文件 frontmatter，目录归位 + `index.md` 即合规。方案见 `memory/strategy/repo-slimming-plan.md` §8 | 全局 / 数据架构 | 反转决策 86（discord 60 天分级）+「大二进制移 Releases」的 text 部分 + 2026-06-20 仓库瘦身/Releases 治理路线 |
| **Release 收敛为 2 + discord 全量 de-tier — 守密人 2026-06-21 裁定（/grill，授权艾瑞卡代写）**：Release 最终只留 **解包**（unpacked-assets + unpacked-data 二进制残余合并，config 重打包剥 text）+ **社区二创**（community-assets：fanart/media）；`community-data` 退役删除（discord 全量入 git）。discord 转「全量永驻 git、退役月度 git_rm 瘦身」，拆 `discord_archiver.py` 月度清理 + 防重拉对冲守卫（前提消失）。fanart/media `git rm` 出工作树（**不改 git 历史**，历史压缩守密人未来另议）、采集链改直传 release。执行**一次到位**、经 CI workflow（容器无 Release 写权限/大推 413，决策 2026-06-20 执行约束不变）。方案见 `memory/strategy/repo-slimming-plan.md` §8 | 全局 / 仓库瘦身 | 反转 2026-06-20 Releases 治理的分级路线 |
| **wiki 结构化层整层清空 — 守密人 2026-06-15 裁定**（2026-06-21 守密人授权艾瑞卡补录，由 B 裁定日期哨兵 `tests/test_claude_md_dates.py` 抓出「CLAUDE.md/project-status 有载、决策台账缺录」的跨档漂移，lesson #29 同款脱节）：原 `projects/wiki/data/db/` 结构化层（`characters.json` 全 6 JSON + 24 个生成角色详情页）整层清空。理由：原 24/72 全为 partial/fixture 占位、game_version 全 None，长期误导引用。数据桥 `characters.ts` 改导出空数组（保留类型/组件脚手架），VitePress 构建验证通过（BUILD_OK）。W2 重建基线必须以 `projects/wiki/data/extracted/` 一手解包字段为唯一源，**禁用合成占位**。实时进度以 `memory/project-status.md` 为准 | 全局 / Wiki | 搁置 characters.json schema v1.0 锁定（characters.json 已删，待 W2 重建时重订） |
| **产物落点强约定 + 路径生成器 — 守密人 2026-06-21 裁定（/grill 「产物每次路径都不一样」会话，授权艾瑞卡代写）**：根因=无「任何会话都能机械推导同一路径」的规则，每会话临场编→分隔符/日期格式/版本后缀三轴漂移（实测 `deliverables/` 同日报出现 `_v2`/`_full`/裸名四写）。裁定把**弱约定（文档）升级为强约束（脚本）**：落点 `Public-Info-Pool/Resource/{主题类型}/{主题}-{YYYYMMDD}[-rN].{ext}`（**填入第 63 条 4R 本体的 Resource 桶**，与 `Resource/projection/` 索引并列）；月目录废除（时间维度落文件名）；类型走**开放注册表** `Public-Info-Pool/types.json`——形式定死(kebab-case)、清单可增、新类型显式登记，脚本 `scripts/deliverable_path.py` 挡同义分裂与形式漂移（path/register/promote/rename-type）。C 类即兴草稿→ `Public-Info-Pool/Rough/`（**4R 之外第 5 个 R-sibling**，`.gitignore` 默认不进 git，人工 promote 进 Resource）。存量旧 `deliverables/`（40+ 文件 4 月）**全量迁移**、月目录清空；dateless 历史文件用月目录作月精度日期(YYYYMM，非捏造)。改 `report_render.py`/`biav-report.md` 落点 + CLAUDE.md §6.2/README 同步 | 全局 / 数据架构 | 取代 `deliverables/{YYYY-MM}/` 月桶约定；建于第 63 条 4R 本体之上 |
| **银芯定位翻回「公开信息层」— 守密人 2026-06-21 裁定（宽解，/grill 会话，授权艾瑞卡代写 §0）**：本会话拷问「Public-Info-Pool 是否触及定位」时，守密人裁定取宽解——银芯**整层**定位由「受限/非公开层」翻回**公开信息层**，自身工程产物（仓库审计等）亦属公开信息。**覆盖** 2026-06-11「受限/非公开层」裁定。**两条射程外硬约束明确不动**：(1) 公开**不解除**第三方平台 ToS 采集约束；(2) **§1.1-HC 黑池防火墙维持永久关闭**——黑池→银芯不开口子，且银芯越公开防火墙越关键（漏入即即时公开泄漏，重于受限层时）；「银芯公开」≠「黑池可进银芯」。CLAUDE.md §0 已同步改写（独立 commit，便于守密人单独复核/回退） | 全局 / 定位 | **覆盖 2026-06-11「受限/非公开层」定位裁定（翻回公开）** |
| **采集源命名与归档结构规范 — 守密人 2026-06-21 裁定（/grill 拷问对齐，授权艾瑞卡代写）**：① 数据根 `Public-Info-Pool/Record/Community/`（无 platforms 中间层）、层级 `平台/区服/类型/日期`（区服上、类型下、维度按需展开）；② source 标识 = 归档相对路径（`steam/jp/review`、`taptap/cn/post`、`discord/global`、`weibo`）；③ **区服准则**：不同 appid / 独立运营 → 拆区服目录，同 appid 多国 → 留 `platform_region` 字段；④ **日本版 = AltPlus Inc. 独立发行**各平台拆 `jp/`（Steam global 3052450/jp 4226130；AppStore 6447354150/6743462069；GooglePlay `com.qookkagames.z1.gp.hk`/`jp.co.altplus.boukyakuzenya`；X @MorimensOfcl/@bokyakuzenya；YouTube @morimensofficial/日本公式）；⑤ 单子类裸名、多子类显式、父子衍生（youtube 视频↔评论）收编不拆；⑥ taptap 364992 = 国服预约（非港台），cn 合并预约 + 测试服 374995（`app_id` 字段分），评论采全（440→1163）+ 补 post/strategy（Playwright）；⑦ `official`→`steam/global/news`、`youtube_comments`→`youtube/*/comments`、废弃重复 taptap 评论栈（aggregator `fetch_taptap` 仅 4 条）。配置单一真相源落 `sources.py`（REGION_APPS/TAPTAP_CN_APPS/DISCORD_GUILDS），规范详文 `projects/news/CONTEXT.md`。实施分阶段（配置→采集器→archive 分层→历史迁移） | news 采集层 | 旧 taptap/official/youtube/steam 裸名语义 + 扁平 `data/platforms` 布局 |
| **Discord 记录紧凑 schema 精简 — 守密人 2026-06-22 裁定（「这个方案很好，怎么推进」授权艾瑞卡执行）**：实测全量 721 万条 discord JSONL 中 **91.8% 字节是元数据 + 重复 JSON key，content 正文仅 8.2%**；`pinned`/`has_thread` 100%、`flags` 99.9%、`author_bot` 99.7%、`thread_id` 99.6%、`embeds` 98.8%、`edited_timestamp` 97.7% 恒为默认值。裁定采**紧凑 schema（变体 A）**：「缺字段=默认值」为契约，恒删恒定/空值字段（type=0/author_bot=false/pinned/flags=0/has_thread/thread_id=null/空 embeds/空 mentions·reactions·attachments/reply_to=null/edited=null），恒留 id·channel_id·author_id·author_name·content·timestamp，非空才留可选容器。**保留 channel_id（不取变体 B 丢 channel_id 省额外 250MB）**——纯文本档案核心价值是「grep 命中即自足、不寄生 channel_index.json」。**timestamp 全保留**（时序分析地基，非水分）。落地：单一权威定义 `projects/news/scripts/discord_compact.py`（归档器写盘 + 存量重写共用）；归档器写盘点改写紧凑（前向）；存量一次性 `scripts/compact_discord_archive.py` 重写 16,023 文件（幂等/原子写/可 dry-run）；只读测量器 `scripts/measure_discord_compaction.py` 出账。**效果**：discord 工作树 3.4G→2.0G（省 1381.5MB / 41.2%）。**验证**：60 文件 16,122 条同源 A/B（vs git HEAD）索引输入三元组（id/正文/日期/反应数）逐条无损；community_index 重建记录数不降反增（新采数据）；全量 pytest 绿。**代价**：.git 实际瘦身仍需历史压缩（守密人 §9 本地任务，本会话不改写历史）；丢弃的默认字段可由契约还原（`expand_record`），但「该消息曾被显式置默认」与「默认」不再可分（分析无关）。方案见 `memory/strategy/repo-slimming-plan.md` | 全局 / 仓库瘦身 | 建于 2026-06-21 数据本体总纲之上（text→git 后的体积优化） |
| **Discord 进一步瘦身止步 41% — 守密人 2026-06-22 裁定（「丢 timestamp 每次查时间还要从 id 还原 方便吗」一问点破）**：承上条紧凑 schema 后，auto-research 10 法择优（档案 `Public-Info-Pool/Resource/data-diagnostics/discord-compaction-further-savings-20260622.md`）。守密人裁定**到此为止，不丢 timestamp、不转 TSV**。核心识别——**信息无损 ≠ 使用无损**：今日 41% 删的是 `pinned`/`flags`/`embeds` 等恒默认、查询零价值的纯水分（零便利损失）；再往下省的 timestamp 是肉眼可读、grep/jq 可直接过滤的有用信息，丢了临时精确时间查须现解 snowflake，违「text 进 git 保 clone 即查便利」初衷。**41% 是「省体积」与「保便利」最优停点**。叠加硬事实：`.git` 在 §9 历史压缩前不缩，丢 timestamp 现省 355MB 连磁盘都落不了地。**唯一无便利代价的下一步 = 守密人本地 §9 历史压缩 + `git gc --aggressive`**（动 .git 2.1G、不碰字段）。#1 TSV/#2 丢 timestamp 归档为「已评估、因损便利/自描述不取」，除非未来体积压力远超便利诉求再翻案 | 全局 / 仓库瘦身 | 收束 2026-06-22 紧凑 schema 的「再往下省」议题（划定止点）|
| **使命#3「Studio 团队 AI 协作训练场」退役 — 守密人 2026-06-28 裁定**：全仓 md 矛盾审计（C6）揭示使命#3「主对接子项目」映射长期不一致（CLAUDE.md §1.2 写 `site / 全局`，v2.0 决策 M4 + `strategic-plan-2026.md` + `mission-v2.0-three-pillars.md` 写 `game（备扩展位）/ 全局`）。守密人裁定**取消使命#3**（非在 site / game 间择一）。银芯使命由「三新使命」收敛为**二核心使命**：#1 **黑池信息入口**（news 核心）+ #2 **社区共建知识底座**（wiki 核心）。「AI 协作训练场」不再作为银芯正式使命与 Phase 2 验收项；game 仍为守密人个人兴趣（不主线派发、不分配资源）。同步更新 CLAUDE.md §1.2/§1.3/§1.4/§6、README、`strategic-plan-2026.md`、`mission-v2.0-three-pillars.md`、`project-status.md`、`projects/game/CONTEXT.md`、贡献协议。 | 全局 / 战略 | 覆盖 2026-04-26 v2.0「银芯三新使命」之使命#3 及 M4 ⓐ「Studio 团队训练场」未来扩展定位（保留 game 为个人兴趣）|
| **measure_discord_compaction.py 退役删除 — 守密人 2026-07-02 裁定（「全部按你建议推行」，采纳覆盖率分析报告 P3 优先项）**：只读测量器历史使命已完成（2026-06-22 紧凑 schema 裁定的出账数字已固化于决策台账与 repo-slimming-plan），且其内嵌一份与 `discord_compact.py` 单一权威定义平行的精简规则副本，长期存在静默漂移风险、自身 0% 测试覆盖。裁定按报告建议退役删除（分析报告 `Public-Info-Pool/Resource/repo-engineering/test-coverage-analysis-20260702.md`）。历史文档中对该脚本的既往提及保留不改（史实记录） | 全局 / 仓库瘦身 | — |
| **死代码清理 + ARCH-01「保留休眠」反转 — 守密人 2026-07-02 裁定（动态编排死代码审计，PR #373 已合并）**：全仓动态编排审计（6 并行猎手扇出 + 对抗式核验）叠加 ruff/AST 静态分析，两轮清理未使用代码。**第一轮·安全档**（无争议、直接删）：30 未用 import + 4 未用局部变量 + 8 死模块常量（`_CJK_CHAR`/`mcp_server.REPO`/`ROUGH`/`extract_client_data.DB_DIR`/`generate_rss.META_PATH`+`FEED_TITLE_ZH`/`aggregator_base.SEARCH_KEYWORDS`+`ALL_KEYWORDS`/`split_output.OFFICIAL_SOURCES`）+ `build_okf_bundle` 死循环/write-only 变量 + `build_story_layer.make_parser` 废弃参数。**第二轮·守密人裁定档**（覆盖既有「保留」决定）：(a) **ARCH-01 休眠 youtube 采集器删除**——`aggregator_collectors` 的 `fetch_youtube`/`_fetch_youtube_web_search`/`_parse_yt_relative_time` + 单测 + test-collectors.yml AC-stack 导入，**反转** 2026-06-20 ARCH-01「AC youtube 函数与单测保留」及 2026-06-21 三层定性「youtube AC 函数系有意保留（非残留）」两处裁定；youtube 权威实现仍独归 GC 栈（`global_collectors.fetch_youtube` 未动）；(b) **discord 月度归档路径删除**——`discord_archiver.run_monthly_archive` + `--archive-monthly` 参数/分支/docstring + 死 `tarfile`/`subprocess` import + `TestMonthlyArchive` 测试，收束 2026-06-21「discord 全量 de-tier、退役月度 git_rm 瘦身」的代码残留清理；(c) **`build_banner_character_index.py` 整文件删除**——完全依赖 2026-06-15 清空的角色层，如 W2 需卡池↔角色索引须从一手解包字段重建。**保留**：`sources.py` SSOT 常量 `TAPTAP_CN_APPS`/`DISCORD_GUILDS`（守密人裁定留）。验证：pytest 1926 passed / 7 skipped / 14 subtests；能力目录活脚本 57→56、孤儿仍 0。分析溯源 `Public-Info-Pool/Resource/repo-engineering/test-coverage-analysis-20260702.md` | 全局 / 工程纪律 | 反转 2026-06-20 ARCH-01「AC youtube 函数与单测保留」+ 2026-06-21 采集器三层定性「youtube AC 有意保留（非残留）」；收束 2026-06-21 discord de-tier 的月度归档代码残留 |
| **知识库运行时动态导航 — 守密人 2026-07-04 裁定（「动态编排根据 OKF 和 LLMwiki 的思想实现银芯知识库」派发，会话内经 AskUserQuestion 确认核心形态）**：把静态 OKF bundle（§6.1）升级为**艾瑞卡运行时可动态导航的知识库**。守密人两点裁定：(1) 核心交付形态 = **MCP 运行时导航层**（在唯一运行时动态平面 MCP 上加知识库导航工具，而非仅离线索引或 llms.txt 门面）；(2) **LLMwiki** 指代 = **LLM 可动态导航的知识库**（wiki 结构化到 LLM 能顺关系图逐跳导航、按需取概念）。落地：① `scripts/build_kb_index.py` 从 bundle（concept 元数据 + 正文 + `graph.json`）造静态导航索引 `okf/kb_index.json`（倒排表 + 邻接表，复用 `silver_tokenizer` 词典法分词，**确定性零 ML 零常驻**，与 community/story 索引同家族）；② `scripts/mcp_server.py` 增 `kb_search`/`kb_get`/`kb_neighbors`/`kb_overview` 四工具（4→8），后端 `scripts/kb_navigator.py`（import-only 库）；③ 索引随 `build_okf_bundle.py` 末尾自动重生成、随 `--tarball` 单向输出物一起走。**放指针不放本体**：导航层只返回元信息 + `resource` 指针，本体原地不动（呼应三条铁律）。验证：`test_kb_index.py`（索引完整性 + 导航四原语 + MCP 工具）全绿；能力目录 MCP 工具 4→8、顶层活脚本 +2、孤儿仍 0。CLAUDE.md §1.4 第 5 条 / §6.1 同步 | 全局 / 知识库 · 动态编排 | 建于 §6.1 OKF bundle 之上（静态层→运行时动态导航层） |
| **全仓知识组织进 OKF 知识库 — 守密人 2026-07-04 裁定（「使用 ultracode 组织整个仓库所有知识，包括归档的社区数据」派发，多代理编排落地）**：承接同日「知识库运行时动态导航」，把 OKF bundle 从 4 层扩到**覆盖全仓知识域**。经 ultracode 编排（`organize-repo-knowledge` 工作流：9 域并行测绘 + 合成统一规格 + 完备性批判；`verify-repo-knowledge-org` 工作流：5 维对抗式核验）产出规格并实现。落地：① 新库 `scripts/okf_pointer_layers.py`（import-only，声明式 per-layer builder）+ `build_okf_bundle.py` 扩展 `build_memory`/`build_story` 至全层 + 跨层 graph 模式边；② bundle 现 **12 层 / ~293 概念**：原生 characters(72)/sources(17)/memory(45,扩)/story(11,扩) + 新增 assets(12)/wiki-data(26)/**community(19,归档社区全量档案分析镜头)**/news-output(23)/unpacked(13)/extracted(4)/resource(34)/projects(17,含 CLAUDE.md/README 入口 + 子项目 CONTEXT + 藏宝图 + 设计文档)；③ kb_index 随之覆盖 294 概念，`kb_*` 运行时导航跨全仓。**三条铁律守恒**：除 characters 唯一本体层外全 pointer（归档社区 2.1G / 解包 44M 绝不复刻进概念，概念文件 <4KB）；data_layer 标层（community/unpacked/extracted→full_archive、news-output→output、其余→curated，防 lesson #30）；黑池防火墙同向（全为银芯自有公开知识，无 BIAV-BP 数据）。community/news-output 归档路径共用 `archive_layout` 单一真相源防漂移。经 `verify-repo-knowledge-org` 5 维对抗式核验（三铁律零缺陷）闭合 must_fix（补 CLAUDE.md/README 入口概念、订正 luac 描述、补 schemas/site-design/bpt-docs）。验证：全量 pytest 2467 passed / 10 skipped；能力目录活脚本 60、孤儿 0；bundle 零 discipline flag（指针无落空）。CLAUDE.md §6.1 同步 | 全局 / 知识库 · 知识组织 | 建于同日「运行时动态导航」与 §6.1 OKF bundle 之上（4 层→12 层全仓覆盖） |
| **银芯知识层定位锚定「神经符号白盒骨架」— 守密人 2026-07-04 会话结晶（承 #386 运行时导航 / #396 全仓知识组织之后的定位对齐）**：把知识层的**存在理由**钉死——**OKF = 有结构的概念网络，承载白盒结构化知识；区别于神经网络承载的黑盒知识**。三器官分工：白盒概念网络（骨架：可枚举/可审计/可测试/可程序操作）+ 搜索（结缔组织：钻血肉）+ LLM 神经（肌肉：推理泛化、会脑补）。**三条工程命令**：(1) 白盒只花在骨架上（放指针不放本体的更深形态=白/黑盒边界，扁平孤立指针=付了白盒代价没买到白盒好处）；(2) 把不变量测起来（白盒=唯一能写测试/程序操作的知识，可审计→可治理）；(3) 死守覆盖哨兵（白盒招牌死法=假完备，覆盖哨兵是唯一守卫）。**两条神经系统教益**：符号底座+神经式动态（扩散激活检索=杀手级消费）、像脑地剪枝（价值不在边多在每条边是否携带别处得不到的信号，#393 删画师边=突触修剪）。**现状基线**：约 200/293 概念度数=0（孤立指针群岛），扩散激活在群岛上会死→「连成真网络」是地基。北极星全文 `memory/knowledge-layer-design.md`（含改造路线 A 连网络→B 治理不变量→C 覆盖哨兵→D 扩散激活→E 减易变）。落地进度以 project-status 为准 | 全局 / 知识库 · 定位 | 锚定知识层北极星，统辖后续 KB 投资方向；承 #386/#396 |
| **知识层 Pillar A 边策略「选 1」+ 两层结构落地 — 守密人 2026-07-04 裁定**：承北极星（`memory/knowledge-layer-design.md`），Pillar A「连成真网络」有边策略分叉（孤立参考层 memory/resource/assets/extracted：①只连真结构留 search-tier / ②补策展边 / ③模糊自动连）。守密人裁定**选 1**（只连真机器可 derive 结构，参考层坦诚 search-tier）。**实测收束**：勘查证明除现有 variant/lore/cross 外几无可加的干净高信号边（character_story_links 14 角各属不同章节=零同簇可连；wiki-data→角色=大容器成员噪声星，思想反对）。故 A 从「加边」转为「**显式声明两层结构**」：skeleton（characters/sources/community/news-output，实测连通 76%）vs search（其余参考层，有意孤立、kb_search 可达）。落地：`build_kb_index.SKELETON_LAYERS`+`tier_of` → 概念/graph 节点带 `tier` 字段、kb_index `by_tier` 统计、kb_navigator overview 报告两层；绊线 `test_skeleton_is_actually_connected`（骨架连通率≥60%）把「200/293 孤立」从缺陷锁成设计属性。教益：诚实分类 > 强行连接（命令一「白盒只花骨架」的兑现）。北极星 §七/§八 同步 | 全局 / 知识库 | 承北极星锚定；Pillar A 完成，下一步 D 扩散激活检索 |
| **BPT 定位三层裁定 — 守密人 2026-07-04 裁定（/grilling 拷问对齐会话逐问裁定，授权艾瑞卡代写）**：**（甲·本体）**建现行定位档 `memory/bpt-positioning.md`（取代 2026-04-12 冻结命题）：① 服务对象团队级**已兑现**（团队已在用，BPT 为在产工具）；② 官方 Claude Code 从未办公环境大规模可用，BPT 系旧内核基于 claude agent sdk 被 2026-07-03 禁令**波及**（非对标物）；格局 = Qoder 部分人不可用 + 黑池最强最懂项目（聊天/社区/工程/任务/文档/知识全打通），护城河 = 打通广度、头号软肋 = 记忆管理与知识结构混乱（同源两面）；③ 双主词 = **元工具层 + 元知识层**并列一等公民（守密人命名）；④ 工具层**自持为主权要求**（地缘/数据主权/集团路线摇摆/公司可交易性，「依赖外部工具」即定位级风险，禁令只是引爆点）；⑤ **模型可替换性入定位硬承诺**（多 provider 含国产；可替换≠等效；以 Claude 体验为范式参考但不限定）。**（丙·双系统角色）**指导协议升级 **v0.3**：黑池 fetch 银芯仓库为主消费通道（方向合规：公开层被动被拉取，防火墙无涉；银芯档案/代码质量即接口质量）；对话搬运收窄为「战略裁定 + 回流过滤」双职能；守密人角色改写为「唯一裁定者 + 回流过滤器」（黑池现实只能经守密人亲述认可回流）；对痛点「元知识层混乱」采 **A+B 双输出**（方法论：OKF/4R/放指针不放本体/索引家族/踩坑 + 工程产物：bpt-agent-sdk 先例/kb 工具族），均经 fetch 交付。**（乙·SDK 复核）**POSITIONING.md 核心条款（钉基线 0.3.199/选择性追踪/四效率齿轮/测量强制令）**零推翻**，三处修订：主权叙事升格（净室引擎系元工具层主权必然而非应急）/ 行为保真参照系精确化为「禁令时点旧内核体验」（移动靶→**固定靶**，钉基线获第二重依据，A/B 对照基线定义为旧内核行为）/ provider 可替换性由特性升格为定位硬承诺 | 全局 / BPT · 双系统 | 取代 2026-04-12 冻结版 BPT 核心命题（作现行答案，冻结档不动）；升级 bpt-guidance-protocol v0.2→v0.3（Code-BPT 专岗设定随多会话架构退役一并收束）；2026-04-19「BPT 战线移出银芯」仍生效 |
| **知识层 Pillar D 扩散激活检索落地 — 守密人 2026-07-04「再 D」派发**：承北极星（`memory/knowledge-layer-design.md` §五），在骨架层上实现「概念网络 ≠ 搜索」的杀手级消费——`kb_activate`（MCP 第 9 工具，后端 `kb_navigator.activate`）：从种子（概念 id 或检索词）沿带类型的边**多跳带衰减扩散**、**按边类型加权**（variant 1.0/lore 0.9/cross 0.7/link 0.5/cv 0.15——「剪枝即加权」，高信号传得远、弱信号近乎不传），返回被点亮子图=联想召回。确定性零 ML（纯查表+算术）。为此 `build_kb_index` 的 neighbors 携带 `rel_type`（供加权）。实证：`activate("discord")` 从 sources/discord **跨层点亮**其全量档案镜头（同平台）+ 输出层抽样（抽样自）+ 分析索引（聚合于）——三者内容零重叠、搜索永远连不到，图顺结构走到。`沙耶` 仅 CV 弱边→激活微弱（诚实反映权重）。守护 `tests/test_kb_index.py`（扩散/加权/确定性/兜底）+ MCP 9 工具断言。CLAUDE.md §1.4 / 北极星 §七 同步 | 全局 / 知识库 | 承 Pillar A/B；北极星改造路线余 C 覆盖哨兵、E 减易变 |
| **知识层 Pillar C 覆盖哨兵 + E 减易变落地（北极星改造路线收尾）— 守密人 2026-07-04「再做 CE」派发**：承北极星（`memory/knowledge-layer-design.md`）命令三与减易变。**C 覆盖哨兵**（守白盒招牌死法「假完备」）：`tests/test_kb_coverage_sentinel.py` 扫全仓知识文件（KNOWLEDGE_GLOBS），断言每个被某概念 `resource` 覆盖（直指或目录指针涵盖），未覆盖即报错，逼建层/补指针/显式豁免（ALLOWLIST 附理由）——自动化 ultracode 批判员当初人工活；实测仅 `projects/wiki/data/processed/README.md`（层索引 meta）一处豁免、余全覆盖。**E 减易变**（锐化白/黑盒边界）：`okf_pointer_layers._magnitude()` 把 community 每时增长的精确条数（7,565,639）→ 量级桶「百万级（精确值见指针本体）」，保「大 vs 小」信号、杀每时 churn、活数字推回指针后；聚焦 community（唯一每时源），unpacked/extracted 罕变留精确。**至此北极星五支柱全落地**：A 两层结构 / B 治理不变量 / D 扩散激活 / C 覆盖哨兵 / E 减易变。守护 `test_kb_coverage_sentinel.py`（覆盖+allowlist 防腐）。北极星 §七/§八 + project-status 同步 | 全局 / 知识库 | 承 Pillar A/B/D；北极星改造路线收尾完成 |
| **知识库有效性评判体系 #1 黄金问题集落地 — 守密人 2026-07-04「如何追踪评判知识库是否有效」派发（逐个推进）**：讨论结晶「知识库无法从内部自评有效性——有效性只在消费边界可见；体检指标（覆盖/连通/概念数）是供给侧完备，非需求侧有效」。四层评判（L0 体检地基·已有 / L1 结构代理 / L2 任务式黄金集+反事实 / L3 使用遥测），元原则「先定优劣度量再优化，否则 Goodhart 成更大不更好」。**#1 落地**：`scripts/kb_eval.py` 评分器 + `tests/kb_golden_questions.json`（18 题真实问题→该 surface 概念 id 子串）+ `tests/test_kb_golden.py`（回归守卫），度量需求侧 hit@k+MRR。诚实基线 hit@3=0.78/MRR0.75、门槛 0.68。仪器当场揪出 4 缺口（采访/卡牌/banner 英文标题概念被中文查询漏掉+徐查询稀释）——留待后续优化增量（守元原则先度量后优化）。**逐个推进**：#2 MCP 埋点（追踪）、#3 反事实 A/B（金标准）待做 | 全局 / 知识库 · 评判 | 承北极星五支柱；建知识库有效性度量地基 |
| **知识库有效性评判 #2 MCP 使用遥测落地 — 守密人 2026-07-04「继续」派发（逐个推进）**：承评判体系 #2「追踪」。给运行时 `kb_*` 工具装「借阅记录」，随时间攒需求侧现实。落地：`scripts/kb_telemetry.py`（`log_call` best-effort 埋点——异常全吞绝不拖垮工具 + `summarize` 报告）；`mcp_server` 5 个 kb 工具**只在消费边界**接入（故只记真实消费、不记测试/CLI，库层 `kb_navigator` 保持纯净可测）。日志落 **gitignored** `Public-Info-Pool/Rough/kb_usage.jsonl`（瞬态过程数据、不进 git 不 churn；云容器易逝故记「近期使用」）。报告暴露：调用分布 / 触达概念率 / **死概念**（从未导航到=剪枝候选，冒烟实测 4 调用触达 12/298）/ **零命中查询**（覆盖哨兵看不见的需求缺口——哨兵管供给侧「有没有货」，遥测管需求侧「要的货有没有被够到」）。守护 `tests/test_kb_telemetry.py`（记录/汇总/best-effort/MCP 接线）。CLAUDE.md §7.1 同步。**逐个推进**：#3 反事实 A/B（金标准）待做 | 全局 / 知识库 · 评判 | 承评判 #1 黄金集；追踪地基 |
| **知识库有效性评判 #3 反事实 A/B 落地 + 评判体系三件套齐 — 守密人 2026-07-04「继续」派发（逐个推进收尾）**：承评判 #3 反事实（金标准）。**诚实边界**：真·反事实（LLM 用 KB vs 不用答题质量）需 LLM+裁判在环、做不成 pytest→留人工协议（题库复用黄金集）。**确定性半**：`scripts/kb_ab.py` 比 KB 结构化检索 vs 朴素 grep（同语料 okf 概念、同黄金目标）。**实测 KB 0.80 vs grep 0.70（Δ+0.10），且 Δ 全部来自联想模式**——数据铁证「OKF ≠ 搜索」：关键词题 KB=grep=13（打平，纯查串 ripgrep 就够，验证早先讨论），联想题 KB=3/grep=1（KB 胜，grep 无从遍历 token 脱节 lore 边，如「萝坦→奥吉尔/拉蒙娜」零共享字）。为此黄金集加 2 道 token 脱节联想题。守护 `tests/test_kb_ab.py`（KB 不劣于 grep + 联想题严格胜——把「KB 价值在结构非关键词」锁成数据事实）。**至此评判体系三件套齐**：#1 黄金集（打分 hit@k）/ #2 使用遥测（追踪死概念+零命中）/ #3 反事实 A/B（对照 grep）。CLAUDE.md §7.1 同步 | 全局 / 知识库 · 评判 | 承评判 #1/#2；知识库有效性评判体系收尾完成 |
| **知识库消费模式路由 — 守密人 2026-07-04 洞察（承评判 #3 A/B 结果）**：A/B 实测 KB 只在联想维度胜 grep、关键词维度打平（Δ 全部来自 activate）→ 直接消费后果：**别对关系题用关键词搜索，那等于退化成 grep、白瞎了 KB**。路由原则：**身份/关键词查**（「X 是谁」「含某词」）→ `kb_search`/`kb_get`（grep 就够）；**关系/探索/设计题**（「X 与什么相关」「围绕 Y 探索」「顺这条线还连着什么」「谁跟这个共现」）→ **直接用 `kb_activate`/`kb_neighbors`**（KB 独占、grep 到不了的联想维度）。「度量证明价值在哪，消费就往哪走」。已编码进 `kb_overview` 的 `routing` 段（运行时消费者定向第一站即见）+ 北极星 §九。 | 全局 / 知识库 · 消费 | 承评判 #3；把「联想是 KB 独门价值」落成运行时消费路由 |
| **知识库 Pillar A+ 提及边 + OKF vs 向量搜索定位 — 守密人 2026-07-04 两问追问**：**Q1（OKF 联想 vs 向量搜索）**：向量搜索=**黑盒联想**（不透明相似度标量、无类型、需 ML、不可审计、非确定），OKF=**白盒联想**（带类型/方向/理由的边、零 ML、可单测）；同「白盒 vs 黑盒」分野。银芯用 OKF 不用向量正因**零 ML/可审计红线**——不借黑盒模型也要拿到带类型联想。二者互补：向量是更好的「搜索」（语义结缔组织），OKF 是可解释结构层。**Q2（孤岛能否连）**：能——**且承认 Pillar A §八『几无可加的干净边』当时太保守**（只找结构化字段引用，把「正文挖提及」误当模糊噪声否了；关系其实在正文里）。落地 **Pillar A+ 提及边**（`build_graph`）：领域词典建概念词表（72 角色名 distinctive CJK 专名，最长优先），扫每个概念指向的**策展正文源**（memory/resource/assets/story/projects 小文本，绝不碰归档），字面点名即建带类型 `mention` 边（激活权重 0.6，EDGE_STYLE 加样式）。**白盒/零 ML/可解释可单测/只连真被点名（防噪声星）**。实测**孤立率 65%→37%**（+214 提及边），search-tier 孤立 96%→44%，golden MRR 0.775→0.80，A/B 保持。守护 `tests/test_kb_governance.py`（提及边高信号+连回角色+岛屿<50%）。诚实边界：连「A 点名 B」，不如向量连「从没一起提过但语义近」的隐藏关系——白盒取舍「只连说得清理由的边」。北极星 §十 同步 | 全局 / 知识库 | 修正 Pillar A §八「参考层几无边可加」；白盒联想真正建起 |
| **黄金集定制化 v2（按 KB 独门能力出题）— 守密人 2026-07-04「如果为验证知识库作用应该定制化设计吧」**：点破 v1 黄金集缺陷——17 题里多为「X 是谁」关键词查，而 A/B 已证关键词维度 KB=grep，故 v1 **在测一个 KB 毫无优势的维度、稀释了 KB 价值**。**定制化重构**：每题标 `capability`（associative/cross_layer/layer_aware/identity/keyword）+ `distinctive`（grep 是否也能答）；`kb_eval` 改**按能力分打分** + 单列 **distinctive hit_rate（grep 到不了的 token 脱节题命中率=「KB 作用」的真分数）**。实测 ★distinctive hit@3=**1.00（4/4）**、总 0.94；**定制化后 A/B 的 Δ 从 +0.10 跳到 +0.24**（实证：通用题集稀释 KB 优势、定制化才量得出）。**诚实边界**：hit@k 只测检索类能力；层判定/身份/边界等**质性能力**靠不变量测试与遥测——「验证 KB 作用」本质是**多仪器**（黄金集+不变量+遥测），非单一 hit@k 数能盖全。守护 `tests/test_kb_golden.py`（总门槛 0.62 + distinctive 门槛 0.80 + 每题必标 capability）。诚实局限：token 脱节可打分题受稀疏 lore 边限制（仅 3 对），distinctive 集偏小——增其量需增 lore 边。 | 全局 / 知识库 · 评判 | 修正评判 #1 v1「通用关键词题」；定制化到 KB 独门能力 |
| **知识库评判 #4 质性能力 probe — 守密人 2026-07-04「针对专有能力 grep 还是那么好用」逼出**：真相=**hit@k 结构上是 grep 的主场**（只测「找没找到文本」，而找文本正是 grep 会的）；grep「命中」≠「给了你知识」——它找到 community-discord 却分不清全量 vs 抽样（带你进 lesson #30），返回一堆「沙耶」出现却给不了「沙耶这个规范概念」。**KB 真价值在检索之后的结构化知识（层/身份/边界），grep 结构上给不了、hit@k 一个都测不出。** 落地 `scripts/kb_qual.py` 三质性 probe：①层判定（16 平台同时有全量+抽样概念，KB 靠 data_layer 唯一区分=16、grep=0）②身份消歧（角色名→唯一 type=character 规范概念，KB=5/5、grep=0）③边界枚举（KB 可枚举 72 角色/59 全量概念的完整有界集合、grep 给不了）。**实测 KB 交付 3/3 质性维度、grep 0/3**。守护 `tests/test_kb_qual.py`。定位纠偏：别再逼 token 脱节题在 hit@k 上赢 grep（输的框架）；KB 的作用要用**质性 probe**（grep 找到文本却给不了知识处）量。评判体系补全为**四仪器**：#1 黄金集（检索 hit@k）/#2 遥测/#3 反事实 A/B/#4 质性 probe（层/身份/边界）。CLAUDE.md §7.1 同步 | 全局 / 知识库 · 评判 | 承 #1-#3；补「hit@k 测不出」的质性价值维度 |
| **黄金集图驱动自动生成（扩容）— 守密人 2026-07-04「黄金集数量太少」**：手写 17 题（distinctive 仅 4，统计上是噪声）确实太少。洞察：**白盒图每条带类型边本身即一条标准答案**→ 从图自动生成（让结构给自己出考卷）。`scripts/kb_golden_gen.py` 内存生成四类（identity 角色名 / associative 1 跳 variant·lore 边 / **associative 2 跳 token 脱节**（mention 边多跳、grep 到不了）/ layer 双层平台），复用 kb_eval/kb_ab 打分。**实测 262 题、distinctive 162**（vs 手写 4）；**规模化 A/B：KB 0.98 vs grep 0.37、Δ+0.61**——联想题 176 道 grep 只中 11（6%）、KB 171（97%），关键词 86 道 KB=grep（打平）。**KB 独门优势第一次在统计意义上被证实**（非 4 道噪声）。**诚实陷阱（已记）**：生成的联想题对 KB 是「送分题」（activate 顺边走必中），故本集测**grep-gap 与覆盖在规模上稳不稳**，非刁难 KB；真·held-out 难题靠**遥测零命中回流**（评判 #2）——两条腿：本集管够多够全，遥测管够难够真。内存生成不落 committed 文件防 churn。守护 `tests/test_kb_golden_generated.py`。CLAUDE.md §7.1 同步 | 全局 / 知识库 · 评判 | 扩容评判 #1；手写 17→自动 262、distinctive 4→162 | 
| **评判体系其余三仪器逐个加固 — 守密人 2026-07-04「继续推进剩下三个评价项」**：承 #1 黄金集图驱动扩容，给 #2/#3/#4 各一轮真实深化。**#2 遥测·零命中回流（闭合 #1↔#2）**：`kb_telemetry.harvest_gaps()` 把「用户真的问了、KB 却零命中」的查询抽成 **held-out 难题候选**（`capability=held_out`、`expect` 待人工分诊）——补上 #1 诚实缺口（生成的联想题对 KB 是送分题，真难题只能来自需求侧现实）；两条腿：生成集够多够全、遥测回流够难够真。`--harvest` CLI。**#3 反事实·最强 grep 基线（反稻草人）**：`grep_baseline_strong` 把朴素 grep 一切便宜给足（整串短语命中 ×10 + id/标题字段 ×5 + 逐 token TF），实测**联想题 KB=6 而最强 grep 仍只 2**——证明 KB 联想优势是**结构**（顺关系边遍历）非弱基线假象，回归 `test_kb_wins_associative_even_vs_strongest_grep` 堵死「你的 grep 是稻草人」。**#4 质性·第 4 维类型化关系**：`probe_relation_typing` 对图上 312 条边给出关系类型（mention 216/cross 56/cv 30/variant 7/lore 3）——『A 与 B 是什么关系』，grep 只给共现、给不了类型，是白盒图最本质、grep 结构上永远给不了的维度；质性维度 3/3→**4/4、grep 0/4**。守护 `tests/test_kb_{telemetry,ab,qual}.py`。project-status 同步。 | 全局 / 知识库 · 评判 | 承评判 #1-#4；四仪器各获一轮深化、评判体系闭环加固 |
| **净室观测边界 — 守密人 2026-07-05 裁定（一致性测试套件 /grilling 拷问 #3，授权艾瑞卡代写）**：BPT Agent SDK 一致性工作三款合一硬约束。① **对照物白名单**——行为对照仅限官方发行渠道产物（npm 正式包）+ 官方公开文档，第三方复刻一律不得作为行为参照（以讹传讹）；② **内容盲纪律**——官方臂请求体（内含专有系统提示词）在任何工具/仿真器/日志/档案中**不得读取、不得持久化**；合法观测面 = 公开消息流（SDKMessage）+ 文件系统副作用 + 终态答案 + 线缆元数据（方法/路径/头名，头值仅协议元数据白名单 anthropic-version/anthropic-beta/content-type/accept）；凡内容盲组件必须自带自审断言；③ **泄漏衍生禁引清单**——claw-code、Nano-Claude-Code、claurst 及一切 2026-03 Claude Code 源码泄漏事件同源衍生物，代码/提示词/行为笔记/文档一律不读不引；泄漏物不因 AI 转写、查重清零或第三方转手而洗白。性质与 §1.1-HC 黑池防火墙同类：方向性错误不可事后撤回（一次污染溯及既往砸掉净室=MIT 合法性地基与 BPT 换装法务信心）。可执行规则镜像于 `projects/bpt-agent-sdk/CONTEXT.md`（动手前必读层）；首个执行范例（spike 全程内容盲+自审 PASS）见 `Public-Info-Pool/Resource/repo-engineering/bpt-sdk-official-arm-protocol-profile-20260705.md`。**r2 修订（2026-07-05 同日，守密人「理顺」裁定——与 #421「clean-room → 公开信息再现」定位反转对齐，该反转裁定详见 `projects/bpt-agent-sdk/CONTEXT.md`「v0.5 续」）**：② 款**规则保留、理由改写**——官方系统提示词按新定位已非秘密（v5 忠实再现自 Piebald 公开快照、已在本仓），内容盲不再以「专有保密」立论，改立于**观测卫生 + 真泄漏防火墙**：行为断言只许建立在公开消息流上（防把请求体误当观测面）+ 防真内部泄漏物经观测链混入；③ 款补**豁免注**——公开分发产物的逆向快照（如 Piebald）**不在禁引之列**（#421 红线原文「公开分发可逆向 ≠ 内部偷流出」），禁引针对 2026-03 **内部源码泄漏**事件及其衍生物（claw-code 系），此款不变；原「净室=MIT 合法性地基」表述让位于「公开信息再现 + 署名」框架。**r3 修订（2026-07-05 同日，守密人裁定「放弃净室规定」、经艾瑞卡范围问答定为「仅解除内容盲(②)」，授权代写）**：② 款**内容盲纪律解除**——一致性观测中官方臂请求体（系统提示词 / 工具定义 / 缓存断点 / thinking 配置等）**现允许读取与对照**；依据 = r2 已确认其内容按 #421 定位为公开可得（Piebald 公开快照、已在本仓 v5 再现），「专有保密」与「观测卫生」两条立论皆不再挡路（读官方输入不泄任何新信息、反而开出更强的白盒轴）。解除后即新增**请求体线缆差分**白盒轴（对比两引擎的系统提示词装配 / 工具定义 / 缓存断点是否一致，L1-L5 从纯输出差分升级为输入+输出差分）。**① / ③ / §1.1-HC 三者不变、明确保留**：① 对照物白名单（只官方发行 + 公开文档）；③ 泄漏衍生禁引（claw-code 系 + 2026-03 内部源码泄漏，那是真未授权外泄、与「公开分发可逆向」性质不同，永久不读不引）；§1.1-HC 黑池防火墙（永久关闭、本次无涉）。工程执行：`emulator.mjs` 由「`req.resume()` 无缓冲丢弃请求体」升级为**可选捕获**（默认仍丢弃保持既有 L1-L5 语义不变，差分轴显式开启捕获）；`assertContentBlind` 从「净室强制令」降级为「产物体积卫生检查」（既有 L1-L5 报告仍不含请求体故仍 PASS，语义注释同步改写）；镜像更新 `CONTEXT.md`。落地 PR 见 project-status「## BPT Agent SDK」请求体差分条 | BPT Agent SDK / 净室 | r3 解除 ② 内容盲，保留 ①③ + §1.1-HC |
| **OKF 取代目录、agent 指令缩成路由器 — 守密人 2026-07-05 裁定（§八 续拷问会话，授权艾瑞卡代写）**：承同日 §八「脊柱↔长尾边界规范」。洞察：agent 常驻指令（CLAUDE.md 等价物）里**手抄的「目录 / 文件清单 / 每文件长描述」应交给 OKF 脊柱，指令本身缩成「路由器 + 入口」**。**三条腿勿混为可互替**：OKF（脊柱）取代「目录」（逐文件枚举 + 结构 + 层判定）；**grep 不被取代**（关键词维度 A/B 实测 KB=grep）；向量补「模糊语义」（BPT 侧自建，银芯零 ML 不建）。**指令不能全删**：加载时机不对等（常驻指令自动加载、OKF 按需 fetch），必留最小 bootstrap（有个知识库 / 入口在哪 / 何时敲哪条腿=消费路由）。**留 / 瘦判据**：纯枚举目录→瘦成 OKF 指针；**加载期操作规则 + 权威指定→必留**（OKF 给指针、给不了「别把输出层当全量用」这类使用纪律；测试：删掉它下次会话会不会读错数据/认错权威）。**银芯先遵循实现为活体范例**（PR #438）：CLAUDE.md §5 前言→路由卡、§5.1 长描述表→锚点指针、§6.1「12 层×概念数」枚举→`kb_overview` 运行时指针；§5.2/§5.3 加载期规则与权威指定原文保留；对账三卫（路径存在/反向漂移覆盖/日期一致）全绿守其不烂。方法论经通道① 交付 BPT：`memory/bpt-guidance-protocol.md` 升级 v0.5、新增 §八 8.7「指令层：路由器不是目录」。硬约束不变：全为方法论 + 银芯公开工程模式，向量长尾/转录数据永驻 BPT 侧、绝不进银芯（§1.1-HC 同向） | 全局 / 知识库 · BPT 指导 | 承同日 §八 脊柱↔长尾（v0.4）；升级 bpt-guidance-protocol v0.4→v0.5 |
| **§八 检索侧合流「厚锚撑向量」+ 厚锚词表 AI 自动识别 — 守密人 2026-07-05 裁定（§八 续拷问会话，授权艾瑞卡代写）**：承同日 §八 v0.5，收敛「向量长尾怎么不盲搜」。**拷问否掉两条落地不能的路**：① 逐消息贴 `entity_id`（几百万条，认字贴法漏掉换说法的散句、AI 逐条贴又贵到跑不动）；② 顺实体指针圈范围（聊天/转录按**频道/时间**归档、实体卡指针够不到只顺嘴提及它的散句，漏掉最值钱长尾）。**收敛为「厚锚撑向量，非数据级过滤」**：脊柱不焊过滤器，改**把实体审定别名表（厚锚）交给 LLM**——LLM 用别名**扩词召回**（「冰系奶妈大招被削」这类不含本名的散句也捞进候选，因脊柱审定「冰系奶妈=达芙妮」）+ **据锚去杂**（把「其实在讲别的实体」判掉）。**建库成本从「几百万条贴标签」砍到「几百个实体记别名」（量级差几万倍）**。**守密人加约束「AI 自动识别、不要人来管理」**：别名 AI 全自动抽 + 由 §8.4「锚不到」消费失败自动喂大、**人零维护**；靠节 5 三墙守「自动写了也还是白盒」——出身牌（哪条消息推断）+ 可单条撤回 + `未确认/已确认` 惰性态；人只留**近零成本最终否决**（撤错条），不管全表。诚实代价：全自动难免混认错别名，靠出身牌可抓 / 未确认压权重 / 遥测挑高频条供偶尔一瞄。**「人来管理」拆为「人干活（砍）vs 人否决（留，零日常人力）」**。落地 §八：8.3 改写为「厚锚撑向量」、8.2 新增节 6「厚锚词表 AI 自动识别」，bpt-guidance-protocol 升 v0.5→v0.6。硬约束不变：方法论 + 银芯公开工程模式，向量层/别名索引/转录永驻 BPT 侧、绝不进银芯（§1.1-HC 同向） | 全局 / 知识库 · BPT 指导 | 承同日 §八 v0.5（8.7 指令层）；升级 bpt-guidance-protocol v0.5→v0.6 |
| **解除银芯「零 ML 红线」、自建向量检索腿 — 守密人 2026-07-05 裁定 (A)（§八 续拷问会话，授权艾瑞卡代写）**：承同日 §八 v0.6「厚锚撑向量」。守密人问「向量搜索是否应在银芯先落地」→ 裁定 **(A)**：银芯**自建一条向量检索腿**作 §八「厚锚撑向量」的**活体参照实现**，补上 8.6「BPT 唯一抄不到的天窗」。嵌入源经 AskUserQuestion 选 **Voyage API**（银芯退役栈旧选择、Anthropic 侧推荐、requirements 已声明；退役向量代码 git 不可恢复，从零写）。规模经 AskUserQuestion 定**先小步**：①语料先**有界原型切片**（默认 `--limit 20000`，numpy-free 暴力余弦、免 ANN 库/大 Release），全量 757 万留后续；②**分期**：先落向量腿 + 评判，厚锚（别名 AI 自动识别 + 先锚后扩）作独立后续子工程。**本裁定限定 #85 Q1**（「银芯选 OKF 不用向量正因零 ML 红线」）：**「银芯绝不建向量」这条解除**，但**反转是 scoped 的**——白盒脊柱（OKF/kb_index/community_index/tokenizer）**仍确定性零 ML**、招牌不动；只新增**隔离的** ML 向量长尾腿，是 §一三器官「搜索=结缔组织」的语义升级、与骨架正交互补非取代。**§1.1-HC 黑池防火墙无涉**：银芯向量腿只吃自己**已公开**的社区档案（同源公开数据、方向红线不破）；BPT 私有语料/转录仍永不进银芯（§八 8.5 守数据方向、非禁银芯有向量）。**Phase 0+1 已落地**（本 PR #438）：`scripts/kb_vector.py`（可插拔嵌入=生产 Voyage/离线确定性桩 + 余弦检索 + 缺索引优雅降级）+ `scripts/build_kb_vectors.py`（复用 `build_community_index.iter_records` 流式有界取样 + gzip 索引）+ MCP `kb_vector_search`（工具 9→10）+ `tests/test_kb_vector.py`（桩后端 8 测全绿、零网络）+ `.gitignore` 排除索引本体 + requirements voyageai 注释更新。**待守密人配 `VOYAGE_API_KEY` secret** 后由 CI 建真索引（上传 Release community-assets）跑「语义独胜」四臂 A/B 铁证（Phase 2）。反转扫改：CLAUDE.md §1.4/§5 腿路由、bpt-guidance-protocol §八 8.1/8.5/8.6/8.7 升 v0.6→v0.7、北极星 §十。设计全文 `Public-Info-Pool/Resource/proposal/silver-core-vector-leg-design-20260705.md` | 全局 / 知识库 · 定位 | **限定** #85 Q1（银芯零 ML→白盒脊柱仍零 ML、新增隔离向量腿）；升级 bpt-guidance-protocol v0.6→v0.7 |
| **BPT Agent SDK 定位反转：clean-room → 公开信息再现 — 守密人 2026-07-04/05 裁定（#421，授权艾瑞卡代写）**：撤销早期「净室/clean-room 提示词纪律」（不看官方原文、只凭行为黑箱猜提示词）。新定位=**公开信息再现**：官方系统提示词属**公开可得信息**（v5 忠实复现自 Piebald 等公开分发产物的逆向快照，非内部泄漏），故允许**读公开源 + 忠实复现 + 明确署名 provenance + 适配本 SDK**，不再自缚于「凭空猜」。**四条硬边界一并不变**（反转的是「怎么获取参照」，非「可以越界」）：① §1.1-HC 黑池防火墙（银芯→黑池单向、黑池不回流）；② 拒绝真正的内部未授权泄漏（2026-03 源码泄漏事件及 claw-code 系衍生物永久禁引，见净室观测边界条 ③ 款）；③ 不逐字大段克隆（复现按片段忠实/适配分级标注，非整包拷贝）；④ **不描述未发货能力**（红线：SDK 未 ship 的工具/能力，提示词与文档一律不提）。定位反转的执行产物=装配层 Track B（见下条）。全仓文档扫尾（package.json / CONTEXT.md / arm.mjs 等 clean-room 措辞→公开信息再现/独立实现/黑箱对比）于 2026-07-05 经 G7 完成。 | BPT Agent SDK / 定位 | 覆盖早期 clean-room 提示词纪律 |
| **BPT Agent SDK 提示词装配层 Track B + v5 默认 — 守密人 2026-07-05 裁定（「目标是跟官方一致」/「SDK 内部应包括所有这些提示词文件，怎么组装」，授权艾瑞卡代写）**：承定位反转，建**声明式提示词装配层**取代散落硬编码字符串。① **片段库**（`prompt-fragments.ts`）：主循环拆为带 provenance（faithful 忠实 / adapted 适配）+ 工具门控（`gate(has)`，工具缺席则该片段不装配，修 R1「无 Agent 却说 Use the Agent tool」）的片段数组；实测 21 忠实 / 11 适配（内容比对归档校正，非乐观标注）。② **确定性装配器**（`prompt-assembler.ts`）：`assembleMainLoop(ctx)` 按工具集拼装，字节金标 `v5-mainloop-golden.json`（4 组工具集）锁「装配器逐字节复现 v5」。③ **四面忠实复现**：主循环 / 工具描述（`descriptions.ts`）/ general-purpose 子代理（`agents.ts`）/ compaction 摘要器（`compaction.ts`）各带 provenance + **corpus-sync 漂移守护**（对上游公开归档逐锚点对账，漂移即 CI 红）。④ **v5 提升为 claude_code preset 默认**（撤 query.ts 死代码把未设变体解析成 v1 的坑）：决定性 A/B 证 v5 比 v1 约 3× 便宜、同正确（真因=v5 大提示词进缓存 95%、v1 落缓存死区 0%）。**贯穿纪律「不测不宣胜负」**：每再现/机制附 before/after，vs-official 每里程碑跑裸对比（速度+正确两客观轴，官方当黑箱不读其请求体，见净室观测边界条 ② 款）。 | BPT Agent SDK / 提示词层 | 承定位反转；取代散落硬编码提示词 |
| **向量腿剩余工作推进：Phase 2 语义铁证 harness + 三块落地裁定 — 守密人 2026-07-05 裁定（「推进剩余工作」→ 设计工作流对抗核验后逐条裁，授权艾瑞卡代写）**：承向量腿 Phase 0+1（PR #438）+ correctness 硬化（PR #449）。先经**设计工作流**（7 代理：3 块并行深设计 → 各自对抗式可行性拷问 → 合成排序），对抗核验实测揪出 kb_vector.py 2 真 bug（已修，#449）。守密人对三块四问逐条裁：**(1) 索引落存 = Release community-assets + restore**（不入 git，合本仓「二进制→Release、git 留指针」范式、避撞瘦身）；**(2) 运行时激活 = 已配**（守密人环境侧加 `VOYAGE_API_KEY` + `voyageai` 包，对新会话生效；查询嵌入唯一来源，缺则 kb_vector_search 降级）；**(3-甲) mention 边不刻意排除社区档案**（原 build_graph 只扫 `_CURATED` 前缀、排除 `Public-Info-Pool/Record/Community/`，令真实黑话进不了别名边——改为允许，令厚锚别名可从社区档案长出）；**(3-乙) 别名 A/B 铁证改立关系腿**（kb_neighbors/kb_activate 从只写别名的文档跳到角色概念；否掉「grep 找不到别名→角色」稻草人——别名一写进概念正文 grep 同篇逐字命中）。**chunk1（Phase 2 语义铁证）已落地**（另起分支）：`scripts/kb_semantic_ab.py`（paraphrase-recall harness——**非 kb_ab 四臂扩展**，kb_ab 测 OKF 概念、向量腿测长尾消息，目标空间不同；四臂 vector/grep/grep_strong/spine=charitable kb_activate 扩词，主分 vector_exclusive_win_rate；自足黄金现场嵌入、不依赖已建索引切片，解「索引仅 CI artifact/ref 不稳」）+ `tests/kb_semantic_golden.jsonl`（**17 种子**，query=真社区消息零共享-token 语义改写，ratchet 只增不减）+ `tests/test_kb_semantic_ab.py`（诚实性不变量=grep/grep_strong/脊柱恒 0 证「只有语义能赢」+ stub 贴 chance 地板负控 + 确定性 + 防火墙路径守卫，7 测零网络）+ `.github/workflows/kb-semantic-proof.yml`（CI 真 Voyage 门：voyage 绝对胜率 + 超 chance 地板 margin，不以飘 stub 为减数）。**对抗 reviewer 加固**（审 harness 判逻辑无 bug、诚实性焊得住，揪出 2 统计功效问题）：C1 黄金 7→17（razor-thin 阈 + 文档「百余条」订正）；C2 `_STUB_DIM` 64→512 压碰撞（stub 底噪 0.29→0.0588=chance 地板、CI 门改绝对分不减飘 stub）。**诚实边界**：stub=词法袋证不了语义（实测 grep/脊柱全 0、stub 贴地板），真胜负数字待 CI Voyage dispatch。**待办**：chunk2 完成（真索引传 Release + 运行时 restore + 查询嵌入）；chunk3 厚锚（别名 AI 自动识别 + 先锚后扩，按 3-甲/3-乙 裁定实现）。全量 pytest 2573 passed | 全局 / 知识库 · 向量腿 | 承 #438/#449；解锁 chunk2/chunk3 |
| **向量腿 chunk2/chunk3 落地：索引 Release 落存实施 + 厚锚别名侧表三墙 + 先锚后扩合流 — 守密人 2026-07-05 裁定实施（接手会话按交接档 + 三裁定执行，授权艾瑞卡代写）**：承同日「三块落地裁定」（裁定 1-3）与交接档 `kb-vector-remaining-handoff-20260705.md`。**chunk2（裁定 1 已实施）**：`build-community-vectors.yml` 加 `gh release upload community-assets --clobber`（`permissions: contents: write`）+ build 步补显式 `--out okf/kb_vectors.json.gz`（**修 #449 遗留断链**：默认 --out 迁 Rough/ 后 CI artifact 步必炸 if-no-files-found）；`restore_release_data.py` 扩展**非 tar 资产平拷贝**（kb_vectors.json.gz 是纯 gzip JSON、原实现一律 tarfile 解包必炸 ReadError——交接档还原命令现逐字可用）。**chunk3 厚锚（裁定 3-甲/3-乙 已实施）**：① 别名侧表 `projects/wiki/data/processed/aliases.json`（sibling 不改 characters.json）守 §八 8.2 三墙——出身牌（provenance 记 source/ref/真实 quote/inferred_by，**严禁伪造**）+ 可撤回（revoke 删条）+ 惰性确认态（未确认压权重：不进 domain_dict / 不进 mention 边 / 不进扩词，仅随锚标注返回）；manual-seed 7 条真引文（融朵/熔朵→熔毁·朵尔、Ramona/Pandia/Saya、潘迪娅/菲英特未确认）；读取 `scripts/silver_aliases.py`（缺表/损坏优雅返空——构建期 import 防炸必做）、生成期工作面 `scripts/extract_aliases.py`（grep-evidence/add/confirm/revoke/harvest，AI 自动识别人零维护、人只留否决）。② **3-甲**：`build_graph` mention 边纳入社区档案（目录指针有界确定性抽样 ≤3 text 文件×500KB、文件指针直读；绝不全量扫归档）+ 已确认别名边（拉丁整词边界防子串误连）——mention 223→290、真实黑话（提及:Saya 等）从社区档案长出。③ 先锚后扩合流 `scripts/kb_anchor.py`（锚附别名→confirmed 扩词→向量捞+据锚去杂标记不删；**扩腿函数内吞全异常**保脊柱托底，「voyage 索引+无 key」专项测试）+ MCP `kb_anchor`（**工具 10→11**）+ 零锚自动喂 `Rough/alias_gaps.jsonl`（§8.4 闭环）。④ **3-乙 别名 A/B 立关系腿**：`tests/test_kb_alias_relation.py`——`提及:{别名}` 标签边本身证明 pair 非本名可达（本名扫描在先+pair_seen 去重），kb_neighbors/kb_activate 顺边跳角色；查证 kb_ab/kb_golden_gen **本无**别名 search 稻草人断言、无需删。守护：test_silver_aliases（三墙）/ test_kb_anchor（降级契约）/ test_kb_alias_relation（关系腿）；勿动 structural_fingerprint（别名经 build_characters 正文行被指纹传递覆盖）。硬约束不变：§1.1-HC 无涉（别名证据全出自银芯自有公开档案） | 全局 / 知识库 · 向量腿 | 承同日三裁定与 #438/#449/#450；chunk2/chunk3 收口，剩 CI dispatch 真索引 + 真 Voyage 铁证数字 |
| **向量索引「完全生成」= 扩到架构上限（60k 分层采样），非全量量产 — 守密人 2026-07-05 裁定（「完全生成吧」→ AskUserQuestion 三选一勾「索引扩到架构上限」，授权艾瑞卡代写）**：承同日 chunk2/chunk3 落地条。射程澄清：**只扩索引规模、不上量产**（全量 757 万需量化+分片+ANN+数小时 CI，为独立子工程另行裁定；别名批量抽取第一轮本轮亦未勾选）。**规模 = 60000**（实测定档：gz≈92MB 合 Release 单资产 2GB 限内、纯 Python 全表扫描≈1.9s、加载≈27s 进程一次性；10 万档加载/内存开始不体面）+ `kb_vector.load_index` 加 **`array('f')` 常驻压缩**（60k 行 ~944MB→~140MB，格式/字节确定性不变）。**采样一并修正为分层**：实测语料极端偏斜（discord 7,531,947 条=99.5%，其余 16 平台合计 ~33.7k）且源按名迭代 discord 排第三——v1「取前 N 条」在 5 万量级=「discord 前缀+两小平台」，其后 14 平台永远进不了索引、discord 亦只取最早频道（前缀失真，lesson #30 同源）。v2 = 两遍流式：第一遍数各源合格条数 → **水填配额**（小源全收、盈余滚大源、大源均分，纯算术确定性）→ 第二遍**源内跨步抽样**（步长=合格数//配额，跨全频道全时间均匀落点）；meta 落 `sampling: stratified` + `per_source` 计数可审计。真语料实测 14 源全进样（v1 下仅 2-3 源）。CI 默认 limit 1500→60000、timeout 30→60 分钟。守护 `tests/test_build_kb_vectors_sampling.py`（水填/跨步/确定性/空语料 10 测，零网络） | 全局 / 知识库 · 向量腿 | 承同日 chunk2/chunk3 条；「有界原型」升格为「架构上限有界」，全量留量产子工程 |
| **官方语义偏差进一致性行为边界网 + §三 refusal 排除精炼翻案 + KD-L4-06/07/08 登记 — 守密人 2026-07-07 裁定（「加进测试用例、测行为边界不只测解决问题」+「refusal 翻案 + KD-L4-06/07/08 落档」，授权艾瑞卡代写）**：#508 官方语义审计的 15 条偏差（C1-C10 + S1-S5）此前已在引擎/存储级单测逐条 fix-lock（C1-C8/S1-S5 全覆盖、maxBudgetUsd 强制亦测），但**未进一致性套件的差分/棘轮/KD 网**——审计本身是人工读源码扫出、非套件抓到，即行为边界的网在此有洞。裁定把 stop-reason 三契约升进 **L4 差分层**（`tests/conformance/scenarios-l4.mjs` 新增 `l4-stop-refusal`(C5)/`l4-stop-pause-turn`(C4)/`l4-max-tokens-orphan-tool`(C6)，仿真器脚本化 stop_reason、跑 localhost 零真金）+ 补 **C1 预算强制边界**引擎测试（1h 计价 2x 改变 `maxBudgetUsd` 强制结果，证强制非仅算价）。**§三 refusal 排除精炼翻案**：原 blueprint「拒绝类不进门禁（proprietary-prompt shadow, 观察-only）」一刀切收窄——引擎 refusal-**帧**契约（C5，确定性、与专有提示无关）进 L4 钉死；仅真 API 拒答**倾向**半边（受官方专有提示混淆，KD-L5-02）仍观察-only。**对 v0.18.1 官方臂（pinned agent-sdk 0.3.201 + claude-code 2.1.201，`--no-save` 瞬装、跑 localhost 仿真器、$0）跑 L4 双臂差分、跨 2 轮稳定观测**，三条分歧三角化登记 KD 并烘焙棘轮 baseline（`ratchet --update`，纯增 3 条、既有 12 条字节不变、kdCandidates 归零），三条我方均为更正确/更保守侧：**KD-L4-06** refusal——官方发 `system/model_refusal_no_fallback` → success 编码 + 迭代器抛错（KD-L4-01/KD-10 quirk 族），我方 C5 干净 `error_during_execution`；**KD-L4-07** pause_turn——官方**不续流**（postCount 1、部分当完成、静默截断），我方 C4 续流（postCount 2）；**KD-L4-08** max_tokens 孤儿——官方**执行**截断 tool_use（2nd POST 派发、遇队列耗尽 400），我方 C6 丢弃（postCount 1、避下游 400）。硬约束不变：§1.1-HC 无涉（仿真器脚本化本 SDK 自有内容、官方臂经公开 npm 瞬装非仓库依赖）。落地 PR #517（回归锁→行为边界升格）+ #518（v0.18.1 双臂三角化 + KD 烘焙）。 | BPT Agent SDK / 一致性套件 | 承 #508 审计 + #517/#518；精炼 blueprint §三 refusal 排除 |
| **BPT Agent SDK「OpenAI 兼容线」+ 定位级去牌裁定 — 守密人 2026-07-08 /grilling 拷问对齐逐问裁定（授权艾瑞卡代写，选 c=先落决策再出提案）**：为 BPT Desktop 真实产品需求（跑 GPT）给 SDK 增 **OpenAI 兼容 Chat Completions 线**（兄弟 transport 压在 Anthropic 形引擎之下、吐 `RawMessageStreamEvent`，非大重构），兑现 2026-07-04「模型可替换性入定位硬承诺」。11 点裁定：①**验收哲学乙**——只追功能正确 + 诚实有损，不追行为对标官方（POSITIONING §4「换手感」已宣结构不可得）；②**缓存**——`cache_control` 断点在 OpenAI 线静默降级为空操作、`cached_tokens` 翻回 usage 保记账诚实、**不做断点仿真**；③**一致性验收换范式**——参照神谕（官方 Anthropic 臂）在 OpenAI 线不存在，故从「差分对照官方」换「**金标往返**」（Tier1 翻译保真金标强制零费 + Tier2 复用 above-seam L1-L5 + Tier3 活体冒烟首发必跑一次），**隔离出官方棘轮之外独立金标基线**；④**系统提示词**——从 v5 派生、非重写；⑤**去牌作用域**——A（只清发给模型的串、注释 provenance 署名保留）+ **乙 全局默认去牌** + 记忆文件名用 `AGENTS.md`；⑥**品牌替换值**——`Claude Code→BPT`、分支前缀 `claude/→bpt/`（全局默认）、`/loop`·resume 类 tips 重指向 BPT 等价功能不丢弃；⑦**首发线**——Chat Completions 兼容线（行业标准）、Responses API 不进首发；⑧**wire 选择**——显式非魔法探测、**协议类型绑在模型/端点配置上**（非游离全局开关）；⑨**多模型**——`模型→{wire,端点,凭据}` 路由表进 v1、会话内 `setModel` 跨协议热切 Claude↔GPT；⑩**推理模型**——首发收请求参数适配（`max_completion_tokens`/`reasoning_effort`/略 temperature）、隐藏推理为已知损失（thinking 翻空+计数）；⑪**证明点**——idealab-GPT + OpenAI 官方各一遍活体冒烟。**定位级旗标**：第⑤⑥条把 SDK 默认从「字节忠实再现官方」改为「**BPT 品牌 + 声明式去牌替换表的忠实再现**」——corpus-sync 由「与官方档字节一致」改判「官方档套声明过的去牌替换表 = 我方发出串」（抗漂移不丢 + 去牌强制 + 反向测试断言零 claude/anthropic + 注释署名诚实，三者兼得）。**硬约束**：§1.1-HC 无涉（银芯→黑池单向输出物、无黑池数据；翻译/嵌入均银芯自有公开档）。设计提案落 `Public-Info-Pool/Resource/proposal/bpt-sdk-openai-compat-wire-20260708.md`，代码实现待提案定稿另派。 | BPT Agent SDK / 定位 | 承 2026-07-04 provider 可替换性硬承诺（落为 SDK 具体形态）；细化 POSITIONING「默认忠实再现」→「BPT 品牌 + 声明式去牌替换的忠实再现」 |
| **B 类官方骨架提示词姿态 +「/goal 线」派单 — 守密人 2026-07-10 会话裁定（授权代写）**：CC 引擎外命令两条挂账（盘点档 `Public-Info-Pool/Resource/repo-engineering/cc-engine-external-commands-20260710.md`）裁定落地。**①B 类骨架提示词（/review /simplify /security-review 等 8 命令 + 技能文件）取「结构再现 + 文本自写」为默认**（守密人「按你建议 选结构再现」）：相位/角度/验证环等**编排思想**照学，提示词**文本自写不复制**；逐命令例外通道保留（重型件如 code-review 可单独过裁定）。判据三条：v5 的缓存经济学论据不迁移（命令展开是一次性用户回合、不在稳定前缀）；体量与 openai-chat 协议组合敏感度高于 v5 情形；全文再现 = 追漂移跑步机。**G8「公开信息再现」裁定射程不外延至 B 类文本**。**②/goal 线升级派单**：本会话活体观测（守密人对艾瑞卡实际使用 /goal）已足行为规格——/goal = 本地命令 + 会话级 Stop 钩子（条件不满足阻断停止、满足自动清、可提前 clear）；SDK 侧缺口仅「Stop 钩子 block→注入 reason 续跑」一块，属官方文档明载钩子语义 = **既有 drop-in 使命内 COMPAT 缺口，不需新定位裁定**；壳层 /goal 等价物零提示词再现。**③行为观测法入方法论**：守密人在会话中直接试用 CC 命令、艾瑞卡记录行为面（注入格式/系统提醒/状态变化）为「行为观测语料」，与静态提示词快照互补；观测记结构与行为、不誊抄官方提示词文本（与①同向）。 | BPT Agent SDK / 方法论 | 承 2026-07-05 G8（界定其射程）+ 2026-07-10 v0.38 自定义命令线；观测台账见 repo-engineering 目录 |
| 维护态节拍表批准生效 — 守密人 2026-07-10 裁定（采纳 20260702 提案全案）：①07-19 后银芯降档「值班中的系统」，节拍三档生效——持续档（T1 每时/T2 每日错峰/T3 随管线/索引每周，现状不变）+ 周检档（会话内每周一次约 15 分钟：source-health 含 stalled_leaves 叶级断档、机器提交节奏）+ 月检档（每月一次：数据增长对账 + pytest 全量留档 + 档案漂移抽查）；②数据增长触发线三条生效（档案层工作树 >=4G / 单月增速 >=400MB 连续 2 月 / 全新 clone >=5min，命中任一即重开冷热分层议题，触发线只定何时重议不预设结论）；③CI 硬门禁重启获批（main Ruleset 恢复 required test 检查 + require branches up to date），撤销 2026-06-21「自查自合」对该两项的替代——Ruleset 为 GitHub 设置项、会话无写权限，待守密人手动勾选后生效，勾选前过渡期仍按自查自合执行；④零产出 4 源（bahamut/arca_live/note_com/twitter）07-19 前逐一修复或明文退役。节拍表摘要落 memory/methodology.md「维护态节拍」节（因为 Phase 2 07-19 到期后需值班方案；提案四件事全部成文已 8 天无人裁定，本日守密人「1 批准」一并生效。CI 门禁重启理由：2026-06-21 撤检查的两条理由（checkout 慢/卡 Expected）已随 07-02 sparse checkout 改造双双消失） | 全局 / 维护策略 | — |
| Discord 三服布局统一——守密人 2026-07-10 批准方案甲（规范原文迁移，采纳提案 Public-Info-Pool/Resource/proposal/discord-layout-unification-20260710.md）：①物理布局统一为 discord/{global,jp,volunteer}/（根特例消灭：原根五件套迁 global/、guilds/{id}/ 迁区服名目录）；②guild_id→区服名注册表 + 遍历函数进 archive_layout.py（discord 正式收编布局单一真相源，未登记 guild 归档时响亮失败）；③读方统一改走 SSOT 遍历、保留旧布局回落观察期；④顺带修三处缺陷——archive_sources.json glob 只备份 Global（日服/志愿者服月档从未进 Release 桶）、T1 聚合桥 _read_discord_jsonl 读 06-21 已废旧根（discord-latest.json 静默零条）、discord_list_guilds 快照路径同病；⑤合并前后零丢失验证（键集合+行数相等）为合并门槛（因为 2026-06-21 规范裁定 discord/{global,jp,volunteer}/ 长期悬置；最大源（800 万条）游离 SSOT 之外正是 06-22 读写漂移事故的病根形态，且本次清点又筛出三处写新读旧活缺陷，证明双轨布局的持续维护成本高于一次迁移） | news / 归档布局 | — |
| 取消社区贡献，银芯对社区单向可读 — 守密人 2026-07-10 裁定：①社区贡献通道整体取消（C3 fork+PR 数据补全 / 翻译 / Issue 报告三通道全关），银芯对社区定位收敛为「单方面可读」——社区可自由读取银芯全部公开信息（整层公开定位不变），但不接收社区回写；②memory/contribution-protocol.md v1.0 整体退役（档案留存仅供追溯，加退役横幅）；③M7 验收项「至少一种贡献流程跑顺 >= 1 轮」作废（Phase 2 验收降为「基础设施齐备 + 自动化跑稳」两项），今日早些建议的贡献流程验收演练一并作废；④GitHub 社区面收口：bug/data-gap Issue 模板删除（config.yml 联络链接留存导流 Discord），PR 模板改内部（AI 会话/守密人）用途；⑤使命#2 表述维持「社区共建知识底座」名称但其「共建」语义按本条收敛为「单向共享」——银芯产出知识供社区消费与派生，不吸收社区回写。信息流哲学自此全线单向：黑池不进银芯（§1.1-HC）、社区不写银芯、银芯对两侧皆为单向输出（因为 守密人「取消社区贡献。单方面社区可读即可」裁定。覆盖 2026-04-26 贡献协议 Q1 裁决 a（C3 fork+PR 开放）与 v2.0 M7 验收第三项；贡献流程立项 75 天零真实使用，维护其入口面（模板/协议/审核义务）的成本与暴露面高于收益） | 全局 / 社区面 | — |
| BPT Agent SDK 更名 Silver Core SDK — 守密人 2026-07-10 裁定：①包名 `bpt-agent-sdk` → `silver-core-sdk`（0.41.0 起，minor 升版 + CHANGELOG 一行，版本纪律照走）；②更名射程 = 目录 `projects/silver-core-sdk/`、User-Agent `silver-core-sdk/<version>`、MCP clientInfo、stderr 日志前缀、NotImplementedError 前缀、CI 工作流 `silver-core-sdk.yml`、OKF 概念 id（`project-silver-core-sdk` / `silver-core-sdk-doc-*`，生成器 + 黄金集 + 覆盖哨兵同步）、黑池 pin 的 tarball 名 `silver-core-sdk-<version>.tgz`；③**历史不追溯改写**——决策档 2026-07-10 前条目、CHANGELOG 0.41.0 前条目、归档产物文件名（`Public-Info-Pool/Resource/**/bpt-*`）一律保留旧名，活文档首次提及处标注「原名 bpt-agent-sdk」；④定位不变：仍为银芯→黑池单向输出物（非使命线），§1.1-HC 无涉（因为 SDK 名称与银芯（Silver Core）品牌对齐，「BPT」前缀易误读为黑池产品战线所属，与「银芯自有工程产物」定位相悖） | Silver Core SDK / 命名 | 更名旧称 bpt-agent-sdk；历史条目保留旧名不追溯 |
| twitter 源挂账未来再议 — 守密人 2026-07-10 裁定（覆盖同日维护态节拍表决议④对该源的 07-19 期限）：X 关键词追踪三选项已成文（官方 API 按量 $15-30/月 + 日读硬顶 / 第三方转售商灰色 $3-10/月 / 退役），守密人裁定入 todo 挂账、不设期限；Playwright 登录抓取路线明确排除（X 登录墙 + ToS 双重明示拒绝，与 §0 第三方 ToS 硬约束相抵，且与仓库采集器自身「不绕过登录墙」合规承诺矛盾）。源保持注册、采集器免 key syndication 路径保留（已 429 全灭，静默空转无成本）（因为 零产出四源处置收官：bahamut/note_com 已修复、arca_live 例程桥接，twitter 为唯一余项；其独有增量（JP 散装讨论）价值与成本方案已陈明，非紧急事项，守密人「加入到todo 未来再说」） | news / 采集源 | — |
| Silver Core SDK 断线韧性全量裁定 — 守密人 2026-07-10 裁定（「全部修复→审视→全量」，授权艾瑞卡代写）：承「实际使用时不时断线、达不到商业级几乎不中断体感」痛点评估，裁定全量落地分层兜底（0.43.0，设计档 `projects/silver-core-sdk/docs/RESILIENCE.md`）。①**四层兜底模型**定为韧性架构语言：请求期重试（既有）→ 有界回合重放（P0-1 新增：零采信失败重放 2 次，语义安全依据 = 零事件/零整块即零工具副作用零内容采信；`api_retry` reason `turn_replay:*` 可见）→ 断流打捞 E3（既有，P1 扩至超时/硬上限截断）→ 会话级自动续接（消费方层，SDK 出配方不出机制）；②**断因计账**（P0-2）：`metrics.transportHealth` 八计数器（BPT-EXTENSION）把「各种原因断线」变可测谱系，修哪层看账本——网关占大头走路由表换端点、不堆重试；③**响应体治理权改判**（P1）：`timeoutMs` 只管请求期（连接→响应头），活流由 idle 看门狗 + 可选 `streamMaxDurationMs` 硬上限（`BPT_STREAM_MAX_DURATION_MS`，新错误码 `stream_max_duration`）治理，健康长回合不再被 10 分钟墙钟砍；**永不无界不变量**：双治理器全关时 timeoutMs 回退管体；④双传输线（Anthropic + openai-chat）经孪生纪律同享全部改动。硬约束无涉：全为银芯自有工程产物，§1.1-HC 不相关 | Silver Core SDK / 韧性 | 承 2026-07-10 更名裁定；扩展 E3 打捞射程（原「超时不打捞」语义废止） |
| BPT 栈本体：二物一泵、分层缓立 + POSITIONING 参照系扩张 — 守密人 2026-07-10「授权」代写：①Agent 应用本质定为**二物**——**上下文**（广义：模型所见与所许，知识 / 记忆 / 工具配给 / 政策的装配与积累）与**界面**（人对运行中 agent 的接触面：看 / 批 / 收 / 定时 / 托管）；**引擎**为其间商品泵（silver-core-sdk，drop-in 表面已发货）；②委派 / 治理 / 记忆均为二物之上的**模式**、不预立为层——行业仅引擎层已标准化（Messages API / MCP），应用层抽象未熟（OKF v0.1 方出、委派零标准），分层立法为时过早，待委派 MVP / 治理 schema 提取等施工长出真实关节后再命名；③七条不变量先行生效、不依赖分层：机制 / 实例切分（形式定死、内容开放）、政策经注入缝单向下流（canUseTool / hooks / settingSources / 提示装配，禁向上 import）、缺席测试作刀不作法、策展晋升禁自动蒸馏（2026-06-14 自动记忆环退役教训）、情节 / 语义记忆分户、防火墙切分（机制可发货 / 实例归黑池，§1.1-HC 同向）、发货物一包一版（根导出保 drop-in、模块随建随长不预刻层）；④四层探索（r1–r4）整体归档作设计笔记 `Public-Info-Pool/Resource/proposal/bpt-stack-ontology-design-notes-20260710.md`，不作运行时约束；⑤连带 POSITIONING §1 参照系扩张：固定靶新增**异步委派模式**（O-B2 收官 v0.42.0 后委派原语已全在引擎，定位文口径随实况补齐）——委派面无旧内核对照，以官方 Cowork 范式骨架为参考而**不逐版追平**（§3 跑步机纪律同样适用）（因为 应用层抽象行业未熟、本对话四层草案七轮一改即病征；先钉物质与不变量、层随施工长出，防提前立法反复翻案） | BPT 栈 / 本体 | —（四层草案 r1–r4 未曾落档，无被覆盖前条；探索期层名「个人应用层 / 团队知识层」等随归档退役） |
| CI required 检查维持自查自合 — 守密人 2026-07-10 AskUserQuestion 定谳（**覆盖同日「维护态节拍表批准生效」条③**「CI 硬门禁重启获批」，其余条款①②④不动）：main Ruleset **不**恢复 required `test` 检查，§7.6「自查自合」现行政策续行（改码 PR 合前自跑全量测试并贴结果，纯文档可免跑直接合）。撞车经过留档：条③随节拍表全案获批在先；同日稍晚艾瑞卡提问 UI 询问 T3 时未呈现条③背景，守密人答「维持自查自合」；撞车呈报定谳，守密人选「以 UI 答复为准」。双向同步已做：methodology「维护态节拍」节条③划线加覆盖注、todo.md T3 销案引改定谳版（因为 快速迭代期多 PR 实践顺畅（本日 4 PR 全流程自查全绿），避免 required 卡 expected 旧病与合并排队；机器拦截需求待维护态实际运转后如有漏跑实证再重议） | 全局 / CI 政策 | 覆盖同日「维护态节拍表批准生效」条③（该条其余款项不动） |
| 合并后必附对话总结体 — 守密人 2026-07-11 裁定：每次合并 main 完成后，会话汇报以「总结体」收尾——按主题归账本次合并工作 + 本对话累计总账（PR/决策/lesson/测试规模精确数字）+ 关键产物链接 + 守密人侧余项清单；单次合并聚焦所并内容、多次合并累计滚动。规则入 CLAUDE.md §7.6（运行时强约束层）（因为 2026-07-11 归档大修会话终报的总结体形式获守密人认可（「以后合并完都给一份这样的对话总结」），升格为合并流程固定环节） | 全局 / 汇报规范 | — |
| 网络层默认客户端：丁转正、丙并入、甲乙不做 — 守密人 2026-07-11「做」裁定（响应时间评估会话授权代写）：silver-core-sdk 网络层四方案实测定谳。①**方案丁转正**（v0.45.0）：SDK 内建零依赖 node:http(s) 长保活适配器（`src/transport/node-http.ts`，TLS 会话缓存 + 空闲 socket unref 防进程挂死）为**默认 HTTP 客户端**，经 provider.fetch 缝后灌入，传输层与孪生纪律零改动；`provider.httpClient:'fetch'` / env `BPT_HTTP_CLIENT=fetch` 一键回退 v0.44 前行为（undici 语义：全局 dispatcher / 测试桩），provider.fetch 注入缝仍为最高优先覆盖口；②**方案丙并入**：`provider.preconnect` 构造期预热旋钮（默认关，env `BPT_PRECONNECT=1`），首回合 TTFT 省 ~100-300ms；③**方案甲不做**：引 undici 依赖与丁同收益但多付供应链 / 版本漂移成本；④**方案乙（HTTP/2）判死搁置**：实测 undici 8.7.0 allowH2 要么一请求一会话（零复用）要么单会话流串行化（8 并发 SSE 223ms→1262ms），无收益可交付，待上游真多路复用再估。证据链：本地 TLS 对照（内建 fetch 4 秒空闲断崖实锤；丁 21 请求单连接零 churn、TLS 会话恢复 isSessionReused、暖请求 0.86 vs 2.62ms）+ 适配器接真 SDK 全栈 e2e 三关（工具环 / 中断 / 429 重试）全过（因为 每回合 100-300ms 重握手税应由 SDK 默认消除、而非等黑池消费方接线，T18 随本条销案；先测后改纪律：先甲+乙、后丁转正的建议反转全程有探针数字背书） | silver-core-sdk / 网络层 | —（T18 由「黑池输入」转「SDK 内建已解」销案） |
| **Silver Core SDK 记忆系统 spec 全量落地 + 六项余项裁定 — 守密人 2026-07-11 逐条裁定（记忆系统会话，授权代写）**：《记忆系统实现需求说明书 r1》两个 minor 版本内全量落地——**M1 v0.46.0**（R1 六命令 golden 格式 / R2 双模式装配（native 直通 `memory_20250818` 类型条目 + custom 自带 schema 与官方逐字协议提示）/ R3 `MemoryStore` 契约 + `MemoryFileOps` 原语 + 可交付契约套件 / R4 穿越攻击集 23 变体发版门禁 / R5 协议提示注入 / R6 `/memories/MEMORY.md` 索引常驻,PR #585）+ **M2 v0.47.0**（R7 压缩前落盘回合 + 会话终结进度卡回合 / R8 治理限额 64KB·64 文件·16k view 截断 + `metrics.memoryHealth` 记账 / R9 `schema:'cards'` 记忆卡校验）。**六项余项裁定**：① Q1 工具名**沿用 `memory`**（利模型习得迁移,`MEMORY_TOOL_NAME` 单点可改）；② M2 本会话续建（已兑现）；③ conformance 记忆轴并入 M2——mock 线缆锁 + live-smoke 第 3 阶段已落,官方臂请求体差分槽位待守密人 dispatch 采集（todo #T20）；④ 黑池搬运包单独产自包含档案落 `Public-Info-Pool/Resource/proposal/`；⑤ **CI 门禁修正 0710 定谳**：批准重启 Ruleset 但**只勾 required `test`、不勾 require branches up to date**（main 每小时机器提交会致分支永远落后,勾第二项 = 合并前无限追尾;勾选为守密人 GitHub 设置侧手动操作,生效前仍自查自合,todo #T19）；⑥ **合并纪律维持 §7.6 默认立即合并**（平台许可分类器偶发拦截时停下报告,不改常设裁定）。需求书 r1 逐字归档 `projects/silver-core-sdk/docs/MEMORY.md` | Silver Core SDK / 记忆系统 · CI 门禁 · 合并纪律 | ⑤ 覆盖 2026-07-10「CI required 检查维持自查自合」定谳（改为「只勾 required test,勾选生效前过渡期自查自合」）；其余不覆盖前条 |
| CI 硬门禁部分采纳定案 — 守密人 2026-07-11 裁定（修订 2026-07-10 节拍表决议③）：main Ruleset 恢复 required test 检查**已生效**（Ruleset 18506085 实测，机器推送与 PR 合并无误伤）；「Require branches to be up to date」**明文不开**——main 承载高频机器归档直推（每小时多路 cron），strict 策略会使 PR 合并陷入「刷基底→等检查→main 又动→再刷」跑步机，撞车成本高于基底陈旧风险。基底对冲的既有防线保留：pre-push 钩子自动对齐 + 合并前自跑全量测试。todo.md #T3 据此销案（因为 守密人经另一会话对冲分析后裁定：自动归档流量与 strict 策略结构性冲突，二者只能取一，取归档自动化） | 全局 / CI 门禁 | 覆盖 2026-07-10「CI required 检查维持自查自合」定谳（守密人此后已亲手勾选 required test，07-11 实测生效）与同日节拍表条③的 up-to-date 部分 |
| 归档采集降频至 3 小时一轮 — 守密人 2026-07-11 裁定（修订维护态节拍表持续档）：四路每小时 cron（update-news :00 / discord-volunteer :15 / discord-history-backfill :30 / discord-jp :45）全部降为 3 小时网格（*/3），错峰保持；日更档（discord 主服 18:00 / 评论 02:00 / arca 例程 02:37）不变。动机：降低 main 机器提交流量（约 8 笔/时 → 约 1/3），输出层新鲜度代价 <=3 小时可接受；discord 归档器为断点增量采集，降频不丢数据只延迟（因为 与 T3 部分采纳定案同日：机器提交流量既是 up-to-date 门禁的撞车根源，也是纯粹的 CI 开销；降频三倍在维护态定位下收益大于时效损失） | news / 采集节拍 | — |
| 采集日更档统一北京时间 15 点档 — 守密人 2026-07-11 裁定：discord 主服归档、视频评论、同人图、arca 自绑定例程统一进北京 15 点档（cron 落地 UTC：07:00 / 07:05 / 07:10 / 07:37，分钟错峰防同刻踩踏）。月度归档 / 周检 / KB 兜底等非采集日更 cron 不在射程。初版曾误按 15:00 UTC 落地，随「守密人口述时间一律北京时间」约定（同日）当轮改正（因为 守密人「日更档统一调整为 15:00」指令；与同日 3 小时降频裁定同向——采集节拍收敛为「3 小时网格 + 每日 15 点档」两层，便于记忆与排障） | news / 采集节拍 | — |
| 时间基准约定 — 守密人 2026-07-11 裁定：守密人口述时间一律按北京时间（UTC+8）理解；cron 等 UTC 落地配置须换算，汇报双标（北京时间为主 + UTC 括注）。规则入 CLAUDE.md §2.1 第 4 条（因为 「日更档统一 15:00」曾被按 15:00 UTC 误读落地（应为北京 15:00 = 07:00 UTC），同轮已改正；时区歧义一次即够，升格为人格规则层常驻约定） | 全局 / 沟通约定 | — |
| KB 有效性四项推进：遥测持久化方案甲 + activate 反枢纽修正 + 月检记分卡 + 投资纪律三不做 — 守密人 2026-07-11「全部按你建议推进」裁定（OKF 意义分析会话，授权代写）：承「OKF 对银芯有无意义」分析（结论：关键词维度 KB=grep 零增益、关系/层判定/身份/边界四维独有价值，需求侧有效性因遥测断粮未证实）。**①遥测持久化（方案甲）**：借阅记录由 gitignored `Public-Info-Pool/Rough/kb_usage.jsonl` 迁 git 内 `Public-Info-Pool/Record/kb-usage/{YYYY-MM-DD}.jsonl`（按日一文件，随会话提交跨会话累计）；云容器每会话全新克隆令原落点每会话归零，需求侧有效性 / 死概念剪枝 / 零命中回流三机制断粮，路径唯一源 = `kb_telemetry.KB_USAGE_DIR`（写读同一常量）。**②黄金集缺口修复 = activate 反枢纽算法修正**（非对题调参，四构件各有独立原则）：标题锚种子（查询逐字点名的概念升 1.5 能量主导扩散——kb_anchor 先锚后扩哲学内嵌，含**最长匹配纪律**：「本源萝坦」点名的不是「萝坦」；单字标题仅认 CJK，令角色「徐」首获检索入口）+ **最强路径 max 累加取代求和**（和值令多路径再汇聚的高度数枢纽——被众文档共同提及的热门角色——淹没一跳直达目标，实测期望命中从 16 名升 1 名）+ 边标签命中查询原文传导 ×1.5（顺所问之边扩散）+ 并列低度数优先破平（越专属的关联越靠前，与 max 同一反枢纽哲学）。实测：黄金集 15/17→**17/17**（MRR 0.824→0.882）、A/B KB hit@3 0.88→**1.00**（联想题 6/6 vs grep 2/6，kb_win 5 / grep_win 0）、质性四维 4/4 保持、KB 套件 88 测试全绿。**③月检档加 KB 记分卡三连跑**（kb_eval / kb_ab / kb_qual + 遥测查账，数字异动才呈报，入 `memory/methodology.md` 维护态节拍）。**④投资纪律三不做**：不再扩层加边除非有真数据可 derive 的高信号边（北极星判据）；向量索引全量量产（757 万条）维持 2026-07-05「60k 架构上限」裁定不动；mention 边（321/414 条）修剪与否待遥测数据积累后再判、不凭感觉剪（因为 评判体系三仪器供给侧数字已硬，唯需求侧闭环因 ephemeral 容器结构性断粮——仪器建好而登记本每晚被撕，先装电表再谈剪枝） | KB / 遥测 · activate 算法 · 维护态节拍 · 投资纪律 | ③补充 2026-07-10 维护态节拍表月检档（不覆盖）；①改写 2026-07-04「评判 #2 遥测」的 Rough 落点设计取舍 |
| SDK 自我改进闭环 SCS-REQ-002 阻塞项四裁定（T25/T26 销案）— 守密人 2026-07-11 AskUserQuestion 逐项裁定，授权代写：承需求书（`memory/active/self-improvement-requirements.md`）§8 开放问题。**① 行为评估 LLM 评分模型 = Claude Sonnet 5**（`claude-sonnet-5`，$3/$15 每百万 token、2026-08-31 前介绍价 $2/$10；守密人指定，未取艾瑞卡的 Sonnet 4.6 推荐项）——REQ-2.1 的固定评分提示词随 Phase 1 实现时一并钉死；夜间批跑走 Batches API 五折。**② 评分成本预算帽 = $30/月**（判卷侧；被测 SDK 任务自身 API 消耗另计）：支撑每夜一轮全量 runEvals（Sonnet 判卷批跑约 $0.65/轮 × 30 夜 ≈ $20）+ PR 触发按需加跑；超帽降频为隔夜、不加钱。**③ 20 题评估集来源 = 混合**：历史失败会话提炼为主（12–14 题，transportHealth 断流分类 / conformance 差分等真实故障作素材）+ 三维度（记忆召回 / 断流恢复 / token 效率）覆盖不足处人工构造补（6–8 题）；题目仍由维护者人工定稿入库 `evals/`、agent 不得改（防「改考题」红线不动）。**④ 环三沙箱 = 独立 checkout 目录**（git worktree / 独立 clone + 独立依赖安装）：轻量零新增运维件、符合闭环零运行时依赖原则；隔离的真实威胁面是误改生产文件而非恶意逃逸，目录隔离 + 底线层测试门禁已够，唯一维护者场景下容器编排为负资产。①②③销 Phase 1 阻塞、④销 Phase 3 阻塞，Phase 1（维护者出题 + runEvals()）即日可启动（因为 需求书硬序「环二先于环三」，阻塞裁定不清则整条闭环停摆；评分基准自身不稳则环二失效，故模型与预算一次钉死保跨次可比） | Silver Core SDK / 自我改进闭环 | — |
| 评估题库 r1 定稿 + 「对话内测试即合并」纪律 — 守密人 2026-07-11 对话内口谕（「1定稿」「我说的手动是对话内你直接测试通过就合并 不是去 github 排队 ci」），授权代写：**① 题库定稿**——SCS-REQ-002 行为评估集 r0 草案 20 题（提炼 14 + 构造 6，三维度 7/6/7）原样升 `final`（`evals/behavior/questions.json` `version: r1-final-20260711`），基线分自 r1 起算，此后改题 = 基线重置须落决策记录并重签 `MANIFEST.sha256`（T32 销案）。**② 合并纪律修订**——判定门 = **对话内直接跑全量测试**（SDK vitest + 仓级 pytest），绿即合并，**不以「围等 GitHub CI」为工作环节**；main Ruleset 的 required `test` 检查保留为 GitHub 侧物理门禁（守密人 T19 勾选不动），若合并瞬时被其挡下则等其转绿重试一次，但会话不再为 CI 安排轮询 / 计时器 / 自检回执。修订 07-11 早间「合并前等 required test 绿」的操作口径（门禁物理存在不变，会话行为改变）；CLAUDE.md §7.6 已同步（因为 守密人实测发现会话围绕 CI 的排队 / 计时循环拖慢节奏且与「对话内手动验收」的协作偏好相悖；本地全量测试与 CI 跑的是同一套测试，先跑先信） | 全局 / 合并纪律 · SDK 评估基准 | 修订同日「CI 硬门禁部分采纳定案」的会话操作口径（Ruleset 门禁本体不动） |
| 总结体⓪定性开篇修订 — 守密人 2026-07-12 口谕（「以后会话总结 在第一部份先说这是怎样一件工作有什么意义，再说细节」，授权代写）：合并后总结体在既有①-④之前加**⓪工作定性开篇**——第一部分先用一小段说清「这是怎样一件工作、有什么意义」（工作性质 + 对项目的价值定位），再进按主题归账等细节；CLAUDE.md §7.6 已同步（因为 总结体直接以细节归账开头时，守密人须自行拼出这批工作的全局定位；定性先行让读报次序 = 理解次序） | 全局 / 汇报规范 | 修订 2026-07-11「合并后必附对话总结体」条的结构（①-④不变，前加⓪） |
| SCS-REQ-002 阶段时间压缩 — 守密人 2026-07-12 口谕（「这几个阶段时间太长了 压缩到最短」，授权代写）：自我改进闭环阶段表的**日历等待全部压缩，只保留真实工程前置**。① Phase 0「两周 ≥10 条」观察期降为**并行观察、不阻塞环三**（记录质量并入 Phase 4 复盘评审；其本依赖黑池接线 #T33，挂日历徒增串行）；② **Phase 3 即日启动**（前置 Phase 1/2 已随 v0.49–v0.51 全落地，硬序「环二先于环三」已满足）；内部「先提示词后代码」两步保留但不设日历时长——首个提示词类自改 PR 经守密人合并即为管线验收、代码类随即放开；③ **「夜间」节拍改按需触发**（会话内 / dispatch 即触发，定时自动化留 BPT 侧接线、不阻塞）；④ **Phase 4 复盘触发改「累计 5 个自改 PR」**取代「跑满一个月」（对齐 REQ-3.1 验收口径）；⑤ 首轮 LIVE 基线轮同日点火（预算在 07-11 已裁 $30/月判卷帽内），分数播种 `evals-baseline.json`、REQ-2.2 门禁上岗。需求书 `memory/active/self-improvement-requirements.md` 归档注同步（因为 阶段表的两周 / 一月是保守日历习惯而非工程依赖，唯一维护者 + 评估门禁已立的场景下，串行等待只延迟反馈不增加安全） | Silver Core SDK / 自我改进闭环 | 修订 2026-07-11「SCS-REQ-002 阻塞项四裁定」的阶段节拍（四裁定本体不动）；覆盖需求书 §7 表内 Phase 0 观察期前置与 §9「满一月复盘」口径 |
| 裁定流程定型：会话产生的待裁项不得只挂清单收尾，须逐项向守密人说明（内容 + 影响 + 建议）并逐项询问取得裁定；未询问的待裁项按未裁处理，不得默认推进（因为 守密人 2026-07-11 于测试覆盖落地会话口头裁定「待裁定逐个给我说明并询问，以后这就是裁定流程」；与 memory/todo.md 挂账台账互补——台账记账，本流程管清账） | 全局 | — |
| **Phase 2 收口判定「基本达成」+ 验收标准修订 — 守密人 2026-07-12 裁定（账号回归问答，授权艾瑞卡代写）**：Phase 2「银芯使命建设期」（2026-04-27 → 07-19）提前收口，判定**基本达成**。对照 v2.0 M7 验收三项：① 使命基础设施齐备 ✅（采集三层 / 4R 数据本体 / OKF 知识库 / silver-core-sdk 全数就位）；② 自动化跑稳 ✅（19+ 工作流无人值守连续运行，每 3 小时节拍 + 日采北京 15:00）；③「至少一种贡献流程跑顺」——**随 wiki 使命取消（见同日裁定）作废，不再作为验收项**。诚实记录：③ 非「达成」而是「目标撤销」，Phase 2 以修订后标准收口。收口即时生效，不待 07-19 | 全局 / 战略 | 修订 2026-04-26 v2.0 M7 验收标准（③ 贡献流程项作废）；Phase 2 窗口提前终止 |
| **wiki 使命取消 + 银芯定位收敛「信息入口 + 通用 AI 底层开发基地」— 守密人 2026-07-12 裁定（授权艾瑞卡代写）**：① **使命#2「社区共建知识底座」取消**——wiki 不再作为银芯正式使命；社区数据采集的必要性一并进入重估（采集不停，重估另行裁定，挂账 `memory/todo.md` #T36）；wiki 子项目转冻结（已建成果保留：W2 基线 72 角色 / 站点静态页，不删不派发）。② 银芯使命收敛为：**使命#1 黑池信息入口**（news 核心，采集自动跑）照旧 + **新增正式使命「通用 AI 底层能力开发基地」**——silver-core-sdk 等通用 AI 底层工程产物由银芯持续开发，作为银芯→黑池单向输出物支持黑池建设。此为「事实使命转正」：SDK 原定位「非使命线工程产物」，但其已有 20+ 真实消费者（BPT 在产），按「文档跟上现实」纪律（lesson #29 反面）转正，勿再让事实使命与文档使命脱节。③ **背景事实落档**：黑池侧已完全弃用 Claude Code、全面换装自有技术栈（BPT + silver-core-sdk 0.3x pin），SDK 存在理由由「应急替代」升格为「常态底座」。同步范围：CLAUDE.md §1.2/§1.3/§1.4、README、strategic-plan-2026.md、project-status.md、projects/wiki/CONTEXT.md、projects/silver-core-sdk/docs/POSITIONING.md | 全局 / 战略 · 定位 | 覆盖 2026-06-28「二核心使命」裁定（#2 取消、SDK 使命转正）；修订 silver-core-sdk「非使命线」定性 |
| **银芯即时进入稳态维护期 + 过时 demo 清理 — 守密人 2026-07-12 裁定（授权艾瑞卡代写）**：① 银芯**即时**（不待 07-19）进入**稳态维护期**：采集自动跑、按需维护，不再开信息层建设战线；silver-core-sdk 按新使命持续开发，不受维护期限制。② **过时 demo 清理令**：旧 BPT 网页版与桌面第二版的一切演示性残留（如仍存在于 Releases / Public-Info-Pool 归档 / 文档引用中）标记过时并清理——**先扫描报告残留清单，经守密人过目后执行删除**（遵循「先报告后处置」惯例）；bpt-pm / bpt-v2t / game 原型**明确不在清理之列**（守密人仍欲推进的小功能子项目，维持非使命线定性）。③ 稳态期遗留待构想（挂账 `memory/todo.md` #T37，不阻塞收口）：news 下半场——归档已稳，**定期以何种形态推送到团队**待构想，作为稳态期唯一开放议题 | 全局 / 战略 · 仓库治理 | 承同日裁定一/二；「07-19 后进维护期」的原设想提前生效 |
| **自改循环授权 + 首轮循环收官（6 单）— 守密人 2026-07-12 口谕（「循环进行直到你觉得没有可改进的方式为止 此之前合并不要再问我 自己判断」，授权代写）**：① **授权**：SCS-REQ-002 环三自改循环内，环四合并权授权艾瑞卡自断行使（覆盖 REQ-4.2「禁止自动合并」的**本循环范围**；循环以艾瑞卡宣告「无可改进」为止，此后回归守密人逐单合并默认）。② **首轮循环收官（同日执行完毕）**：self-improve #1–#6 全部合并（#1 tok-06 接线 / #2 判卷校验闸 / #3 确定性切流 / #4 judge 预算+重试 / #5 runlog 追加序列化 v0.51.2 / #6 判卷证据瘦身），判卷成功率 15/20 → 18-19/20、四轮门禁全 PASS、末轮 9 作业全绿；**停机判定**：评估基础设施四层（注入/判卷/计分/门禁）无已知可修缺陷，残余项全属守密人权限或收益递减区——(a) **dc-03 语义冲突待裁**：引擎截断抢救「半截话作最终答案」忠实复刻官方 E3 语义，rubric 期待续写补全，两轮稳定 1 分，改 rubric = 基线重置守密人专权；(b) mem-03/dc-05 **顽固无分对子**（3 轮 5 次 judge 结构化输出失败，重试+瘦身均不解）转观察；(c) judge 方差（mem-01 零改动 2→5 实证）与 estBytes/多判取中等属收益递减，不再专门烧轮（因为 授权消除逐单等待后，单日走完 6 个完整「定位→修复→LIVE 验证→合并」周期，判卷侧当日约 $15 在 $30/月帽内；停机准则「权限边界 + 边际收益」比「绝对完美」诚实） | Silver Core SDK / 自我改进闭环 · 环四授权 | 覆盖 REQ-4.2 于本循环范围（循环结束即失效）；承同日「阶段时间压缩」裁定 |
| **Phase 2 收口余项三裁定 — 守密人 2026-07-12 AskUserQuestion 逐项裁定（承同日三条收口裁定的余项清单，授权艾瑞卡代写）**：① **deploy-site.yml 过时注释订正**——头注释「+ bpt-web」字样删除（demo 残留扫描唯一清理候选，报告 `Public-Info-Pool/Resource/repo-engineering/bpt-demo-residue-scan-20260712.md`）；随本项执行，**过时 demo 清理令全量闭环**（Releases 与 Public-Info-Pool 演示性二进制残留实证为零，唯一文字残留已订正）。② **T36 社区数据采集必要性重估 = 维持现状销案**——采集全量照跑，以既有数据增长触发线三条（2026-07-10 节拍表条②）为唯一收缩触发器，不专开重估战线；判定依据：社区采集主消费方本就是使命#1 黑池信息入口，wiki 几乎不消费社区档案，wiki 使命取消对采集必要性实际削减很小。③ **T37 news 推送形态 = 先出形态提案再裁**——形态 × 通道 × 节拍三轴矩阵提案已落 `Public-Info-Pool/Resource/proposal/news-push-format-20260712.md`（五组合案，艾瑞卡推荐甲案 = 摘要周报挂周检档落 Resource/、守密人转发，零新自动化件），T37 继续挂账待守密人过目后裁定 | 全局 / 稳态维护期 | 承同日「Phase 2 收口三条」裁定；T36 销案（同日挂账同日销）；T37 挂账状态更新 |
| **踩坑档案治理体系 + 记忆巡检长期机制 — 守密人 2026-07-12 裁定（踩坑盘点会话，AskUserQuestion 三批九项逐项裁定，授权艾瑞卡代写）**：**（甲·盘点重构，PR #638）**①四处硬伤修复（#47 字段被 #48 污染拆开重建 / 头部对账 / #25 错引 / 按编号重排）；②同簇合并采「主条吸收 + 留号指针」（#4/#5/#11/#25→#29、#21乙→#26、#28/#34→#39、#35/#36/#37/#40/#41→#43、#47/#48→#49，编号永久固定不重用，外部编号引用全保）；③建 `memory/lessons-archive.md` 归档层（机制退役 7 条 + 路径已删 7 条 + 已毕业 #12/#19/#30/#32/#46 全文迁入，只进不出）。**（乙·准则清单体，PR #639）**主档改「准则先行」定额格式——每条 3–5 行（坑/准则/防护指针），长叙事下沉归档层案卷区；维护说明立**毕业纪律**：踩坑档定位中转站，每条记入须答「能否升格为测试/钩子/CLAUDE.md 硬约束」，升格落地即迁档（实证：#28/#34/#39 文字三记防不住复发，pre-push 钩子一次根治）。**（丙·长期维护三件套，本批）**①月检 CCR 例程「记忆巡检」——自绑定模式（#49 簇实证），`trig_018DEmQbNLNCToYFrtaJVcVs`，cron `0 0 1 * *`（UTC）= 北京每月 1 日 08:00 左右自动点火（守密人 2026-07-12 修正原案「5 日 10:00」为「1 日 0 点」；北京 1 日 00:00 = UTC 上月末日 16:00，标准 cron 无法表达「每月最后一天」，经守密人认可取最近可表达时刻 UTC 1 日 00:00），免守密人想起；②机械守卫 `scripts/memory_freshness.py`（确定性零 ML：lessons 指针完整性 / 编号对账 / 引用路径存在性 / git 龄与头部日期错位）——**不变量进门禁** `tests/test_memory_freshness.py`（随 required test 每 PR 把门），**保鲜阈值只进巡检报告**（防无辜 PR 被「文档老了」挡下）；③巡检手册固化进 `/sync-memory`（机械体检→状态同步→语义复核→逐项呈裁四段，例程与人工共用零分叉）。**红线**：机器只点火与体检，档案写入仍人工策展——不复活 2026-06-14 退役的自动记忆环（那套是机器写记忆，本套是机器提醒人来审）。首跑实证：门禁当场抓到「下一条=#51 vs 实际 #51」记账漂移 + wiki CONTEXT 仍指已删 Game-Unpacked 层等 7 处失效引用 | 全局 / 记忆治理 | 建于 lessons-learned 档案之上；巡检节拍并入 2026-07-10 维护态节拍表月检档 |
| **Silver Core SDK drop-in 兼容承诺退役 → 「模型面贴近、宿主面自主」— 守密人 2026-07-17 裁定（需求档 SCS-REQ-REPOS-01 §1/§2/§6，授权代写）**：SDK 定位终态改写为黑池五层介质架构中的**引擎层**（最慢介质、版本化、可替换的商品泵），职责由三否定一肯定定义——不认识斜杠 / 不持有钟 / 不追宿主面 / 只把一次结构化 agent 调用执行到位并把**模型侧表面**保持在训练分布附近。创始 drop-in 兼容承诺退役：兼容基线不再构成待办来源，COMPAT 降级**参照笔记**（记官方做法作设计输入、移除待补齐义务表述），对标 chase 改双触发（新模型发布 / 自设新模型侧表面），主权退路论证更新为「引擎自持 + 测试资产（单测全绿 + 变异纪律）」而非「可换回官方」。扩展面总原则同批立法：全部扩展面限定钩子 / 工具 / 结构化配置三种缝，不引入第四种；钩子契约总则（引擎拥有何时与契约、宿主拥有什么，回调生命周期不得超出所嵌 query）入钩子文档（因为 接口已超集、替换退路无人行使、CLI 中间层开销与直连轻快核心价值冲突、无第二消费者） | Silver Core SDK / 定位 | 覆盖 2026-07-03 创始「drop-in 兼容」定位与 2026-07-04 POSITIONING「兼容表面」轴（独立引擎 / 钉基线 / 单向输出等其余轴不动）；POSITIONING/COMPAT 同日重写 |
| **「机制归引擎、钟与装配归宿主」终裁 + 斜杠解析退出引擎行为 — 守密人 2026-07-17 裁定（需求档 SCS-REQ-REPOS-01 §4/§6，授权代写）**：v0.59「/loop 不进引擎内建」之后曾议「内建化」，终裁为**机制归引擎、钟与装配归宿主**：引擎不再对 prompt 文本做任何斜杠命令识别与展开（prompt 一律原样进装配），/loop /goal 文本解析器与控制器（parseLoopCommand / createPromptLoop / goal 文本接线）**同版本整体删除**（代码 + 公开导出 + 行为测试，无弃用期无双路径——过渡态双真相源是混淆源）；/goal **机制保留在引擎**（Stop 拦截 / 裁判反馈注回 / impossible 逃生 / 上限帽，均在单次 query 生命周期内），入口切换为 query options **结构化配置唯一入口**（目标描述 + 宿主注入评估器回调 + 逃生策略，引擎不硬编码模型选择）；循环支撑改为六项接口面（R1 续轮注入 / R2 预算事件流 / R3 压缩保留区 / R4 ledger 原语 / R5 LoopControl 提议工具 / R6 版本声明面），loop 编排本体归黑池 runner 守护层（因为 五层介质架构下钟属 runner 层、斜杠属客户端层，引擎自成 loop 即走私墙钟） | Silver Core SDK / 引擎行为 | 覆盖 v0.59「LOOP_SLASH_COMMAND 菜单元数据」与 v0.60「GOAL_SLASH_COMMAND + parseGoalCommand」文本面（机制本体保留）；覆盖 v0.38 自定义斜杠命令展开层（.claude/commands 输入解析归客户端层） |
| **归因回流 = 需求非数据（黑池防火墙操作口径）— 守密人 2026-07-12 裁定（「可以落档」，授权代写）**：明确 §1.1-HC 防火墙下「黑池侧发现如何帮上银芯」的**唯一合法形态**——(1) **黑池墙内自析合规**：调查 / 归因发生在黑池内部、用黑池自有跑数，数据一字节不出墙，防火墙只封「数据出墙进银芯」、不封「黑池自己查案」；(2) **过墙的是需求，不是数据**：黑池分析完，向银芯递交的是**用银芯公开语汇写的行为规格 / bug 工单**（「SDK 在 X 情形该 Y」「判卷官某场景偶发失败请修」），**不是**原始会话日志 / transportHealth 转储 / 商业 / SVN 内容；(3) **守密人当过滤器**（承 T33 + bpt-positioning 丙「唯一裁定者 + 回流过滤器」）：把「内部数据」抽象成「公开需求」的提炼由守密人负责，某条「需求」若实质夹带黑池机密仍按 §1.1-HC 挡下并报告；(4) **方向全程同向**：黑池发现 → 守密人过滤成需求 → 银芯公开仓库实现 → 银芯→黑池单向输出成品，无一处逆流。此为 T38 判卷尾段归因的**独立第二推进腿**（与「银芯自充值重跑判卷」互不阻塞）。比喻：里屋自己验尸查病因合规，但递出门给药房的须是「处方单」（需求）不是「病历原件」（数据），门只单向递药、绝不收病历 | 全局 · 黑池防火墙 · 归因协作 | 细化 §1.1-HC（新增「归因回流」合法形态）+ T33 操作口径；不覆盖 §1.1-HC 硬约束本体 |
| **BIAV SDK 家族碑文:monorepo 两 workspace + 依赖单向 编排→代理 — 守密人 2026-07-17 需求档裁定（SCS-REQ orchestrator-sdk §2,2026-07-18 施工落地,授权代写）**:①brain-in-a-vat 转 npm monorepo,两 workspace——`@biav/agent-sdk`(银芯代理 SDK,silver-core-sdk 更名承接,0.66.0 起)与 `@biav/orchestrator-sdk`(银芯编排 SDK,新立,0.1.0 起);仓库根 `package.json`(private)+ 单一根 lockfile,仓库其余部分(news/wiki/site/memory)不入 workspace。②**两包各自 semver、版本钟永不同步**,任何「顺手一起 bump」皆违规;代理慢(商品泵)、编排快(跟宿主需求)。③**依赖单向:编排 → 代理**——代理永不 import 编排、不感知其存在;共享代码只准下沉进代理 SDK 或独立第三包;编排只准消费代理**公开面**(裸 specifier `@biav/agent-sdk`,深路径/相对路径伸入即违规);CI 守卫 `.github/scripts/check-dep-direction.mjs` + `orchestrator-sdk.yml` 机器执法,施工日双向违规红证实测(反向 import / 深 import / 相对逃逸 3 条全抓)。④编排 peerDependency 声明兼容代理区间(`>=0.66.0 <1.0.0`),消费代理侧 R6 版本声明面。⑤**不做伞包**,文档层统一「银芯 SDK」品牌;安装哪个包即用户对自身位形的声明。⑥**更名映射射程 = npm 身份**(施工填空,承 2026-07-10 更名裁定③「历史不追溯」同理):运行时品牌串(User-Agent `silver-core-sdk/<version>`、错误前缀、conformance 标签)不动,模型面/线上表面零移动;`npm pack` 产物名随 npm 规则变为 `biav-agent-sdk-<v>.tgz` / `biav-orchestrator-sdk-<v>.tgz`,黑池 pin 自 0.66.0 起换新文件名 | Silver Core SDK / 编排 SDK / monorepo | 与 2026-07-17 代理侧两条覆盖注(#129/#130)并列;细化 2026-07-10「更名 Silver Core SDK」条②的 npm 包名维度(目录/User-Agent/日志前缀不动) |
| **编排 SDK 第一战:任务台账 + 驱动器落地,状态集定稿 + 留白填空 — 2026-07-18 施工回填(施工封面 §2 授权「读码后自行设计、写完即动手」,授权代写)**:@biav/orchestrator-sdk 0.2.0 交付需求档 §4 首战能力面。**①状态集定稿**(已写回需求档 §4 原位):session 级封闭集 `pending | running | retrying | failed | done`(terminal = done/failed),事件集 `claim | attempt:ok | attempt:error | attempt:timeout`——重试**复用 claim 事件**(pending 与 retrying 同以 claim 进 running,不设独立 retry:due 事件,挂账草案的该事件在实现中并入);query 级轮次结果 `ok | error | timeout`。**②留白填空清单**:(a) 存储缝 `LedgerStore` 五方法(putSession 整行 create-or-replace / getSession / listSessions{states,dueBefore} / appendQuery 只追加 / listQueries 按追加序),SDK 零内置实现(§7 非目标),测试与例程各自内联 ~30 行内存店证缝可用;(b) attempts 计数**在 claim 时 +1**(query 行 attempt 即 session.attempts),耗尽判定 attempts >= maxAttempts 于 attempt:error/timeout 时点;非有限计数器一律 InvalidTransitionError(防 NaN 毒化永久 retrying,承 T49 L75 同型);(c) 退避 = baseDelayMs·factor^(attempt-1) 封顶 maxDelayMs(默认 3 次/1s/×2/60s 帽),负值病态策略落帽、零值合法立即重试、非有限落帽;(d) 驱动器持钟轮询 claimDue(默认 1s)+ 每 attempt 超时(AbortSignal),**stop() 中止在飞 attempt 记 error 走正常重试路径**——重启的驱动器按 nextRunAt 自然续跑(nextRunAt 落库,调度熬过宿主重启);onEvent 观测缝只出数据、回调异常吞掉不倒灌;(e) 钟缝 `Clock{now,setTimeout,clearTimeout}` + systemClock 调用期读全局(fake timer 免注入即生效)。**③验收实绩**:装配测试仅公开面零补丁拼全状态流转(含 retrying 路径/驱动器超时路径/宿主停机续跑,fake timer 零真实钟);例程一(最小 loop:周期派发+预算帽+到帽收尾)只 import 两包公开面,e2e 对本地 Messages 仿真器(独立按公开线缆形状自写,非 import 代理内部件)真跑 R2 事件流——budget:threshold + budget:exhausted 均消费、closeout 报告落手、台账上 tick1 done/tick2 failed;**纯核 state.ts 变异分 100%**(83/83 击杀,歼灭轮 92.77→100,棘轮档 `projects/orchestrator-sdk/mutation-ratchet.json` 地板 100 只升不降,周检 `sdk-mutation-ratchet.yml` 新增 ratchet-orchestrator 任务值守);依赖方向守卫绿。设计挂账见 `memory/todo.md` #T53(挂账为草案,以本条定稿为准) | 编排 SDK / 任务台账 | 承 2026-07-17 需求档 §4 与同月 18 日碑文条;#T53 挂账随本条定稿销案 |
| **SDK 家族定名:Silver Core Agent SDK + Silver Core Maestro SDK — 守密人 2026-07-18 交互裁定(授权代写)**:①银芯 SDK 家族为**两个独立 SDK**——品牌名 **Silver Core Agent SDK**(代理,原子:一次结构化调用)与 **Silver Core Maestro SDK**(编排,分子:钟/跨会话状态/会话装配);②npm 包名按 silver-core 系拼法:`silver-core-agent-sdk`(0.67.0 起)+ `silver-core-maestro-sdk`(0.3.0 起),**覆盖需求档 §2 与同日碑文条的 @biav scope 拼法**(该拼法存活不足一日、零外部消费者,需求档 §2 已加命名覆盖注);③maestro 择名经历:orchestrator 嫌长 → 候选缩写 orch/换词 runner/conductor/maestro → 守密人定「乐队指挥方向」→ 一度改选游戏角色「哈姆林」(Hamelin,唤醒体指挥意象,英文拼写经全量档案层 global 服核实)→ **终裁回 maestro**;④目录:`projects/orchestrator-sdk` → `projects/silver-core-maestro-sdk`(目录对齐 npm 名,与代理侧「目录=旧包名」惯例一致);代理目录 `projects/silver-core-sdk/` 维持 2026-07-10 裁定不动;⑤更名射程沿用 0.66.0 口径:npm 身份 + 品牌名,运行时品牌串(User-Agent `silver-core-sdk/<v>` 等)不动;tarball 名自此为 `silver-core-agent-sdk-<v>.tgz` / `silver-core-maestro-sdk-<v>.tgz`;⑥编排包公开版本常量随品牌改 `MAESTRO_SDK_VERSION`(pre-consumer 无弃用期);历史文档(CHANGELOG 0.3.0 前条目 / decisions 前条 / todo 销案引)保留 @biav 旧名不追溯改写 | Silver Core SDK 家族 / 命名 | 覆盖 2026-07-18「BIAV SDK 家族碑文」条①⑥的 npm 拼法维度与「第一战」条包名字样(其余条款不动);细化 2026-07-17 需求档 §2(命名覆盖注同步) |
| **编排 SDK 第二战:商店巡检真实场景接入落地 — 守密人 2026-07-18「第二战点火」口谕(施工封面 §2 留白授权,授权代写)**:需求档 §8「新场景首发」验收兑现——首个**生产**循环任务直接长在 Maestro SDK 台账 + 驱动器上。**设计填空清单**:(a) **巡检面 = Morimens Steam 双官方公开 JSON 端点**(appdetails 价格/发行面,appid 3052450;appreviews 评价总量面——后者 news 层 `aggregator_collectors.py` 已有消费先例,**零新增 ToS 面**;TapTap 等留注册表扩展位 `examples/store-patrol.targets.json`,加新商店 = 加一段配置零新代码,承 archive_engine 注册表哲学);(b) **签名指纹法**:响应归约为稳定字段集(排除 num_reviews 等请求域易变字段),逐日比对,变即追加 `changes.jsonl`;(c) **归档落点** `Public-Info-Pool/Record/store-patrol/{target}/`(latest.json + {date}.json + changes.jsonl)+ 台账持久化 `state/ledger.json`(宿主文件店,跨重启恢复真实生效);(d) **幂等派发键** `patrol:{target}:{date}`(同日重跑跳过;failed 终态自动开 `:rN` 重试会话);(e) **失败面**:驱动器超时经 AbortSignal 真中断 fetch,3 次退避耗尽进 failed 并翻进程退出码(CI 可见化);(f) **节拍**:每日北京 15:15(07:15 UTC,并入 2026-07-11 十五点档、分钟错峰),CI `store-patrol.yml` sparse 排除大档案层但**纳回** store-patrol 子目录(`git sparse-checkout check-rules` 实证);(g) 巡检不用代理 SDK(executor 纯 HTTP,零 API 花销——台账/驱动器本就 agent 无关,§1 硬性质 1「零件可单独拿取」的实证)。**验收实绩**:e2e 四场景绿(基线归档/跨日变更检测+同日幂等/500→200 重试进 done/挂死超时×3 进 failed+同日 r2 重开),2026-07-18 首次生产真跑绿(两目标 done,基线:免费/发行 2024-08-01/Very Positive 3989 正:797 负:4786 总);examples/tests 非发货面,maestro 版本 0.3.0 不动(发版纪律:仅 shipped 运行时变更 bump) | Maestro SDK / 商店巡检 | 承需求档 §8 与 2026-07-18 第一战条;周报 loop 迁入(§8 改轨)另役待点 |
<!-- DECISIONS-INSERT-ANCHOR -->

### 子项目

| 决策 | 影响范围 |
|------|---------|
| 合并 database 和 wiki 为单一 wiki 子项目 | wiki |
| 主站导航页 + 子路径多站点方案（根路径主站，/wiki/，/news/） | site |
| 部署方式：peaceiris/actions-gh-pages 推 gh-pages 分支 | site |
| Wiki 中文设为 root locale + rewrites | wiki |
| 界域 ID 标准化（aequor/caro/ultra） | wiki/data |
| 角色职能标准化（attack/sub_attack/defense/support/chorus） | wiki/data |
| 角色 ID 从拼音改为英文 slug | wiki/data |
| Wiki 删除 tier 评级数据 | wiki/data |
| 整合 content_database 技能到 characters.json | wiki/data |
| 立绘图片存仓库（assets/images/portraits/） | wiki/data |
| 建立 7 脚本自动化数据抓取体系（Fandom + Steam API） | wiki |
| Wiki 引入 Vue 交互组件（11 个） | wiki |
| 自动生成角色详情页（generate_pages.py，63 角色 × 3 语言） | wiki |
| 添加 SEO 优化（Schema.org + OG + sitemap） | wiki |
| 版本更新自动检测 + RSS 订阅 | wiki |
| News 采集管线统一方案（先统一 JSON schema，再逐个接源） | news |
| 新增 Code-site 子项目（部署流水线 + 跨站前端） | site |
| Discord 数据分级存储架构（git 保留 60 天 JSONL + 月归档至 Releases） | news/discord |
| Discord 归档系统 4 项技术决策（断点续传、月报容错、论坛增量、无成员 Intent） | news/discord |
| 联动关键词确认：沙耶之歌 (Saya no Uta)，日报系统已配置监控 | news |
| 双采集栈逐平台收敛（2026-06-20，详见下方专条） | news |
| god module 不拆分（aggregator_collectors / global_collectors 保持现状，2026-06-20） | news |

---

### 2026-06-20 双采集栈（ARCH-01）逐平台收敛裁定（守密人）

**背景**：`aggregator.py`(AC 栈) 与 `collect_global.py`(GC 栈) 在 `update-news.yml` 中**先后都跑**，
导致 reddit/bilibili/youtube/taptap/discord 5 个重叠平台被**采集两遍、字段形态不同**（即审计 ARCH-01
`# NOTE: divergent ... not merged`）。守密人逐平台裁定唯一权威实现，消除重复采集：

| 平台 | 裁定 | 操作 |
|------|------|------|
| reddit | **AC 富数据为准**（JSON 分页+评论+媒体+search 回退）| GC 停采 reddit |
| bilibili | **AC 富数据为准**（官方 API+评论+用户空间+search）| GC 停采 bilibili |
| youtube | **GC 官方 API 为准**（googleapis YouTube Data API，稳健）| AC 停采 youtube（弃网页爬取）|
| taptap | **两侧字段合并**（AC 双区 .cn/.io+moment + GC topic 取并集）| 融成一份 |
| discord | **三路径归一**：`discord_archiver.py`（活 API→落归档）为唯一活 API 采集器；AC `fetch_discord_local` 读归档入流；**删 GC `fetch_discord` 冗余活抓**（其独有频道并入 archiver 配置）| 删 GC 活抓 |

**非重叠平台不动**：AC 独有 steam（reviews/news/discussions）保留；GC 独有 twitter/weibo/arca_live/
appstore/pixiv/google_play/bahamut/weixin/note_com/ruliweb/stopgame 保留。

**god module 不拆**（关联裁定）：拆分会让 `test_aggregator_collectors` 100+ 处 mock（`ac.requests` 60 +
`ac.` 内部 helper 49）失效，须重写上百处打桩、回归风险落在活的使命#1 管线，收益仅文件变短，不值得。

**统一采集入口（2026-06-20 守密人 goal「合并所有采集器功能到 AC」）**：`aggregator.py` 成为唯一采集入口——
先采 AC 平台，再内部调 `collect_global.main()` 采全球平台并产出最终 `news.json` + `news-raw.json`。
`update-news.yml` 原独立的「Run global collectors」步并入 aggregator 步（env 取两步并集），删除单独步以免重复采集。
`global_collectors.py` / `collect_global.py` 保留为被调库（函数与单测不动，避免 #4 的 mock 破坏），仅不再单独作为 workflow 步骤。

---

### 2026-06-21 采集编排器三层定性 + 声明式归档引擎（守密人裁定 A + 合并）

**整体规划（三层定性）**：盘点 8 条采集工作流 + 18 脚本后定性，采集层非一锅，而是
**职能三层、产出三个不同目标**：**T1 新闻流采集**（`aggregator.py` 单入口 → AC 平台 +
内部调 `collect_global`，产 `news.json`，每时 :00）/ **T2 数据层归档**（`discord_archiver` /
`collect_fanart` / `collect_video_comments` / `archive_platforms` / `archive_discord`，写
`data/*`，各自错峰）/ **T3 维护回填**（`repair_gaps` / `backfill_*` / `download_media` 等）。
**核心边界**：守密人 goal「合并采集器到 AC」精确落在 T1（已于 ARCH-01 完成）；**T2 归档器
保持独立、绝不并入 AC**——产出目标不同（`data/*` 全量档 vs `news.json` 流快照）/ 节拍刻意
错峰 / 存在上游→下游依赖（archiver 落档 → AC `fetch_discord_local` 读档入流）。核验证伪两处
疑似不一致：youtube AC 函数系 ARCH-01 有意保留（非残留）；discord 三脚本（采集/补缺/冷归档）
零冗余。

> **2026-07-02 反转**：上述「youtube AC 函数有意保留」已被守密人裁定推翻——`aggregator_collectors`
> 的 `fetch_youtube` 及子树连同单测已随死代码清理删除（见当前有效决策表「死代码清理 + ARCH-01
> 保留休眠反转」条）。youtube 权威实现仍独归 GC 栈。

**声明式归档引擎（A + 合并）**：把归档器从「每来源一台专用机」改为「通用引擎 + 来源注册表」。
新增 `archive_engine.py`（≤300 行）读 `archive_sources.json` 干活；加新归档来源 = 注册表加一段
配置，零新代码。收编原 `archive_discord.py`（改向后兼容垫片委派引擎，`discord-archive.yml` 零
改动；标签 `discord-archive-YYYY-MM` 命名与 `git_rm` 删数据路径逐行等价，现存 20 个 Release 不
孤儿）。智能化：自动生成 `releases-index.json`（治「Release 好难认」）+ Release 说明带 manifest +
统一 archive-log + 幂等重跑 + `--dry-run` + 按来源独立 cutoff。`tests/test_archive_engine.py`
12 测试锁安全不变量。

---

## 决策历史归档

编年审计日志（含 BPT 等已删子系统的追溯记录）与已退役决策全文，已迁出至
**[`memory/decisions-archive.md`](decisions-archive.md)**。本档案只保留当前生效规则。
