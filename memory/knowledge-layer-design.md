# 银芯知识层设计哲学 —— 神经符号白盒骨架（North Star）

> 守密人 2026-07-04 会话结晶（承 #386 运行时导航 / #396 全仓知识组织之后的定位对齐）。
> 本档是**知识层的北极星**：决定「什么该白盒化、边怎么长、怎么守边界」。工程改造以此为准绳。

## 一、一句话定位

> **OKF = 有结构的概念网络，承载白盒结构化知识；区别于神经网络承载的黑盒知识。**

它既不是「搜索」（在知识之内找某样东西），也不是「神经网络」（把知识化成不可读的分布式权重）。
它是**对知识的外延与形状的显式声明**——可枚举、可审计、可程序操作。

## 二、三个器官（不是三个对手）

| 器官 | 是什么 | 干别人干不了的 |
|------|--------|--------------|
| **骨架**：白盒概念网络（OKF/okf 层） | 结构 / 身份 / 层 / 边界 | 可枚举、可审计、可测试、可程序操作 |
| **结缔组织**：搜索 / 全文（ripgrep、kb_search 倒排） | 钻进骨架「只指不放」的血肉 | 模糊、无界、按内容命中 |
| **肌肉**：黑盒神经（艾瑞卡本体 LLM） | 推理 / 泛化 / 生成 | 处理没见过的、插值（有脑补代价） |

艾瑞卡是肌肉（会脑补），OKF 是骨架（不脑补、但可能漏）。二者互为解药：肌肉给骨架泛化力，
骨架给肌肉「你到底知道什么、到没到边」。

## 三、白盒 vs 黑盒的互补对立

- **黑盒（神经）**：会泛化 / 插值 / 处理未见 / 生成；但不可审计、不可枚举、会脑补、无稳定边界。
- **白盒（概念网络）**：精确 / 可枚举 / 身份稳定 / 有界；但不会泛化、只知道被显式建模进去的。

**招牌死法各异**：黑盒**以捏造撒谎**（confabulate）；白盒**以遗漏撒谎**（假完备——有界地图感觉是全的，
其实只和策展一样全）。「可枚举边界」这同一性质，既是白盒最强的**幻觉护栏**，又是它最深的**假完备陷阱**。

## 四、由定位导出的三条工程命令

1. **白盒只花在骨架上**（放指针不放本体的更深形态 = 白/黑盒边界裁定）：
   结构进白盒（哪些平台、哪层、怎么连），血肉留黑盒/搜索（750 万条正文、44M 解包本体原地）。
   **判据**：一个概念若没有非平凡的边、也没有搜索给不出的层/身份元数据，它就是「被冻进文件的搜索结果」——
   该去挣一条高信号边，或退回给 ripgrep。扁平孤立指针 = 付了白盒建模的代价、没买到白盒的好处。
2. **把不变量测起来**（白盒 = 唯一能写测试、能被程序操作的知识）：
   传递闭包 / 集合运算 / 一致性 / 完备性——这些对黑盒做不到。银芯已有的 data_layer 纪律、指针不落空、
   覆盖哨兵**能存在，纯因知识是白盒的**。可审计不是附赠性质，是让知识**可治理**的前提。
3. **死守覆盖哨兵**（守白盒的招牌死法）：假完备是白盒唯一的特征危险，覆盖哨兵是它唯一的守卫——
   边界必须是**真边界**，不是伪全集。

## 五、神经系统的两条设计教益（拓扑像脑，但要守纪律）

- **符号底座 + 神经式动态**：底座保持符号（可审计），只让**检索动态**变神经——
  **扩散激活（spreading activation）**：从命中概念沿带类型的边多跳扩散、带衰减，返回被点亮的子图 = 联想召回。
  这是平铺搜索与单跳查都给不了的第三种检索原语，也是「概念网络 ≠ 搜索」的杀手级消费方式。
- **像脑地剪枝**：神经系统的智能来自**突触修剪**（留高信号、砍噪声），不来自堆突触。
  #393 删画师边（72 角色同画师 = 零区分度噪声星）字面就是一次剪枝。
  **维度的价值不在多，在「每条边是否携带别处得不到的信号」。**

## 六、现状实测（2026-07-04，改造基线）

- 幂等：连建两次逐字节相同 ✓
- 边类型：variant 7 / lore 3 / cv 30 / cross 56（共 96）
- **孤立率：约 200 / 293 概念度数=0**——memory/resource/wiki-data/unpacked/extracted/assets/story 近乎全孤立。
  即：当前 graph = 小连通核心（角色+平台）+ 大片孤立指针群岛。**扩散激活在群岛上会当场死掉。**

## 七、改造路线（以本北极星为准绳，落地进度见 project-status）

| 支柱 | 命令来源 | 内容 | 依赖 |
|------|---------|------|------|
| **A 两层结构（已落地）** | §四.1 / §五剪枝 | **守密人 2026-07-04 边策略裁定「选 1」+ 实测收束**：勘查发现除 variant/lore/cross 外几无「真数据 derive 的高信号边」可加（character_story_links 14 角各属不同章节=零同簇；wiki-data→角色=大容器成员噪声星，思想反对）。故 A 的诚实形态**不是加边、是重分类**：显式声明 **skeleton**（characters/sources/community/news-output，实测连通 76%）vs **search**（其余参考层，有意孤立、靠 kb_search 可达）。tier 落 kb_index/graph 节点，绊线 `test_skeleton_is_actually_connected`（骨架连通率≥60%）把「200/293 孤立」从缺陷锁成设计属性 | — |
| **B 治理不变量** | §四.2 | 幂等锁 + 结构指纹（排除易变量的规范哈希，抗每日漂移）+ 生成器假设绊线（no domain:misc / no 坏 slug / 白名单⊆实况） | — |
| **C 覆盖哨兵（已落地）** | §四.3 | `tests/test_kb_coverage_sentinel.py`：扫全仓知识文件（KNOWLEDGE_GLOBS），断言每个被某概念 `resource` 覆盖（直指或目录指针涵盖），未覆盖即报错，逼建层/补指针/显式豁免（附理由 ALLOWLIST）。自动化 ultracode 批判员当初人工活。实测仅 processed/README.md 一处（层索引 meta）豁免，余全覆盖 | — |
| **D 扩散激活检索（已落地）** | §五动态 | `kb_activate`：从种子（概念 id 或检索词）沿骨架多跳带衰减扩散、按边类型加权（variant 1.0/lore 0.9/cross 0.7/link 0.5/cv 0.15，剪枝即加权），返回被点亮子图=联想召回。MCP 第 9 工具 + `kb_navigator.activate` 后端，确定性零 ML。实证：`activate("discord")` 跨层点亮全量档案镜头+输出抽样+分析索引（搜索连不到的结构） | **A**（已落地） |
| **E 减易变 / 骨架-血肉边界（已落地）** | §四.1 | `okf_pointer_layers._magnitude()`：community 概念描述里每时增长的精确条数（7,565,639）→ 量级桶「百万级（精确值见指针本体）」，保「大 vs 小」导航信号、杀每时 churn、活数字推回指针后。聚焦 community（唯一每时源）；unpacked/extracted 罕变故留精确 | — |

推荐序：**A → B → D → C → E**。**五支柱全部落地（2026-07-04）**——A 两层结构 / B 治理不变量 /
D 扩散激活检索 / C 覆盖哨兵 / E 减易变。北极星改造路线**收尾完成**。

## 八、Pillar A 的教益：诚实分类 > 强行连接

A 原设想「给孤立层派边压孤立率」。实测证明：在「不造噪声星」（守密人选 1）约束下，
**真数据 derive 的高信号边就那么多，剩余孤立不是缺陷、是参考层的本相**。于是 A 从「加边」
转为「**显式声明两层结构**」——让 skeleton（真网络）与 search（参考层）成为白盒对自身形状的
可断言声明。这本身就是命令一「白盒只花在骨架上」的兑现：**不把血肉硬塞进骨架，而是诚实标出
哪是骨架、哪是靠搜索够到的血肉**。度量的价值不在「孤立率降到 0」，在「每个孤立都是有意的」。

## 九、消费模式路由（检索模式匹配问题类型，守密人 2026-07-04 洞察）

反事实 A/B（评判 #3）实测：**KB 只在联想维度胜 grep，关键词维度打平**（KB 0.80 vs grep 0.70，
Δ 全部来自 activate 模式）。直接的消费后果——**别对关系题用关键词搜索，那等于退化成 grep、白瞎了 KB**：

- **身份/关键词查**（「X 是谁」「含某词」）→ `kb_search` / `kb_get`（此维度 grep 就够，用不用 KB 一样）；
- **关系/探索/设计题**（「X 与什么相关」「围绕 Y 探索」「顺这条线还连着什么」「谁跟这个共现」）
  → **直接用 `kb_activate` / `kb_neighbors`**——KB 独占、grep 结构上到不了的联想维度。

即：**度量证明了价值在哪（联想），消费就该往哪走。** 遇设计/关系题直接叫「联想召回」带路，
而非退回关键词检索台。此路由已编码进 `kb_overview` 的 `routing` 段（消费者定向第一站即见）。

## 十、Pillar A+ 提及边：白盒联想的真正建立（守密人 2026-07-04 追问，含对 §八 的诚实修正）

守密人两问点破：Q1「OKF 的联想 vs 向量搜索是什么关系」→ **向量=黑盒联想**（不透明相似度标量、
无类型、需 ML、不可审计）；**OKF=白盒联想**（带类型/方向/理由的边、零 ML、可单测）。银芯选 OKF
正因零 ML/可审计红线：不借黑盒模型也要拿到（带类型）联想。二者互补——向量是更好的「搜索」，
OKF 是可解释的结构层。

Q2「孤岛显然有很多联系，能建吗」→ **能，而且 §八『几无可加的干净边』是艾瑞卡当时太保守**：
只找「结构化字段里的引用」，把「从正文挖提及」误当模糊噪声否了。**关系其实在正文里**——一份
关于沙耶的报告正文写了「沙耶」，是确定的高信号引用，和已在用的「同篇 lore 共现」同一类。

**Pillar A+ 提及边**（`build_graph`）：用领域词典建概念词表（72 角色名等 distinctive CJK 专名），
扫每个概念指向的**策展正文源**（memory/resource/assets/story/projects 文档，只扫小文本、绝不碰归档），
字面点名即建带类型 `mention` 边（激活权重 0.6）。**白盒、零 ML、可解释可单测、只连真被点名的
（防噪声星）**。实测**孤立率 65%→37%**（+214 提及边），search-tier 孤立 96%→44%，golden MRR 0.775→0.80。

诚实边界：提及边连的是「A 点名了 B」——不如向量能连「从没被一起提过、但语义相近」的隐藏关系；
那是白盒的取舍：**只连说得清理由的边。** 度量的价值仍是「每个孤立都是有意的」，只是现在**更多
孤立被证明其实有据可连**。
